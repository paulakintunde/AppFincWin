---
phase: 02-record
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260926000100_categories.sql
  - supabase/migrations/20260926000200_transactions_record_fields.sql
  - supabase/tests/database/25_categories.test.sql
  - supabase/tests/database/26_transactions_record_fields.test.sql
autonomous: true
requirements: [REC-01, REC-02, REC-03, REC-04, REC-07, ACT-01, ACT-03]

must_haves:
  truths:
    - "Categories are per-user rows, RLS-scoped to auth.uid(), never visible to another user (D-33)"
    - "Every user has the 13 built-in categories plus system-owned Transfer and Settlement, seeded at signup and back-filled idempotently for users who signed up before this migration (D-34, RESEARCH Pitfall 7)"
    - "Built-in categories can be renamed, recoloured and archived; system categories cannot be changed at all; colours are one of the 7 prototype swatch keys (D-34, D-35)"
    - "Transactions gain name, category_id, payment_type, status (pending/paid/skipped, default paid), deleted_at, import_batch_id, recurring_series_id, occurrence_date and updated_by as additive columns (D-01, D-16, D-30, D-37, Phase 1 D-26/D-27)"
    - "A transaction can only reference a category owned by the acting user or by the row's creator"
    - "Every client read goes through transactions_active, which hides soft-deleted rows under the caller's own RLS (D-30, RESEARCH Pitfall 1)"
    - "Cross-month name search is served by a pg_trgm GIN index (ACT-03, RESEARCH Pitfall 6)"
  artifacts:
    - path: "supabase/migrations/20260926000100_categories.sql"
      provides: "categories table, RLS, seed_user_categories(), handle_new_user() seeding, back-fill, set_updated_by()"
      contains: "create table public.categories"
    - path: "supabase/migrations/20260926000200_transactions_record_fields.sql"
      provides: "additive transaction columns, guard_transaction_category, transactions_active view, trigram index, grants"
      contains: "transactions_active"
    - path: "supabase/tests/database/25_categories.test.sql"
      provides: "pgTAP proof for categories RLS, seed and immutability"
    - path: "supabase/tests/database/26_transactions_record_fields.test.sql"
      provides: "pgTAP proof for new columns, grants, category guard and soft-delete view"
  key_links:
    - from: "public.handle_new_user()"
      to: "public.seed_user_categories(uuid)"
      via: "perform call inside the signup trigger"
      pattern: "seed_user_categories\\(new.id\\)"
    - from: "public.transactions_active"
      to: "public.transactions"
      via: "security_invoker view filtering deleted_at is null"
      pattern: "security_invoker"
---

<objective>
Add the Record schema foundation: the per-user `categories` table with seeding, and the additive transaction columns, guards, the soft-delete-filtering `transactions_active` view and the search index — each proven by pgTAP against the local stack.

Purpose: REC-01..04, REC-07, ACT-01, ACT-03 all read or write these columns; every later Record migration (02-08, 02-09) builds on them.
Output: two migrations and two pgTAP files. Production push happens in plan 02-31.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-record/02-CONTEXT.md
@.planning/phases/02-record/02-RESEARCH.md
@supabase/migrations/20260922000100_household_of_one.sql
@supabase/migrations/20260924000100_custom_currencies.sql
@supabase/migrations/20260924000300_accounts.sql
@supabase/migrations/20260924000400_transactions.sql
@supabase/tests/database/05_accounts_transactions.test.sql
@supabase/tests/database/08_custom_currencies_prefs.test.sql
@.squawk.toml

<interfaces>
Existing shared objects to reuse (do not redefine): `public.bump_version()` trigger function; `public.user_household_ids()`; RLS form `(select auth.uid())`.
Existing `public.handle_new_user()` body (20260922000100_household_of_one.sql section 5) inserts profiles, households, household_members — the replacement MUST keep those three statements byte-for-byte and add the seed call.
Built-in keys and colour keys must equal src/engine/categorize/builtins.ts (plan 02-06):
Housing green, Utilities slate, Groceries teal, Transport slate, Insurance blue, Health teal, Subscriptions plum, Debt rust, Savings green, Business ochre, Tax rust, Dining ochre, Income green; system: Transfer slate, Settlement slate.
Payment types (D-37, prototype PTYPE line 3357) as snake_case DB values: out = card, bank_transfer, direct_debit, standing_order, cash; in = direct_deposit, invoice, transfer, card_payout, cash.
Migration gates: `npm run lint:migrations` (squawk + contract-ok markers) and `npm run verify:migrations`. Additive only: no drop/rename/type change; NOT NULL only with a DEFAULT.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: categories migration + pgTAP</name>
  <files>supabase/migrations/20260926000100_categories.sql, supabase/tests/database/25_categories.test.sql</files>
  <read_first>
    - supabase/migrations/20260924000100_custom_currencies.sql (per-user RLS + column-grant pattern to copy)
    - supabase/migrations/20260922000100_household_of_one.sql (handle_new_user body to preserve exactly)
    - supabase/tests/database/08_custom_currencies_prefs.test.sql (pgTAP per-user test structure, auth.users fixture inserts, set local role / request.jwt.claims switching)
  </read_first>
  <action>
Write `supabase/migrations/20260926000100_categories.sql` with a header comment citing D-33, D-34, D-35, D-36 and stating Tax is a plain label. Contents, in order:
1. `public.set_updated_by()` — `returns trigger language plpgsql set search_path = ''`, body: `if coalesce(current_setting('fincwin.system_restamp', true), '') = 'on' then new.updated_by := old.updated_by; else new.updated_by := (select auth.uid()); end if; return new;` — a system bookkeeping update (the Phase 1 restamp GUC, reused by 02-08's materialiser) leaves updated_by alone exactly as bump_version leaves version alone; otherwise null for cron/service writes, which the undo refusal reports as a system change. `revoke execute … from public, anon, authenticated;`
2. `create table public.categories (id uuid primary key, owner_id uuid not null default auth.uid() references auth.users(id) on delete cascade, builtin_key text check (builtin_key is null or builtin_key in ('Housing','Utilities','Groceries','Transport','Insurance','Health','Subscriptions','Debt','Savings','Business','Tax','Dining','Income','Transfer','Settlement')), name text check (name is null or char_length(btrim(name)) between 1 and 60), color_key text not null check (color_key in ('green','slate','teal','blue','plum','rust','ochre')), is_system boolean not null default false, archived_at timestamptz, version integer not null default 1, updated_by uuid references auth.users(id) on delete set null, created_at timestamptz not null default now(), updated_at timestamptz not null default now(), constraint categories_named check (name is not null or builtin_key is not null), constraint categories_system_builtin check (not is_system or builtin_key in ('Transfer','Settlement')), constraint categories_owner_builtin_key unique (owner_id, builtin_key));` plus `create index categories_owner_id_idx on public.categories (owner_id);`
3. Triggers: `set_version before update … execute function public.bump_version()`; `set_updated_by before update … execute function public.set_updated_by()`; `guard_category` before update (security definer, search_path '') that forces `new.owner_id := old.owner_id; new.builtin_key := old.builtin_key; new.is_system := old.is_system;` and raises 23514 'system category is read-only' when `old.is_system`. Revoke execute on guard from public, anon, authenticated.
4. RLS enable + policies: `"owner reads own categories"` select using `owner_id = (select auth.uid())`; `"owner inserts own categories"` insert with check `owner_id = (select auth.uid()) and not is_system and builtin_key is null`; `"owner updates own categories"` update using `owner_id = (select auth.uid()) and not is_system` with check `owner_id = (select auth.uid()) and not is_system`. No delete policy (archive instead, D-36).
5. Grants: `revoke all on public.categories from anon; revoke insert, update, delete, truncate on public.categories from authenticated; grant select on public.categories to authenticated; grant insert (id, name, color_key) on public.categories to authenticated; grant update (name, color_key, archived_at) on public.categories to authenticated;`
6. `public.seed_user_categories(p_user uuid) returns void language sql security definer set search_path = ''` inserting the 15 seed rows `(gen_random_uuid(), p_user, key, color, is_system)` from a VALUES list (keys/colours from the interfaces block; is_system true only for Transfer and Settlement) with `on conflict on constraint categories_owner_builtin_key do nothing`. Revoke execute from public, anon, authenticated; grant to service_role.
7. `create or replace function public.handle_new_user()` — identical to the existing body, plus `perform public.seed_user_categories(new.id);` after the household_members insert. Re-issue `revoke execute on function public.handle_new_user() from public, anon, authenticated;`.
8. Back-fill, idempotent: `select public.seed_user_categories(p.id) from public.profiles p;`

Write `supabase/tests/database/25_categories.test.sql` (pgTAP, begin/rollback, explicit plan count) proving at least: (1) a newly inserted auth user has exactly 15 categories, 2 with is_system; (2) calling seed_user_categories again leaves 15; (3) user B selects 0 of A's rows; (4) A inserts a custom category (name 'Pets', color 'plum') OK and version = 1; (5) A insert with is_system = true throws 42501; (6) A insert naming builtin_key throws 42501; (7) A renames built-in Groceries (name 'Food shop') OK, version becomes 2 and updated_by = A; (8) A update of the Transfer row affects 0 rows; (9) color_key 'purple' throws 23514; (10) a 61-char name throws 23514; (11) anon cannot select categories; (12) a superuser update of builtin_key on a normal row is reverted by guard_category (builtin_key unchanged).
Run `npx supabase db reset --local && npx supabase test db`.
  </action>
  <verify>
    <automated>npx supabase db reset --local && npx supabase test db 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase test db` reports all files passing, including 25_categories.test.sql
    - `grep -c "perform public.seed_user_categories(new.id)" supabase/migrations/20260926000100_categories.sql` returns 1
    - `grep -c "select public.seed_user_categories(p.id) from public.profiles p" supabase/migrations/20260926000100_categories.sql` returns 1
    - `grep -cE "grant insert \(id, name, color_key\)" supabase/migrations/20260926000100_categories.sql` returns 1
    - `npm run lint:migrations` and `npm run verify:migrations` pass
  </acceptance_criteria>
  <done>Categories exist, are seeded for new and existing users, and are owner-isolated.</done>
</task>

<task type="auto">
  <name>Task 2: transaction record fields, guard, view, index + pgTAP</name>
  <files>supabase/migrations/20260926000200_transactions_record_fields.sql, supabase/tests/database/26_transactions_record_fields.test.sql</files>
  <read_first>
    - supabase/migrations/20260924000400_transactions.sql (existing columns, grants, guard trigger style)
    - supabase/migrations/20260924000300_accounts.sql (accounts table, to add updated_by)
    - supabase/tests/database/05_accounts_transactions.test.sql (fixture setup: users, fx_rates, accounts; copy it)
    - .squawk.toml (which rules are enforced)
  </read_first>
  <action>
Write `supabase/migrations/20260926000200_transactions_record_fields.sql` (header cites D-01, D-16, D-30, D-37, Phase 1 D-26/D-27; states every change is additive):
1. `alter table public.transactions add column name text check (name is null or char_length(name) between 1 and 200), add column category_id uuid references public.categories(id) on delete set null, add column payment_type text check (payment_type is null or payment_type in ('card','bank_transfer','direct_debit','standing_order','cash','direct_deposit','invoice','transfer','card_payout')), add column status text not null default 'paid' check (status in ('pending','paid','skipped')), add column deleted_at timestamptz, add column import_batch_id uuid, add column recurring_series_id uuid, add column occurrence_date date, add column updated_by uuid references auth.users(id) on delete set null;` — recurring_series_id gets its foreign key and uniqueness in plan 02-08 (the table does not exist yet); adding it now means the view below already carries it.
2. `alter table public.accounts add column updated_by uuid references auth.users(id) on delete set null;`
3. Triggers: `set_updated_by before update on public.transactions` and `on public.accounts`, both `execute function public.set_updated_by()`.
4. `public.guard_transaction_category()` (security definer, search_path '') raising `'unknown category' using errcode = '23514'` when `new.category_id is not null and not exists (select 1 from public.categories c where c.id = new.category_id and (c.owner_id = (select auth.uid()) or c.owner_id = new.created_by))` — the actor's own category, or the row creator's (so a household member editing another member's row, or 02-08's materialiser writing a series' rows, keeps the creator's category; Phase 8 adds per-member overrides); trigger `before insert or update of category_id on public.transactions`; revoke execute from public, anon, authenticated.
5. Indexes: `transactions_category_id_idx (category_id)`; `transactions_import_batch_idx (import_batch_id) where import_batch_id is not null`; `transactions_active_household_date_idx (household_id, local_date desc) where deleted_at is null`; `transactions_tombstones_idx (deleted_at) where deleted_at is not null`; `create extension if not exists pg_trgm with schema extensions;` then `create index transactions_name_trgm_idx on public.transactions using gin (name extensions.gin_trgm_ops);`
6. View: `create view public.transactions_active with (security_invoker = true) as select * from public.transactions where deleted_at is null;` `revoke all on public.transactions_active from anon, public; grant select on public.transactions_active to authenticated;` Comment: every client read path uses this view; single-row conflict lookups read the table so tombstones stay visible to the undo/version logic.
7. Grants (additive): `grant insert (name, category_id, payment_type, status, import_batch_id) on public.transactions to authenticated; grant update (name, category_id, payment_type, status, deleted_at) on public.transactions to authenticated;` — recurring_series_id, occurrence_date and updated_by stay server-only (written by 02-08/02-09 definer functions and the trigger).
If squawk flags any statement under an enforced rule, restructure the statement rather than adding a contract-ok marker (nothing here is destructive).

Write `supabase/tests/database/26_transactions_record_fields.test.sql` (copy the user/fx/account fixture block from 05_accounts_transactions.test.sql) proving at least: (1) insert without status gets 'paid'; (2) insert with status 'pending', name, payment_type 'direct_debit' and an own category succeeds; (3) payment_type 'cheque' throws 23514; (4) a 201-char name throws 23514; (5) category_id owned by user B throws 23514 for user A; (6) update of deleted_at by a member succeeds, bumps version and sets updated_by; (7) the soft-deleted row is absent from transactions_active and present in transactions; (8) user B sees zero of A's rows through transactions_active; (9) anon cannot select transactions_active; (10) client update of import_batch_id throws 42501; (11) client insert naming recurring_series_id throws 42501; (12) `has_index('public','transactions','transactions_name_trgm_idx')`; (13) an accounts rename sets accounts.updated_by.
Run the full local gate for migrations.
  </action>
  <verify>
    <automated>npx supabase db reset --local && npx supabase test db && npm run lint:migrations && npm run verify:migrations</automated>
  </verify>
  <acceptance_criteria>
    - every pgTAP file passes (01..26), proving the earlier suites still hold after handle_new_user changed
    - `grep -c "security_invoker = true" supabase/migrations/20260926000200_transactions_record_fields.sql` returns 1
    - `grep -c "gin_trgm_ops" supabase/migrations/20260926000200_transactions_record_fields.sql` returns 1
    - `grep -ciE "drop (column|table)|rename" supabase/migrations/20260926000200_transactions_record_fields.sql` returns 0
    - lint:migrations and verify:migrations pass with no new contract-ok marker
  </acceptance_criteria>
  <done>Transactions carry every Record field, tombstones are hidden from client reads, and search is indexed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> PostgREST (categories, transactions) | Untrusted payloads; RLS and column grants are the enforcing boundary |
| household member -> another member's rows | Shared household data vs per-user categories |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Information Disclosure | categories | mitigate | Owner-only RLS `owner_id = (select auth.uid())` on select/insert/update; pgTAP cross-user test |
| T-02-07-02 | Elevation of Privilege | categories.is_system / builtin_key | mitigate | Not in insert/update grants; insert policy requires `not is_system and builtin_key is null`; guard_category pins them on update |
| T-02-07-03 | Tampering | transactions.category_id | mitigate | guard_transaction_category rejects a category not owned by the actor (23514) |
| T-02-07-04 | Tampering | server-only columns (recurring_series_id, occurrence_date, updated_by, stamp columns) | mitigate | Excluded from column grants; pgTAP asserts 42501 |
| T-02-07-05 | Information Disclosure | soft-deleted rows | mitigate | transactions_active view (security_invoker) is the client read path; RLS still applies underneath |
| T-02-07-06 | Denial of Service | name search | mitigate | pg_trgm GIN index; name length capped at 200 |
</threat_model>

<verification>
- `npx supabase db reset --local && npx supabase test db` all green; `npm run lint:migrations`, `npm run verify:migrations` green.
</verification>

<success_criteria>
- Two additive migrations with pgTAP proof; nothing pushed to production yet (plan 02-31).
</success_criteria>

<output>
After completion, create `.planning/phases/02-record/02-07-SUMMARY.md`
</output>
