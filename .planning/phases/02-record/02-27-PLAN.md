---
phase: 02-record
plan: 27
type: execute
wave: 10
depends_on: ["02-19", "02-20", "02-24", "02-26"]
files_modified:
  - src/features/record/import/ImportScreen.tsx
  - src/features/record/import/MappingStep.tsx
  - src/features/record/import/ReviewStep.tsx
  - src/features/record/import/SuggestionsStep.tsx
  - src/features/record/import/__tests__/ImportScreen.test.tsx
autonomous: true
requirements: [REC-09, REC-10, REC-05]

must_haves:
  truths:
    - "A user picks (or adds) the account the file came from, reads the privacy line, and chooses a CSV file (REC-09, D-12, D-17)"
    - "The mapping step shows every detected column role, the date format being used (with an explicit choice when dates are ambiguous), the decimal mark and a sign flip, all correctable before continuing; invalid mappings block Continue with the reason (REC-10, D-11, Pitfall 5)"
    - "The review step lists what will be created, with Possible duplicate rows unticked, row issues shown, categories editable, a note when older rates are still being fetched, and an 'Import N transactions' commit (D-13, D-14, D-19)"
    - "Files over 5,000 rows show the split-the-file message (D-18)"
    - "After commit, recurring suggestions read 'Looks like {name}, {amount} {frequency}. Make it recurring?' with Make recurring / Not now (D-21)"
  artifacts:
    - path: "src/features/record/import/ImportScreen.tsx"
      provides: "ImportScreen"
      exports: ["ImportScreen"]
    - path: "src/features/record/import/ReviewStep.tsx"
      provides: "preview list with tick, duplicate tag, issues, category edit"
      contains: "importCsv.duplicate"
  key_links:
    - from: "src/features/record/import/ImportScreen.tsx"
      to: "src/features/record/import/useCsvImport.ts"
      via: "renders each stage of the hook"
      pattern: "useCsvImport\\("
---

<objective>
Build the CSV import screens that render `useCsvImport`'s stages: account + file pick, mapping, review, and post-commit recurring suggestions.

Purpose: the user-facing half of REC-09/REC-10 and D-21.
Output: one screen, three step components, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-record/02-UI-SPEC.md
@src/features/record/import/useCsvImport.ts
@src/features/record/import/importPipeline.ts
@src/features/record/accounts/AccountSheet.tsx
@src/features/record/entry/pickers/CategoryPicker.tsx

<interfaces>
From 02-26: `useCsvImport({ entry, accountId? })` -> `{ stage, start, cancel, setAccount, setMapping, setDateFormat, setDecimalMark, setNegate, continue, back, toggleRow, setRowCategory, selectAllClean, commit, acceptSuggestion, dismissSuggestion, mappingErrors, counts }`; `PreviewRow`; `RejectReason`.
From 02-24: `AccountSheet({ visible, mode: { kind: 'new', context }, onClose, onSaved(id) })`.
From 02-20: `CategoryPicker`, `OptionPicker`.
From 02-19: Screen, Sheet, Row, Pill, Chip, CategoryGlyph, EmptyState.
Copy: importCsv.* (privacy, pick, pickAccount, newAccount, mappingHeading, column.*, dateFormat, dateFormatAmbiguous, formats.*, decimal*, flipSigns, mappingError.*, reviewHeading, duplicate, issue.*, ratesPending, commit, committing, tooManyRows, unreadable, noRows, tooBig, suggestion, suggestionAccept, suggestionDismiss, doneHeading, done, back, cancel, continue), record.recurring.frequency.*.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ImportScreen shell + MappingStep</name>
  <files>src/features/record/import/ImportScreen.tsx, src/features/record/import/MappingStep.tsx, src/features/record/import/__tests__/ImportScreen.test.tsx</files>
  <read_first>
    - src/features/record/import/useCsvImport.ts
    - .planning/phases/02-record/02-UI-SPEC.md (import copy; accent only on the commit CTA; danger only on the row-count-exceeded message)
  </read_first>
  <behavior>
    - idle: account Rows (non-archived accounts) with 'Add an account' opening AccountSheet (context 'onboarding' when entry is onboarding else 'later'), the privacy line 'This file is read on your device and never uploaded.', and 'Choose a CSV file' (disabled until an account is chosen)
    - rejected: too_many_rows shows importCsv.tooManyRows in the danger colour; unreadable / no_rows / too_big show their copy; a Back action returns to idle
    - mapping: one Row per role (Date, Description, Amount, Money out, Money in, Currency) whose value is the chosen header name or 'Not used', each opening an OptionPicker of headers + 'Not used'; 'Reading dates as DD/MM/YYYY' with a format picker, and when ambiguous the dateFormatAmbiguous prompt with DMY/MDY chips; decimal mark chips; 'Money out shows as positive in this file' toggle; mapping errors listed; Continue disabled while errors exist
    - Cancel at any stage calls cancel()
  </behavior>
  <action>
Create `ImportScreen({ entry, accountId, onDone })` inside Screen; it renders the stage from useCsvImport and delegates to MappingStep / ReviewStep / SuggestionsStep. Create `MappingStep({ state, actions })` per <behavior>. Tests with a mocked useCsvImport returning each stage.
  </action>
  <verify>
    <automated>npx jest src/features/record/import/__tests__/ImportScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tests pass
    - `grep -c "importCsv.privacy" src/features/record/import/ImportScreen.tsx` returns 1
  </acceptance_criteria>
  <done>Users can choose account and file and correct the mapping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReviewStep + SuggestionsStep</name>
  <files>src/features/record/import/ReviewStep.tsx, src/features/record/import/SuggestionsStep.tsx, src/features/record/import/__tests__/ImportScreen.test.tsx</files>
  <read_first>
    - src/features/record/import/importPipeline.ts (PreviewRow)
    - src/features/record/activity/ActivityRow.tsx (row layout to echo)
  </read_first>
  <behavior>
    - review: heading 'Review {n} lines'; FlashList of PreviewRows with a tick (accessibilityRole checkbox; disabled when locked), date, name, amount formatted in its currency, category chip (glyph + name, tap -> CategoryPicker -> setRowCategory), 'Possible duplicate' tag, issue texts (importCsv.issue.*; unknownCurrency with {code}); ratesPending note shown when any included row's currency differs from home and its date is before today; primary 'Import {n} transactions' (accent) calls commit(); disabled at 0; 'committing' shows 'Importing…'
    - done: 'Import finished' and, per suggestion, 'Looks like {name}, {amount} {frequency}. Make it recurring?' with 'Make recurring' / 'Not now'; 'Done' calls onDone()
  </behavior>
  <action>
Create both components per <behavior>. Frequency text from record.recurring.frequency.<freq>; amount formatted with useMoneyFormatter. Extend tests.
  </action>
  <verify>
    <automated>npx jest src/features/record/import && npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - tests, lint and typecheck pass
    - `grep -c "importCsv.suggestion" src/features/record/import/SuggestionsStep.tsx` returns >= 1
    - `grep -rcE "#[0-9A-Fa-f]{6}" src/features/record/import/*.tsx` returns 0 for every file
  </acceptance_criteria>
  <done>The full import flow is usable on screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file content -> screen | Untrusted text rendered as plain Text (no HTML, no links) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-27-01 | Spoofing (misleading preview) | ReviewStep | mitigate | Every issue and duplicate shown per row; commit count equals ticked rows |
| T-02-27-02 | Information Disclosure | privacy copy | mitigate | Exact approved privacy line; no on-device storage claim (DSG-05) |
</threat_model>

<verification>
- `npx jest src/features/record/import`, lint, typecheck green.
</verification>

<success_criteria>
- REC-09/REC-10 usable end to end from a screen.
</success_criteria>

<output>
After completion, create `.planning/phases/02-record/02-27-SUMMARY.md`
</output>
