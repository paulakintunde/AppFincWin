---
phase: 02-record
plan: 27
type: execute
wave: 11
depends_on: ["02-19", "02-20", "02-24", "02-26", "02-39"]
files_modified:
  - src/features/record/import/ImportScreen.tsx
  - src/features/record/import/FormatStep.tsx
  - src/features/record/import/MappingStep.tsx
  - src/features/record/import/ReviewStep.tsx
  - src/features/record/import/MatchesStep.tsx
  - src/features/record/import/SuggestionsStep.tsx
  - src/features/record/import/formatSentence.ts
  - src/features/record/import/__tests__/formatSentence.test.ts
  - src/features/record/import/__tests__/ImportScreen.test.tsx
autonomous: true
requirements: [REC-09, REC-10, REC-13, REC-15, REC-16, REC-17, REC-18, REC-05]

must_haves:
  truths:
    - "A user picks (or adds) the account the file came from, reads the privacy line, and chooses a CSV, OFX or QFX file; a file holding several accounts' statements asks which one (REC-09, D-12, D-17, D-39)"
    - "Before any preview, the format step states the reading in plain words with one example row — for example 'We read this as a credit card statement. Purchases are shown as positive and payments as negative. The balance is what you owe: £1,250, which is over your £1,000 limit.' — with 'Doesn't look right? Flip the reading'; an ambiguous file shows the candidate readings and nothing can be imported until one is chosen; a remembered reading skips the step and shows 'Read the same way as your last statement from this account.' (D-41, D-42, D-53, REC-13)"
    - "The CSV mapping step shows every detected column role (including balance and money in/out type), the date format (with an explicit choice when ambiguous) and the decimal mark, all correctable; invalid mappings block Continue with the reason (REC-10, D-11)"
    - "The review step shows the reconciliation result as a plain sentence — 'Balances check out.', 'Some rows can't be checked against the balance' + 'Review them before importing. The rest reconciled.', 'The opening and closing balances don't add up' + 'The rows between them can't be checked one by one. Review them before importing.', or 'Couldn't check this file against a balance.' — with a 'Can't verify' tag on affected rows styled like 'Possible duplicate'; no colour, icon or error styling, and a negative balance is never shown as a problem (D-46, REC-15, UI-SPEC)"
    - "Possible duplicate rows are unticked, identical rows within one file are not tagged, row issues are shown, categories are editable, and a statement limit is offered as 'This statement shows a {limit} limit. Add it to {account}?' with Add limit / Skip (D-47, D-14, D-48)"
    - "Before commit, transfer suggestions read 'Looks like a payment from {account A} to {account B}. Link as a transfer?' (Link as transfer / Not a transfer), unmatched payment-like rows offer 'Looks like a transfer, but we can't tell where it went. Pick the other account.', and pending-bill matches read 'Looks like this pays the pending {name} bill. Mark it paid?' (Mark paid / Keep both) (D-52, D-55)"
    - "Files over 5,000 rows show the split-the-file message; after commit, recurring suggestions read 'Looks like {name}, {amount} {frequency}. Make it recurring?' (D-18, D-21)"
    - "Accent appears only on the commit CTA; danger only on the row-count-exceeded message; no new colours; every new control meets the 44px target and carries a descriptive accessibility label (UI-SPEC Color, Accessibility for New States)"
  artifacts:
    - path: "src/features/record/import/ImportScreen.tsx"
      provides: "ImportScreen"
      exports: ["ImportScreen"]
    - path: "src/features/record/import/FormatStep.tsx"
      provides: "format read-back, flip, ambiguous candidates"
      contains: "importCsv.format.flip"
    - path: "src/features/record/import/formatSentence.ts"
      provides: "formatSentenceKeys (pure i18n key/params assembly from a FormatProfile)"
      exports: ["formatSentenceKeys"]
    - path: "src/features/record/import/ReviewStep.tsx"
      provides: "preview list with tick, duplicate and can't-verify tags, issues, category edit, reconciliation line, limit offer"
      contains: "importCsv.reconcile"
    - path: "src/features/record/import/MatchesStep.tsx"
      provides: "transfer and mark-paid suggestion cards"
      contains: "importCsv.transfer.pair"
  key_links:
    - from: "src/features/record/import/ImportScreen.tsx"
      to: "src/features/record/import/useStatementImport.ts"
      via: "renders each stage of the hook"
      pattern: "useStatementImport\\("
---

<objective>
Build the statement import screens that render `useStatementImport`'s stages: account + file pick (and statement choice for multi-account OFX), format confirmation, CSV mapping, review with reconciliation and the limit offer, transfer and mark-paid suggestions, and post-commit recurring suggestions.

Revised 2026-09-25 for the import extension: FormatStep and MatchesStep are new, ReviewStep gains the reconciliation line, can't-verify tags and the limit offer, and the screen now drives `useStatementImport` (02-39) instead of `useCsvImport`. Copy comes from the `importCsv.*` key namespace (values say "statement", 02-10).

Purpose: the user-facing half of REC-09/REC-10/REC-13/REC-15…REC-18 and D-21.
Output: one screen, five step components, the pure sentence helper, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-record/02-UI-SPEC.md
@src/features/record/import/useStatementImport.ts
@src/features/record/import/importPipeline.ts
@src/features/record/accounts/AccountSheet.tsx
@src/features/record/entry/pickers/CategoryPicker.tsx
@src/features/record/entry/pickers/AccountPicker.tsx

<interfaces>
From 02-39: `useStatementImport({ entry, accountId? })` -> stages idle | reading | rejected | choose-statement | format | mapping | review | matches | committing | done, and the members listed in 02-39 (statementChoices/chooseStatement, profile/candidates/rememberedNote/exampleRow/flip/chooseCandidate/confirmFormat, mapping members, preview/counts/toggleRow/setRowCategory/selectAllClean, limitOffer/acceptLimit, transferRows/payMatchRows/linkTransfer/dismissTransfer/setOrphanAccount/dismissOrphan/acceptPayMatch/dismissPayMatch, commit, suggestions/acceptSuggestion/dismissSuggestion).
From 02-26: `PreviewRow { converted, check, duplicate, categoryId, included, locked, transfer, payMatch }`, `Preview { reconcile: { file }, limitOffer }`, `RejectReason`.
From 02-32: `FormatProfile { accountFamily, positiveMeans, balanceMeans, statedLimit }`.
From 02-24: `AccountSheet({ visible, mode: { kind: 'new', context }, onClose, onSaved(id) })`.
From 02-20: `CategoryPicker`, `OptionPicker`, `AccountPicker`.
From 02-19: Screen, Sheet, Row, Pill, Chip, CategoryGlyph, EmptyState.
Copy (02-10): importCsv.* (privacy, pick, pickHelper, pickAccount, newAccount, pickStatement, statementKind.*, statementOption, mappingHeading, column.*, dateFormat, dateFormatAmbiguous, formats.*, decimal*, mappingError.*, format.*, reconcile.*, limit.*, transfer.*, payMatch.*, matchesHeading, reviewHeading, duplicate, issue.*, ratesPending, commit, committing, tooManyRows, unreadable, noRows, tooBig, unsupportedFormat, unsupportedStatement, suggestion, suggestionAccept, suggestionDismiss, doneHeading, done, back, cancel, continue), record.recurring.frequency.*.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: formatSentence helper + ImportScreen shell + FormatStep + MappingStep</name>
  <files>src/features/record/import/formatSentence.ts, src/features/record/import/__tests__/formatSentence.test.ts, src/features/record/import/ImportScreen.tsx, src/features/record/import/FormatStep.tsx, src/features/record/import/MappingStep.tsx, src/features/record/import/__tests__/ImportScreen.test.tsx</files>
  <read_first>
    - src/features/record/import/useStatementImport.ts (02-39)
    - .planning/phases/02-record/02-UI-SPEC.md (Import: Format & Reconciliation; Typography role table; the D-42 example sentence; accent only on the commit CTA; danger only on the row-count-exceeded message)
  </read_first>
  <behavior>
    - formatSentenceKeys(profile, { closing, limit }) -> ordered [{ key, params }] parts: 'importCsv.format.readAs' { type: key of format.type.<family> }; then for a card 'cardPositivePurchases' (money-spent) or 'cardNegativePurchases' (money-in), for a deposit 'depositPositiveOut' / 'depositPositiveIn', for a loan 'loanPositiveOut' / 'loanPositiveIn'; then balance: owed with a limit and owed > limit -> 'balanceOwedOverLimit' { amount, limit }; owed -> 'balanceOwed' { amount }; held -> 'balanceHeld' { amount }; available -> 'balanceAvailable'; none -> nothing. Amount params are pre-formatted strings passed in. The D-42 example (card, money-spent, owed 1,250 > limit 1,000) produces exactly 'We read this as a credit card statement. Purchases are shown as positive and payments as negative. The balance is what you owe: £1,250, which is over your £1,000 limit.'
    - idle: account Rows (non-archived accounts) with 'Add an account' opening AccountSheet (context 'onboarding' when entry is onboarding else 'later'), the privacy line, the pick helper 'CSV, OFX or QFX — up to 5,000 rows (about 2MB).', and 'Choose a statement file' (disabled until an account is chosen)
    - choose-statement: one Row per statement option ('Credit card in GBP · 12 lines'); tapping chooses it
    - rejected: too_many_rows in the danger colour; unreadable / no_rows / too_big / unsupported_format / unsupported_statement show their copy; Back returns to idle
    - format: heading 'Check how we read this'; the sentence (Body role); 'Example row' label (Label role, inkMuted) with the example's date, description and signed amount; a 'Doesn't look right? Flip the reading' control (accessibilityLabel importCsv.format.flipA11y naming the new meaning) calling flip(); primary 'Use this reading' calling confirmFormat()
    - format, ambiguous: the ambiguous prompt, then one selectable Row per candidate ('Positive amounts are money in' / 'Positive amounts are money out'); 'Use this reading' disabled until one is chosen
    - rememberedNote true: the next screen shows 'Read the same way as your last statement from this account.' as a quiet note
    - mapping: one Row per role (Date, Description, Amount, Money out, Money in, Money in or out, Balance, Currency, Limit) whose value is the chosen header or 'Not used', each opening an OptionPicker; date-format picker with the ambiguous DMY/MDY chips; decimal mark chips; errors listed; Continue disabled while errors exist (the old 'Money out shows as positive' toggle is gone — the flip lives on the format step)
    - Cancel at any stage calls cancel()
  </behavior>
  <action>
Write `src/features/record/import/formatSentence.ts` first (pure, tested; no React):
```typescript
export interface SentencePart { key: string; params?: Record<string, string> }
export function formatSentenceKeys(profile: FormatProfile, figures: { closing: string | null; limit: string | null; overLimit: boolean }): SentencePart[];
```
Then create `ImportScreen({ entry, accountId, onDone })` inside Screen, rendering the stage from useStatementImport and delegating to FormatStep / MappingStep / ReviewStep / MatchesStep / SuggestionsStep; `FormatStep({ state, actions })` and `MappingStep({ state, actions })` per <behavior>. Text roles per the UI-SPEC typography table (sentence Body, example Label/inkMuted); no colour-coding of the reading. Tests with a mocked useStatementImport returning each stage, plus the formatSentence unit tests including the exact D-42 sentence.
  </action>
  <verify>
    <automated>npx jest src/features/record/import/__tests__/formatSentence.test.ts src/features/record/import/__tests__/ImportScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tests pass, including one asserting the exact D-42 example sentence
    - `grep -c "importCsv.privacy" src/features/record/import/ImportScreen.tsx` returns 1 and `grep -c "importCsv.pickHelper" src/features/record/import/ImportScreen.tsx` returns 1
    - `grep -c "importCsv.format.flip" src/features/record/import/FormatStep.tsx` returns >= 1 and `grep -c "importCsv.format.ambiguous" src/features/record/import/FormatStep.tsx` returns 1
    - `grep -c "flipSigns" src/features/record/import/MappingStep.tsx` returns 0
  </acceptance_criteria>
  <done>Users choose account, file and statement, confirm or flip the reading, and correct the mapping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReviewStep (reconciliation, tags, limit offer)</name>
  <files>src/features/record/import/ReviewStep.tsx, src/features/record/import/__tests__/ImportScreen.test.tsx</files>
  <read_first>
    - src/features/record/import/importPipeline.ts (PreviewRow, Preview)
    - src/features/record/activity/ActivityRow.tsx (row layout to echo)
    - .planning/phases/02-record/02-UI-SPEC.md (Reconciliation result; "Can't verify" reuses the "Possible duplicate" chip; the addendum row for ends-only mismatch)
  </read_first>
  <behavior>
    - reconciliation line above the list (Body role, plain ink/inkMuted, no icon): file 'all-verified' -> 'Balances check out.'; 'partial' -> heading 'Some rows can't be checked against the balance' + body 'Review them before importing. The rest reconciled.'; 'ends-only-mismatch' -> heading 'The opening and closing balances don't add up' + body 'The rows between them can't be checked one by one. Review them before importing.' (never 'The rest reconciled.'); 'none-in-file' -> 'Couldn't check this file against a balance.'
    - rows with check 'cannot-verify' carry the 'Can't verify' tag (fill1 chip, inkMuted Label text, accessibilityLabel 'Can't verify against the balance'), stay tickable
    - heading 'Review {n} lines'; FlashList of PreviewRows with a tick (accessibilityRole checkbox; disabled when locked), date, name, amount formatted in its currency with the minus sign, category chip (tap -> CategoryPicker -> setRowCategory), 'Possible duplicate' tag, issue texts (importCsv.issue.* incl. conflictingMarkers, badBalance); ratesPending note when any included row's currency differs from home and its date is before today
    - a row showing a negative running balance renders no warning, tag or colour for it
    - limit offer card when limitOffer is set: 'This statement shows a {limit} limit. Add it to {account}?' with 'Add limit' / 'Skip' (acceptLimit(true/false)); never blocks
    - primary 'Import {n} transactions' (accent) when no matches remain, otherwise 'Continue' to the matches step; disabled at 0 included rows and 0 accepted matches; 'committing' shows 'Importing…'
  </behavior>
  <action>
Create ReviewStep per <behavior>. Tags share one small internal Tag component (same visual treatment for both tags, distinguished by words only). Colours from useTheme() tokens only. Extend tests (one per reconciliation state, a cannot-verify row, a negative-balance row with no tag, the limit card).
  </action>
  <verify>
    <automated>npx jest src/features/record/import/__tests__/ImportScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tests pass
    - `grep -cE "importCsv\\.reconcile\\.(allVerified|partialHeading|partialBody|endsOnlyHeading|endsOnlyBody|noneInFile|rowTag)" src/features/record/import/ReviewStep.tsx` returns 7
    - `grep -c "importCsv.limit.offer" src/features/record/import/ReviewStep.tsx` returns 1
    - `grep -cE "danger|warn1" src/features/record/import/ReviewStep.tsx` returns 0 (reconciliation and balances are never colour-coded)
  </acceptance_criteria>
  <done>The review shows whether the balances check out, which rows can't be verified, and offers the statement's limit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MatchesStep (transfers, mark paid) + SuggestionsStep</name>
  <files>src/features/record/import/MatchesStep.tsx, src/features/record/import/SuggestionsStep.tsx, src/features/record/import/__tests__/ImportScreen.test.tsx</files>
  <read_first>
    - src/features/record/entry/pickers/AccountPicker.tsx (reuse for the orphan's other account)
    - .planning/phases/02-record/02-UI-SPEC.md (Transfer-pair suggestions; the D-55 addendum row; Accessibility for New States)
  </read_first>
  <behavior>
    - matches: heading 'Before importing'; one card per transfer 'pair' row: 'Looks like a payment from {from} to {to}. Link as a transfer?' (from/to = account names in money-out -> money-in order), accessibilityLabel importCsv.transfer.pairA11y, actions 'Link as transfer' / 'Not a transfer'; a 'choose' row renders one pair card per option, and linking one dismisses the others
    - orphan rows: 'Looks like a transfer, but we can't tell where it went. Pick the other account.' with an inline AccountPicker (labelled 'Pick the other account for this transfer', excluding the target account); when the picked account's currency differs, an 'Amount received in {account}' field parsed by the strict parser; 'Not a transfer' dismisses
    - pay-match rows: 'Looks like this pays the pending {name} bill. Mark it paid?' with 'Mark paid' / 'Keep both', accessibilityLabel importCsv.payMatch.a11y
    - both card types use the recurring-suggestion card treatment (Body role, two actions at the foot, 44px targets); primary 'Import {n} transactions' (accent) calls commit()
    - done: 'Import finished' and, per suggestion, 'Looks like {name}, {amount} {frequency}. Make it recurring?' with 'Make recurring' / 'Not now'; 'Done' calls onDone()
  </behavior>
  <action>
Create both components per <behavior>. Frequency text from record.recurring.frequency.<freq>; amounts formatted with useMoneyFormatter. Extend tests.
  </action>
  <verify>
    <automated>npx jest src/features/record/import && npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - tests, lint and typecheck pass
    - `grep -c "importCsv.transfer.pair" src/features/record/import/MatchesStep.tsx` returns >= 1, `grep -c "importCsv.transfer.orphan" src/features/record/import/MatchesStep.tsx` returns 1 and `grep -c "importCsv.payMatch.suggestion" src/features/record/import/MatchesStep.tsx` returns 1
    - `grep -c "importCsv.suggestion" src/features/record/import/SuggestionsStep.tsx` returns >= 1
    - `grep -rcE "#[0-9A-Fa-f]{6}" src/features/record/import/*.tsx` returns 0 for every file
  </acceptance_criteria>
  <done>The full statement import flow — including transfer and bill matches — is usable on screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file content -> screen | Untrusted text rendered as plain Text (no HTML, no links) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-27-01 | Spoofing (misleading preview) | ReviewStep | mitigate | Every issue, duplicate and can't-verify row shown; commit count equals ticked rows; ends-only mismatch never claims the rest reconciled |
| T-02-27-02 | Information Disclosure | privacy copy | mitigate | Exact approved privacy line; no on-device storage claim (DSG-05) |
| T-02-27-03 | Tampering (sign misread committed) | FormatStep | mitigate | Plain-words reading + example row + flip before any preview; ambiguous blocks commit (D-42) |
| T-02-27-04 | Repudiation (overdrawn shown as an error) | ReviewStep | mitigate | No danger/warn styling anywhere in the review; negative balances untagged (D-46, D-49) |
| T-02-27-05 | Tampering (wrong link) | MatchesStep | mitigate | Each suggestion names both accounts in text and accessibility label before the user accepts (D-52) |
</threat_model>

<verification>
- `npx jest src/features/record/import`, lint, typecheck green.
</verification>

<success_criteria>
- REC-09/REC-10/REC-13/REC-15…REC-18 usable end to end from a screen.
</success_criteria>

<output>
After completion, create `.planning/phases/02-record/02-27-SUMMARY.md`
</output>
