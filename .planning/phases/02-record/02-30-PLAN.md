---
phase: 02-record
plan: 30
type: execute
wave: 12
depends_on: ["02-24", "02-26", "02-29"]
files_modified:
  - src/features/record/setup/firstAccountGate.ts
  - src/features/record/setup/SetupAccountScreen.tsx
  - src/features/record/setup/SetupHistoryScreen.tsx
  - app/(app)/setup/account.tsx
  - app/(app)/setup/history.tsx
  - app/(app)/activity.tsx
  - src/features/you/YouScreen.tsx
  - src/features/system/routeDecision.ts
  - src/features/record/setup/__tests__/setup.test.tsx
autonomous: true
requirements: [REC-09, ANL-05, REC-08, REC-11, REC-07]

must_haves:
  truths:
    - "A signed-in user with no accounts is taken to create their first account before anything else in Record (D-22)"
    - "Right after the first account, the user is offered 'Bring your history' (CSV import, onboarding entry) or 'Start fresh', and can skip import entirely (D-22, REC-09 'during onboarding')"
    - "Import stays reachable later from You and from an account's detail screen (D-22, REC-09 'or later')"
    - "The You screen links to Activity, Accounts, Categories, Import CSV and History; History is available to every user (D-32)"
    - "A signed-in user lands on Activity (the dogfoodable home until Shell's tabs arrive)"
    - "The onboarding choice fires onboarding_history_choice; together with sign_in_completed, account_created, transaction_added and import_* events the signup -> first entry -> first import funnel is measurable in PostHog (ANL-05)"
  artifacts:
    - path: "src/features/record/setup/SetupHistoryScreen.tsx"
      provides: "Bring your history / Start fresh step"
      contains: "onboarding_history_choice"
    - path: "src/features/record/setup/firstAccountGate.ts"
      provides: "needsFirstAccount (pure)"
      exports: ["needsFirstAccount"]
    - path: "src/features/you/YouScreen.tsx"
      provides: "Money section entry points"
      contains: "you.record"
  key_links:
    - from: "app/(app)/activity.tsx"
      to: "/setup/account"
      via: "Redirect when needsFirstAccount"
      pattern: "setup/account"
---

<objective>
Add the Record onboarding step (first account, then "Bring your history" or "Start fresh"), the You-screen entry points, and make Activity the signed-in landing screen.

Purpose: D-22 and the ANL-05 funnel's middle steps; REC-09's "during onboarding or later".
Output: gate logic, two setup screens and routes, edited Activity route, You screen and route decision, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-record/02-CONTEXT.md
@.planning/phases/02-record/02-UI-SPEC.md
@src/features/you/YouScreen.tsx
@src/features/system/routeDecision.ts
@app/(app)/activity.tsx
@src/features/record/accounts/AccountSheet.tsx

<interfaces>
From 02-24: `AccountSheet({ visible, mode: { kind: 'new', context: 'onboarding' | 'later' }, onClose, onSaved(id) })`.
From 02-29: routes /activity, /accounts, /accounts/[id], /categories, /history, /import (params entry, accountId).
Existing (Phase 0 00-18): YouScreen composes `SettingsGroup` sections (Appearance, Privacy, Connection, Account) — add a 'Money' group with Rows; (00-17) `routeDecision` decides the signed-in destination.
Analytics: `track('onboarding_history_choice', { choice: 'import' | 'fresh' })`.
Copy: setup.accountHeading/accountBody, importCsv.onboardingHeading/onboardingBody/onboardingImport/onboardingFresh/privacy, you.record.*.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: First-account gate and setup screens</name>
  <files>src/features/record/setup/firstAccountGate.ts, src/features/record/setup/SetupAccountScreen.tsx, src/features/record/setup/SetupHistoryScreen.tsx, app/(app)/setup/account.tsx, app/(app)/setup/history.tsx, app/(app)/activity.tsx, src/features/record/setup/__tests__/setup.test.tsx</files>
  <read_first>
    - app/(app)/activity.tsx (from 02-29)
    - src/features/record/accounts/AccountSheet.tsx
  </read_first>
  <behavior>
    - needsFirstAccount({ loading: true }) -> false; ({ loading: false, accounts: [] }) -> true; accounts only archived -> true; any active account -> false; accounts query errored (offline, nothing cached) -> false (never trap the user)
    - SetupAccountScreen shows 'Your first account' / 'Add the account you use most. More can be added later.' and the AccountSheet content with context 'onboarding'; on save navigates to /setup/history with the new account id
    - SetupHistoryScreen shows 'Bring your history?' with the body and privacy line; 'Bring your history' fires onboarding_history_choice {choice:'import'} and goes to /import with entry 'onboarding' and the account id; 'Start fresh' fires {choice:'fresh'} and replaces to /activity
    - the Activity route redirects to /setup/account when needsFirstAccount is true
  </behavior>
  <action>
Create `needsFirstAccount(input: { loading: boolean; isError?: boolean; accounts?: readonly Pick<AccountRow, 'archived_at'>[] }): boolean`, the two screens, the two thin routes, and add the gate (useRecordContext + useAccounts -> `<Redirect href="/setup/account" />`) to app/(app)/activity.tsx. Tests per <behavior>.
  </action>
  <verify>
    <automated>npx jest src/features/record/setup && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - tests, typecheck, lint pass
    - `grep -c "track('onboarding_history_choice'" src/features/record/setup/SetupHistoryScreen.tsx` returns 2
  </acceptance_criteria>
  <done>New users create an account, then choose import or a fresh start.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: You-screen entry points + Activity landing</name>
  <files>src/features/you/YouScreen.tsx, src/features/system/routeDecision.ts, src/features/record/setup/__tests__/setup.test.tsx</files>
  <read_first>
    - src/features/you/YouScreen.tsx and src/features/you/__tests__/YouScreen.test.tsx
    - src/features/system/routeDecision.ts and its test
  </read_first>
  <behavior>
    - YouScreen gains a 'Money' SettingsGroup with Rows: Activity (/activity), Accounts (/accounts), Categories (/categories), Import CSV (/import, entry 'you'), History (/history); existing groups unchanged
    - routeDecision: a signed-in user who has passed consent resolves to '/activity' where it previously resolved to the You screen; every other branch (update required, signed out, consent pending) unchanged
  </behavior>
  <action>
Edit YouScreen to add the group (copy you.record.*), using router.push. Edit routeDecision's signed-in destination constant to '/activity' and update src/features/system/__tests__/routeDecision.test.ts accordingly (keep every other expectation). Extend YouScreen's existing test with the five rows.
  </action>
  <verify>
    <automated>npx jest src/features && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/features` passes (including Phase 0 YouScreen/routeDecision suites, updated)
    - `grep -c "you.record.importCsv" src/features/you/YouScreen.tsx` returns 1 and `grep -c "you.record.history" src/features/you/YouScreen.tsx` returns 1
    - `grep -c "'/activity'" src/features/system/routeDecision.ts` returns >= 1
  </acceptance_criteria>
  <done>Record is reachable from You, and Activity is home.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app -> PostHog | Onboarding choice event |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-30-01 | Information Disclosure | onboarding_history_choice | mitigate | Single literal property; consent-gated track() |
| T-02-30-02 | Denial of Service (user trapped) | first-account gate | mitigate | Gate never fires while loading or on a read error |
</threat_model>

<verification>
- `npx jest src/features`, typecheck, lint green.
</verification>

<success_criteria>
- D-22 onboarding step and all Record entry points are live; the funnel is instrumented end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/02-record/02-30-SUMMARY.md`
</output>
