---
phase: 02-record
plan: 19
type: execute
wave: 2
depends_on: ["02-10"]
files_modified:
  - src/theme/typography.ts
  - src/ui/Sheet.tsx
  - src/ui/SheetHeader.tsx
  - src/ui/Row.tsx
  - src/ui/Pill.tsx
  - src/ui/Chip.tsx
  - src/ui/SwatchDot.tsx
  - src/ui/CategoryGlyph.tsx
  - src/ui/AmountDisplay.tsx
  - src/ui/ConfirmSheet.tsx
  - src/ui/ToastView.tsx
  - src/ui/EmptyState.tsx
  - src/ui/__tests__/recordPrimitives.test.tsx
autonomous: true
requirements: [REC-01, REC-07, ACT-01]

must_haves:
  truths:
    - "Every Record screen is composed from one shared set of list and sheet primitives styled only with the existing tokens (UI-SPEC Design System, DSG-02)"
    - "Sheets use the 28px top radius token and are plain components Phase 3 can re-host in its sheet infrastructure without prop changes (UI-SPEC, NAV-03 later)"
    - "Every tappable primitive is at least 44px tall and carries an accessibility role and label (UI-SPEC Accessibility)"
    - "Category tiles and swatches use only the 7 category swatch pairs; accent is used only on primary CTAs, selected pills and focus; danger only on destructive actions (D-35, UI-SPEC Color)"
  artifacts:
    - path: "src/ui/Sheet.tsx"
      provides: "Sheet (modal bottom sheet container)"
      contains: "radii.sheetTop"
    - path: "src/ui/SwatchDot.tsx"
      provides: "SwatchDot colour picker dot"
      contains: "categorySwatch"
    - path: "src/ui/ToastView.tsx"
      provides: "presentational toast with optional action"
      contains: "accessibilityLiveRegion"
  key_links:
    - from: "src/ui/*.tsx"
      to: "src/theme useTheme / textRole"
      via: "all colour and type resolved through the theme"
      pattern: "useTheme\\(\\)"
---

<objective>
Build the first list and sheet primitives in `src/ui/` (UI-SPEC: "build them once, reuse across every Record screen") and add the sheet-title text role.

Purpose: every Phase 2 screen plan composes these; nothing in a feature screen re-styles a row, pill or sheet.
Output: 11 primitive components, one text role, one test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-record/02-UI-SPEC.md
@src/ui/Screen.tsx
@src/ui/SyncStatusLine.tsx
@src/ui/__tests__/SyncStatusLine.test.tsx
@src/theme/ThemeProvider.tsx
@src/theme/typography.ts
@src/theme/layout.ts
@src/theme/tokens.ts

<interfaces>
`useTheme() -> { colors (incl. accent), pairing, fonts, … }`; `textRole(pairing, 'display' | 'heading' | 'body' | 'label')`; `fontSize` (meta 12.5, eyebrow 11.5, amountDisplay 44 from 02-10); `radii` (card 26, pill 999, glyphTile 11, sheetTop 28 from 02-10); `space` (screenH 22, cardPad 16, sheetPadBottom 30, sheetCornerPad 22, groupGap 18, rowPad 15, rowPadDense 13, gapSm 8, gapMd 12, pillPadV 15, touchMin 44); `useScreenInsets()`; `categorySwatch[key] -> { color, tint }` and `CategorySwatchKey` (02-10).
i18n: `useT()`; keys a11y.close, a11y.dismiss, a11y.swatch, categories.swatch.*.
ESLint DSG-04: no literal JSX text in src/ui — every visible string comes in through props or t().
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sheet, SheetHeader, ConfirmSheet, ToastView, EmptyState + sheetTitle role</name>
  <files>src/theme/typography.ts, src/ui/Sheet.tsx, src/ui/SheetHeader.tsx, src/ui/ConfirmSheet.tsx, src/ui/ToastView.tsx, src/ui/EmptyState.tsx, src/ui/__tests__/recordPrimitives.test.tsx</files>
  <read_first>
    - src/ui/Screen.tsx and src/ui/SyncStatusLine.tsx (style conventions: useTheme, textRole, StyleSheet)
    - src/ui/__tests__/SyncStatusLine.test.tsx (render harness and mocks)
    - src/theme/typography.ts (textRole switch)
  </read_first>
  <behavior>
    - Sheet renders children only when visible; pressing the backdrop or the Android back action calls onDismiss; container has top-left/top-right radius 28
    - SheetHeader shows the title with the sheetTitle role and a Cancel control labelled from props with accessibilityRole button
    - ConfirmSheet shows body text and two actions; the destructive action uses colors.danger text; onConfirm/onCancel fire
    - ToastView renders message and optional action; accessibilityLiveRegion 'polite'; action and dismiss are buttons >= 44px
    - EmptyState renders heading and body
    - textRole(pairing, 'sheetTitle') -> fontSize 16, lineHeight 19.2, letterSpacing -0.16, 600 weight face
  </behavior>
  <action>
Add `'sheetTitle'` to `TextRole` in src/theme/typography.ts: `{ fontFamily: p.body[600], fontSize: fontSize.sheetTitle, lineHeight: fontSize.sheetTitle * 1.2, letterSpacing: fontSize.sheetTitle * -0.01 }`.
Create:
- `Sheet({ visible, onDismiss, children, accessibilityLabel })` — RN `Modal` (transparent, animationType 'slide', onRequestClose=onDismiss) with a full-screen backdrop `Pressable` (accessibilityRole 'button', label t('a11y.close')) and a bottom `View` (colors.surface, borderTopLeftRadius/borderTopRightRadius = radii.sheetTop, paddingHorizontal/paddingTop = space.sheetCornerPad, paddingBottom = space.sheetPadBottom + insets.bottom) inside a KeyboardAvoidingView (behavior 'padding' on iOS). Header comment: Phase 3 (NAV-03) swaps internals for @gorhom/bottom-sheet with drag-to-dismiss; props stay the same.
- `SheetHeader({ title, cancelLabel, onCancel })`.
- `ConfirmSheet({ visible, body, cancelLabel, confirmLabel, destructive, onConfirm, onCancel })` built on Sheet + Pill.
- `ToastView({ message, actionLabel, onAction, onDismiss, dismissLabel })` — ink background (colors.ink), surface text, radius radii.pill, action text in the label role; `accessibilityRole="alert"`, `accessibilityLiveRegion="polite"`.
- `EmptyState({ heading, body })` — heading in body role, body in label role muted.
Write tests per <behavior>.
  </action>
  <verify>
    <automated>npx jest src/ui src/theme</automated>
  </verify>
  <acceptance_criteria>
    - tests pass
    - `grep -c "radii.sheetTop" src/ui/Sheet.tsx` returns >= 1
    - `grep -rcE "#[0-9A-Fa-f]{6}" src/ui/Sheet.tsx src/ui/SheetHeader.tsx src/ui/ConfirmSheet.tsx src/ui/ToastView.tsx src/ui/EmptyState.tsx` returns 0 for every file
  </acceptance_criteria>
  <done>Sheet, confirmation, toast and empty-state primitives exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Row, Pill, Chip, SwatchDot, CategoryGlyph, AmountDisplay</name>
  <files>src/ui/Row.tsx, src/ui/Pill.tsx, src/ui/Chip.tsx, src/ui/SwatchDot.tsx, src/ui/CategoryGlyph.tsx, src/ui/AmountDisplay.tsx, src/ui/__tests__/recordPrimitives.test.tsx</files>
  <read_first>
    - src/theme/tokens.ts (categorySwatch)
    - src/theme/layout.ts (space, radii)
  </read_first>
  <behavior>
    - Row: label + optional value + optional leading node + optional trailing chevron; minHeight 44; dense uses space.rowPadDense else space.rowPad; onPress makes it a button
    - Pill variants: 'primary' (accent background, surface text), 'secondary' (fill1 background, ink text), 'danger' (danger text on dangerTint1), 'selected' state uses accent border; disabled lowers opacity and blocks onPress; vertical padding space.pillPadV; minHeight 44
    - Chip: label role, fill1 inactive, accentTint1 background + accent text when selected; accessibilityState { selected }
    - SwatchDot({colorKey, selected, onPress}) renders categorySwatch[colorKey].color; 44x44 touch target; accessibilityLabel t('a11y.swatch', { colour: t(`categories.swatch.${colorKey}`) }); accessibilityState { selected }
    - CategoryGlyph({colorKey, letter}) 11px-radius tile, tint background, colour letter
    - AmountDisplay({text, tone}) uses fontSize.amountDisplay, 500 weight, letterSpacing -0.04 * 44; tone 'danger' uses colors.danger
  </behavior>
  <action>
Create each component with the props in <behavior>, colours only from `useTheme().colors` and `categorySwatch`, sizes only from `fontSize`/`space`/`radii`. No icon library: the chevron is the Phase 0 convention of a small rotated-border View. Extend the test file.
  </action>
  <verify>
    <automated>npx jest src/ui && npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - tests, lint (incl. i18next/no-literal-string) and typecheck pass
    - `grep -rcE "#[0-9A-Fa-f]{6}|rgba\(" src/ui/Row.tsx src/ui/Pill.tsx src/ui/Chip.tsx src/ui/SwatchDot.tsx src/ui/CategoryGlyph.tsx src/ui/AmountDisplay.tsx` returns 0 for every file
    - `grep -c "vector-icons" package.json` returns 0
  </acceptance_criteria>
  <done>All list/sheet primitives exist, token-only and accessible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Presentational components; no data or network access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-19-01 | Spoofing (misleading UI) | Pill/ConfirmSheet | mitigate | Destructive actions always render in the danger style and require an explicit confirmation sheet where the UI-SPEC lists one |
| T-02-19-02 | Information Disclosure | ToastView | accept | Shows the user's own payee name to the user only; no logging |
</threat_model>

<verification>
- `npx jest src/ui src/theme`, lint, typecheck green.
</verification>

<success_criteria>
- A complete token-only primitive set exists for every Record screen.
</success_criteria>

<output>
After completion, create `.planning/phases/02-record/02-19-SUMMARY.md`
</output>
