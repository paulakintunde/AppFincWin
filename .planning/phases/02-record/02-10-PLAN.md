---
phase: 02-record
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/locales/en.ts
  - src/i18n/copyStatus.ts
  - src/services/analytics/catalogue.ts
  - src/services/analytics/__tests__/catalogue.typecheck.ts
  - src/theme/layout.ts
  - src/theme/typography.ts
  - src/theme/tokens.ts
autonomous: true
requirements: [ANL-05, REC-07, REC-11, REC-09, REC-13, REC-15, REC-17, REC-18]

must_haves:
  truths:
    - "Every Phase 2 screen string exists once in the typed English catalogue, in the prototype's declarative voice with typographic apostrophes, and is marked draft (UI-SPEC Copywriting Contract, DSG-04)"
    - "No Phase 2 string claims data stays on the device; the import privacy line only says the file is read on the device and never uploaded (D-17, DSG-05)"
    - "The ANL-05 funnel (signup -> first entry -> first statement import, with drop-off at each import stage) is expressible as typed PostHog events whose properties are finite literal unions — the statement format ('csv' | 'ofx' | 'qfx'), how the reading was decided and the reconciliation outcome are literal properties — with no amounts, payees, bank names, file names or free text (ANL-05 widened, Phase 0 D-18, RESEARCH Security extension)"
    - "Every string the import extension needs exists in the catalogue in the declarative voice: statement (not CSV) entry points, the format read-back sentences and flip, the ambiguity prompt, the remembered-reading note, the four reconciliation states, the limit offer, transfer and mark-paid suggestions, transfer entry and delete-both copy, limit fields, the sign toggles and every standing sentence including 'Nothing owing.' (UI-SPEC Copywriting Contract as revised 2026-09-25, D-42, D-46, D-48, D-49, D-51, D-52, D-55)"
    - "The 7 category swatch pairs are exposed as a theme map built only from existing category colour values; no new hex value is added (D-35, DSG)"
    - "The sheet top radius (28) and the entry-sheet amount figure size (44) are declared once as tokens (UI-SPEC)"
  artifacts:
    - path: "src/i18n/locales/en.ts"
      provides: "record, activity, accounts, categories, importCsv, history, undo, setup, you.record namespaces"
      contains: "Balances check out."
    - path: "src/services/analytics/catalogue.ts"
      provides: "ANL-05 funnel events"
      contains: "import_committed"
    - path: "src/theme/tokens.ts"
      provides: "categorySwatch map"
      contains: "categorySwatch"
  key_links:
    - from: "src/i18n/copyStatus.ts"
      to: "src/i18n/locales/en.ts"
      via: "every Phase 2 leaf path listed in DRAFT_COPY_KEYS"
      pattern: "importCsv"
---

<objective>
Lay down the shared contracts every Phase 2 UI plan consumes, so those plans never edit the same shared file: the complete Phase 2 copy catalogue (marked draft), the ANL-05 analytics events, and the three design-token additions (category swatch map, sheet radius, amount-display size).

Purpose: interface-first — UI plans 02-19..02-30 run in parallel only if en.ts, catalogue.ts and the theme are settled up front.
Output: edited en.ts, copyStatus.ts, analytics catalogue (+ typecheck fixture), layout/typography/tokens.

Revised 2026-09-25 for the import extension: copy for transfers, limits and standing, statement format confirmation, reconciliation, limit offers, transfer and mark-paid suggestions (UI-SPEC revision + the planner's three addendum rows), and the widened ANL-05 events. The `importCsv` key namespace keeps its name as an internal identifier (plans 02-24/02-27/02-30 grep it), but every VALUE now says "statement" (UI-SPEC: "Import CSV" becomes "Import statement").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-record/02-UI-SPEC.md
@.planning/phases/02-record/02-CONTEXT.md
@src/i18n/locales/en.ts
@src/i18n/copyStatus.ts
@src/i18n/__tests__/catalogue.test.ts
@src/services/analytics/catalogue.ts
@src/theme/tokens.ts
@src/theme/layout.ts
@src/theme/typography.ts

<interfaces>
Catalogue test rules (src/i18n/__tests__/catalogue.test.ts): no straight apostrophe in any value (use ’ U+2019); no match for /\badvice\b|recommend|you should|stays? on (this|your) device|figures stay/i; every DRAFT_COPY_KEYS entry must resolve to a string.
Pluralisation: i18next `_one`/`_other` suffixes (see sync.offlineQueued_one in en.ts).
Analytics guard (catalogue.ts): every property must be boolean or a finite string-literal union; numbers are banned (use banded literals).
Existing theme: `categoryColor`/`categoryTint` keyed by category name; `radii = { card: 26, pill: 999, glyphTile: 11 }`; `space` map; `fontSize` map.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 2 copy catalogue + draft marking</name>
  <files>src/i18n/locales/en.ts, src/i18n/copyStatus.ts</files>
  <read_first>
    - src/i18n/locales/en.ts (existing structure; keep every existing key unchanged)
    - src/i18n/copyStatus.ts
    - src/i18n/__tests__/catalogue.test.ts (the voice and apostrophe rules)
    - .planning/phases/02-record/02-UI-SPEC.md (Copywriting Contract)
  </read_first>
  <action>
Add these namespaces to `en` in src/i18n/locales/en.ts (before `a11y`), with exactly these keys and values (every apostrophe is ’, every `·` is U+00B7, every dash in copy is an em dash where shown):
```typescript
record: {
  sheet: {
    titleNewExpense: 'New expense', titleNewIncome: 'New income', titleNewTransfer: 'New transfer', titleEdit: 'Edit transaction',
    namePlaceholder: 'What is it for?', cancel: 'Cancel',
    saveExpense: 'Save expense', saveIncome: 'Save income', saveTransfer: 'Add transfer', saveChanges: 'Save changes',
    delete: 'Delete', directionOut: 'Money out', directionIn: 'Money in', directionTransfer: 'Transfer',
    field: { category: 'Category', account: 'Account', date: 'Date', paymentType: 'Payment type', repeats: 'Repeats', status: 'Status', note: 'Note', currency: 'Currency', amount: 'Amount', fromAccount: 'From account', toAccount: 'To account', amountOut: 'Amount sent', amountIn: 'Amount received' },
    transferNote: 'Transfers move money between your own accounts. They never count as income or spending, so the totals and the savings rate stay honest.',
    transferCrossCurrency: 'Recorded as {{amountA}} from {{accountA}} and {{amountB}} to {{accountB}} — each in its own currency.',
    transferEditBoth: 'Editing a transfer updates both sides.',
    transferDeleteConfirm: 'Delete this transfer? Both linked entries — {{from}} and {{to}} — will be removed.',
    transferSameAccount: 'Pick two different accounts.', transferAmountsMatch: 'Both sides of a transfer in the same currency are the same amount.',
    status: { paid: 'Paid', pending: 'Pending', skipped: 'Skipped' },
    uncategorised: 'Uncategorised', none: 'None',
    nameRequired: 'Give it a name.', accountRequired: 'Pick an account.',
    confirmDelete: 'Delete this transaction?', confirmDeleteCancel: 'Cancel', confirmDeleteProceed: 'Delete',
  },
  paymentType: { card: 'Card', bank_transfer: 'Bank transfer', direct_debit: 'Direct debit', standing_order: 'Standing order', cash: 'Cash', direct_deposit: 'Direct deposit', invoice: 'Invoice', transfer: 'Transfer', card_payout: 'Card payout' },
  repeats: {
    never: 'Doesn’t repeat', weekly: 'Every week', fortnightly: 'Every 2 weeks', monthly: 'Every month', quarterly: 'Every quarter', yearly: 'Every year',
    endLabel: 'Ends', endNever: 'Never', endOnDate: 'On a date', endAfterCount: 'After a number of times',
    endCount_one: 'After {{count}} time', endCount_other: 'After {{count}} times',
  },
  recurring: {
    scopeHeading: 'Edit this one, or this and future?', scopeThisOne: 'This one', scopeThisAndFuture: 'This and future',
    skip: 'Skip this one', end: 'End this series',
    endConfirm: 'Ends {{name}}. Paid history stays; nothing pending after {{date}} is created.', endCancel: 'Cancel', endProceed: 'End series',
    markPaid: 'Mark paid', markUnpaid: 'Mark unpaid', adjustThenMarkPaid: 'Adjust, then mark paid',
    overdue: 'Overdue', dueOn: 'Due {{date}}', projected: 'Expected',
    frequency: { weekly: 'weekly', fortnightly: 'every 2 weeks', monthly: 'monthly', quarterly: 'quarterly', yearly: 'yearly' },
  },
},
activity: {
  title: 'Activity', add: 'Add', you: 'You', accounts: 'Accounts', history: 'History',
  emptyHeading: 'Nothing in {{month}} yet.', emptyBody: 'Add the first one with the Add button.',
  transferTo: 'Transfer to {{account}}', transferFrom: 'Transfer from {{account}}', transferOther: 'Transfer',
  noMatchHeading: 'Nothing matches.', noMatchBody: 'Try a different search, or set the filter back to All.',
  searchMonth: 'Search {{month}}', searchAll: 'Search every month', searchScopeMonth: 'This month', searchScopeAll: 'Every month',
  select: 'Select', done: 'Done', selectFirst: 'Select some rows first.',
  selected_one: '{{count}} selected', selected_other: '{{count}} selected',
  bulkDelete: 'Delete', bulkMarkPaid: 'Mark paid', bulkMarkUnpaid: 'Mark unpaid',
  bulkDeleteConfirm_one: 'Delete {{count}} transaction?', bulkDeleteConfirm_other: 'Delete {{count}} transactions?',
  bulkDeleteCancel: 'Cancel', bulkDeleteProceed: 'Delete',
  totals: { in: 'In', out: 'Out', net: 'Net', stillToCome: 'Still to come', unconverted_one: '{{count}} line waiting for a rate', unconverted_other: '{{count}} lines waiting for a rate' },
  section: { pending: 'Still to come', paid: 'Paid', skipped: 'Skipped' },
  monthSwitcher: { label: 'Choose month', previous: 'Previous month', next: 'Next month' },
  filter: { all: 'All', category: 'Category', account: 'Account', amount: 'Amount', direction: 'Direction', in: 'Money in', out: 'Money out', transfers: 'Transfers', min: 'At least', max: 'At most', clear: 'Clear filters', apply: 'Show results' },
},
accounts: {
  title: 'Accounts', add: 'Add account', emptyHeading: 'No accounts yet.', emptyBody: 'Add one to start logging money in and out.',
  balanceNow: 'Balance now', otherCurrency: 'Also {{amount}} in {{code}}', stillToCome: '{{amount}} still to come', inHome: '≈ {{amount}}',
  importCsv: 'Import statement', archived: 'Archived',
  sheet: { titleNew: 'New account', titleEdit: 'Edit account', name: 'Name', kind: 'Type', currency: 'Currency', currencyFixed: 'The currency is set when the account is made.', openingBalance: 'Opening balance', amountOwed: 'Amount owed', save: 'Save account', saveChanges: 'Save changes', archive: 'Archive account', restore: 'Restore account', nameRequired: 'Give the account a name.',
    overdraftLimit: 'Overdraft limit', overdraftLimitHelp: 'Optional — leave blank if you don’t have one.', creditLimit: 'Credit limit', creditLimitHelp: 'Add it to see how close you are to your limit.',
    signInCredit: 'In credit', signOverdrawn: 'Overdrawn', signIOwe: 'I owe this', signImInCredit: 'I’m in credit' },
  standing: {
    inCredit: 'In credit.', overdrawnWithin: 'Overdrawn by {{amount}}, within a {{limit}} overdraft.', overdrawnBeyond: '{{beyond}} beyond your {{limit}} overdraft.', overdrawnNoLimit: 'Overdrawn by {{amount}}. No overdraft set.',
    cardInCredit: 'In credit by {{amount}}.', owingWithin: 'Owing {{amount}} of your {{limit}} limit.', owingNoLimit: 'Owing {{amount}}.', nothingOwing: 'Nothing owing.', overLimit: '{{over}} over the {{limit}} limit.',
    loanOwing: 'Owing {{amount}}.', loanInCredit: 'In credit by {{amount}}.',
  },
  kind: { cash: 'Cash', checking: 'Current account', savings: 'Savings', credit: 'Credit card', investment: 'Investment', loan: 'Loan', other: 'Other' },
},
categories: {
  title: 'Categories', add: 'Add category', archivedSection: 'Archived', restore: 'Restore',
  builtin: { Housing: 'Housing', Utilities: 'Utilities', Groceries: 'Groceries', Transport: 'Transport', Insurance: 'Insurance', Health: 'Health', Subscriptions: 'Subscriptions', Debt: 'Debt', Savings: 'Savings', Business: 'Business', Tax: 'Tax', Dining: 'Dining', Income: 'Income', Transfer: 'Transfer', Settlement: 'Settlement' },
  systemNote: 'Used by transfers and settlements. It can’t be changed.',
  usage_one: '{{count}} transaction', usage_other: '{{count}} transactions',
  sheet: { titleNew: 'New category', titleEdit: 'Edit category', name: 'Name', colour: 'Colour', save: 'Save category', saveChanges: 'Save changes', nameRequired: 'Give the category a name.', archive: 'Archive', merge: 'Merge into another category' },
  removeInUse_one: '{{name}} is used by {{count}} transaction. Merge it into another category, or archive {{name}} and keep its history.',
  removeInUse_other: '{{name}} is used by {{count}} transactions. Merge them into another category, or archive {{name}} and keep its history.',
  removeCancel: 'Cancel', removeMerge: 'Merge', removeArchive: 'Archive', mergePick: 'Merge {{name}} into',
  swatch: { green: 'Green', slate: 'Slate', teal: 'Teal', blue: 'Blue', plum: 'Plum', rust: 'Rust', ochre: 'Ochre' },
},
importCsv: { // key namespace kept for stability; every value says "statement" (D-39, UI-SPEC revision)
  entry: 'Import statement', onboardingHeading: 'Bring your history?', onboardingBody: 'Import a statement from your bank, or start with an empty month.',
  onboardingImport: 'Bring your history', onboardingFresh: 'Start fresh',
  privacy: 'This file is read on your device and never uploaded.',
  pick: 'Choose a statement file', pickHelper: 'CSV, OFX or QFX — up to 5,000 rows (about 2MB).', pickAccount: 'Which account is this file from?', newAccount: 'Add an account',
  pickStatement: 'This file holds more than one account. Which one is this import for?',
  statementKind: { bank: 'Bank account', card: 'Credit card' },
  statementOption_one: '{{kind}} in {{currency}} · {{count}} line', statementOption_other: '{{kind}} in {{currency}} · {{count}} lines',
  mappingHeading: 'Check the columns',
  column: { date: 'Date', description: 'Description', amount: 'Amount', debit: 'Money out', credit: 'Money in', currency: 'Currency', balance: 'Balance', direction: 'Money in or out', limit: 'Limit', ignore: 'Not used' },
  format: {
    heading: 'Check how we read this',
    readAs: 'We read this as {{type}}.',
    type: { deposit: 'a bank account statement', card: 'a credit card statement', loan: 'a loan statement' },
    depositPositiveIn: 'Money in is shown as positive and money out as negative.',
    depositPositiveOut: 'Money out is shown as positive and money in as negative.',
    cardPositivePurchases: 'Purchases are shown as positive and payments as negative.',
    cardNegativePurchases: 'Purchases are shown as negative and payments as positive.',
    loanPositiveIn: 'Payments are shown as positive and charges as negative.',
    loanPositiveOut: 'Charges are shown as positive and payments as negative.',
    columnsDecide: 'Money in and money out are in separate columns.',
    balanceHeld: 'The balance is what the account holds: {{amount}}.',
    balanceOwed: 'The balance is what you owe: {{amount}}.',
    balanceOwedOverLimit: 'The balance is what you owe: {{amount}}, which is over your {{limit}} limit.',
    balanceAvailable: 'The balance column shows the credit still available.',
    exampleRow: 'Example row',
    flip: 'Doesn’t look right? Flip the reading',
    flipA11y: 'Flip the reading — read positive amounts as {{meaning}} instead',
    meaningIn: 'money in', meaningOut: 'money out',
    ambiguous: 'We can’t tell how this file reads its amounts. Choose how it should be read before importing.',
    candidateIn: 'Positive amounts are money in', candidateOut: 'Positive amounts are money out',
    remembered: 'Read the same way as your last statement from this account.',
    confirm: 'Use this reading',
  },
  reconcile: {
    allVerified: 'Balances check out.',
    partialHeading: 'Some rows can’t be checked against the balance', partialBody: 'Review them before importing. The rest reconciled.',
    endsOnlyHeading: 'The opening and closing balances don’t add up', endsOnlyBody: 'The rows between them can’t be checked one by one. Review them before importing.',
    noneInFile: 'Couldn’t check this file against a balance.',
    rowTag: 'Can’t verify', rowTagA11y: 'Can’t verify against the balance',
  },
  limit: { offer: 'This statement shows a {{limit}} limit. Add it to {{account}}?', add: 'Add limit', skip: 'Skip' },
  transfer: {
    pair: 'Looks like a payment from {{from}} to {{to}}. Link as a transfer?', pairA11y: 'Suggested transfer: {{from}} to {{to}}. Link as a transfer?',
    link: 'Link as transfer', notTransfer: 'Not a transfer',
    orphan: 'Looks like a transfer, but we can’t tell where it went. Pick the other account.', pickOther: 'Pick the other account for this transfer',
    counterAmount: 'Amount received in {{account}}',
  },
  payMatch: { suggestion: 'Looks like this pays the pending {{name}} bill. Mark it paid?', accept: 'Mark paid', dismiss: 'Keep both', a11y: 'Suggested match: this line pays the pending {{name}} bill. Mark it paid?' },
  matchesHeading: 'Before importing',
  dateFormat: 'Reading dates as {{format}}', dateFormatAmbiguous: 'These dates could be day-first or month-first. Pick one.',
  formats: { YMD: 'YYYY-MM-DD', DMY: 'DD/MM/YYYY', MDY: 'MM/DD/YYYY' },
  decimalMark: 'Decimal mark', decimalPoint: 'Point (1,234.56)', decimalComma: 'Comma (1.234,56)',
  mappingError: { noDate: 'Pick the date column.', noDescription: 'Pick the description column.', noAmount: 'Pick an amount column, or money out and money in.', amountAndDebitCredit: 'Use one amount column, or money out and money in, not both.', duplicateColumn: 'Each column can only be used once.' },
  continue: 'Continue', back: 'Back', cancel: 'Cancel',
  reviewHeading_one: 'Review {{count}} line', reviewHeading_other: 'Review {{count}} lines',
  duplicate: 'Possible duplicate',
  issue: { badDate: 'Date not recognised', badAmount: 'Amount not recognised', zeroAmount: 'Zero amount', emptyDescription: 'No description', unknownCurrency: 'Currency not recognised, using {{code}}', amountTooLarge: 'Amount too large', conflictingMarkers: 'Amount shows two different signs', badBalance: 'Balance not recognised' },
  ratesPending: 'Rates for older dates are still being fetched.',
  commit_one: 'Import {{count}} transaction', commit_other: 'Import {{count}} transactions', committing: 'Importing…',
  tooManyRows: 'This file has more than 5,000 rows. Split it into smaller files and import them one at a time.',
  unreadable: 'This file couldn’t be read as a statement.', noRows: 'This file has no transactions in it.', tooBig: 'This file is too large to read.',
  unsupportedFormat: 'This file isn’t a CSV, OFX or QFX statement.', unsupportedStatement: 'This file holds an investment or loan statement, which can’t be imported yet.',
  imported_one: 'Imported {{count}} line', imported_other: 'Imported {{count}} lines',
  suggestion: 'Looks like {{name}}, {{amount}} {{frequency}}. Make it recurring?', suggestionAccept: 'Make recurring', suggestionDismiss: 'Not now',
  doneHeading: 'Import finished', done: 'Done',
},
history: {
  title: 'History', label: 'Recent changes', note: 'The last twelve changes. Rolling back to one undoes everything after it as well.',
  emptyHeading: 'Nothing yet.', emptyBody: 'Every change that can be undone shows up here.',
  undoToHere: 'Undo to here', undoToHereCount_one: 'Undoes {{count}} change', undoToHereCount_other: 'Undoes {{count}} changes',
  cantUndo: 'Can’t undo', mostRecent: 'Most recent',
},
undo: {
  action: 'Undo',
  label: {
    added: 'Added · {{name}}', edited: 'Edited · {{name}}', deleted: 'Deleted · {{name}}',
    deletedMany_one: '{{count}} deleted', deletedMany_other: '{{count}} deleted',
    markedPaid: 'Marked paid · {{name}}', markedPaidMany_one: '{{count}} marked paid', markedPaidMany_other: '{{count}} marked paid',
    markedUnpaidMany_one: '{{count}} marked unpaid', markedUnpaidMany_other: '{{count}} marked unpaid',
    skipped: 'Skipped · {{name}}', imported_one: 'Imported {{count}} line', imported_other: 'Imported {{count}} lines',
    seriesCreated: 'Made recurring · {{name}}', seriesEdited: 'Series edited · {{name}}', seriesEnded: 'Series ended · {{name}}',
    categoryAdded: 'Category added · {{name}}', categoryEdited: 'Category edited · {{name}}', categoryArchived: 'Archived · {{name}}', categoryMerged: 'Merged into {{name}}',
    accountAdded: 'Account added · {{name}}', accountEdited: 'Account edited · {{name}}',
    transferAdded: 'Transfer · {{name}}', transferEdited: 'Transfer edited · {{name}}', transferDeleted: 'Transfer deleted · {{name}}',
  },
  reverted: 'Undone · {{label}}', rolledBack_one: 'Undid {{count}} change', rolledBack_other: 'Undid {{count}} changes',
  refusal: {
    member: '{{person}} edited {{record}} after this, so it can’t be undone.',
    memberUnknown: 'Someone in your household edited {{record}} after this, so it can’t be undone.',
    self: '{{record}} was changed on another device after this, so it can’t be undone.',
    system: '{{record}} was changed elsewhere after this, so it can’t be undone.',
    record: { transactions: 'this line', categories: 'this category', recurring_series: 'this series', accounts: 'this account' },
  },
  notRecorded: 'That change can’t be undone.',
},
setup: {
  accountHeading: 'Your first account', accountBody: 'Add the account you use most. More can be added later.',
},
```
Also add `record: { section: 'Money', activity: 'Activity', accounts: 'Accounts', categories: 'Categories', importCsv: 'Import statement', history: 'History' }` inside the existing `you` object, and to `a11y` add `toastUndo: 'Undo last change'`, `selectRow: 'Select {{name}}'`, `swatch: '{{colour}} colour'`, `dismiss: 'Dismiss'`.

In src/i18n/copyStatus.ts add a small pure helper `function leafPaths(prefix: string, node: unknown): string[]` (same recursion as the test's collectLeaves) and extend DRAFT_COPY_KEYS with `...leafPaths('record', en.record), ...leafPaths('activity', en.activity), ...leafPaths('accounts', en.accounts), ...leafPaths('categories', en.categories), ...leafPaths('importCsv', en.importCsv), ...leafPaths('history', en.history), ...leafPaths('undo', en.undo), ...leafPaths('setup', en.setup), ...leafPaths('you.record', en.you.record), 'a11y.toastUndo', 'a11y.selectRow', 'a11y.swatch', 'a11y.dismiss'` (import en from './locales/en'). Update the header comment to say Phase 2 copy is Claude-drafted per UI-SPEC and awaits review.
  </action>
  <verify>
    <automated>npx jest src/i18n && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - i18n tests pass (no straight apostrophes, no forbidden voice, every draft key resolves)
    - `grep -c "never uploaded" src/i18n/locales/en.ts` returns 1
    - `grep -c "Balances check out." src/i18n/locales/en.ts` returns 1, `grep -c "Nothing owing." src/i18n/locales/en.ts` returns 1, `grep -c "Mark it paid?" src/i18n/locales/en.ts` returns >= 1 and `grep -c "don’t add up" src/i18n/locales/en.ts` returns 1
    - `grep -c "'Import CSV'" src/i18n/locales/en.ts` returns 0 and `grep -c "Import statement" src/i18n/locales/en.ts` returns >= 2
    - `grep -ciE "you should|advice|recommend" src/i18n/locales/en.ts` returns 0
    - `grep -ciE "kept on this device|stays on (this|your) device" src/i18n/locales/en.ts` returns 0
    - `grep -c "leafPaths('importCsv'" src/i18n/copyStatus.ts` returns 1
    - `grep -cE "Save expense|Save income|Save changes" src/i18n/locales/en.ts` returns >= 3 (no bare "Save" primary CTA)
  </acceptance_criteria>
  <done>All Phase 2 copy exists, typed, voice-checked and marked draft.</done>
</task>

<task type="auto">
  <name>Task 2: ANL-05 events + theme token additions</name>
  <files>src/services/analytics/catalogue.ts, src/services/analytics/__tests__/catalogue.typecheck.ts, src/theme/tokens.ts, src/theme/layout.ts, src/theme/typography.ts</files>
  <read_first>
    - src/services/analytics/catalogue.ts (AssertAll guard)
    - src/services/analytics/__tests__/catalogue.typecheck.ts (proof fixture style)
    - src/theme/tokens.ts, src/theme/layout.ts, src/theme/typography.ts
    - src/theme/__tests__ (any token snapshot/count tests to keep green)
  </read_first>
  <action>
Add to `EventCatalogue` in src/services/analytics/catalogue.ts (ANL-05 widened; comment: "funnel = first sign_in_completed -> first transaction_added -> first statement import (import_committed, any format); no amounts, payees, bank names, file names or free text"):
```typescript
account_created: { context: 'onboarding' | 'later' };
onboarding_history_choice: { choice: 'import' | 'fresh' };
transaction_added: { kind: 'expense' | 'income' | 'transfer'; recurring: boolean };
import_started: { entry: 'onboarding' | 'you' | 'account' };
import_file_rejected: { reason: 'too_many_rows' | 'unreadable' | 'no_rows' | 'too_big' | 'unsupported_format' | 'unsupported_statement' };
import_format_confirmed: { format: 'csv' | 'ofx' | 'qfx'; decided_by: 'labels' | 'reconciliation' | 'remembered' | 'user'; flipped: boolean };
import_mapping_confirmed: { corrected: boolean };
import_committed: { entry: 'onboarding' | 'you' | 'account'; size: '1-50' | '51-500' | '501-5000'; format: 'csv' | 'ofx' | 'qfx'; reconciliation: 'all_verified' | 'partial' | 'none_in_file' | 'ends_only_mismatch' };
import_abandoned: { stage: 'pick' | 'format' | 'mapping' | 'review' | 'matches' };
recurring_suggestion_answered: { accepted: boolean };
transfer_suggestion_answered: { accepted: boolean; kind: 'pair' | 'orphan' };
pay_match_answered: { accepted: boolean };
```
In catalogue.typecheck.ts add one valid `track('import_committed', { entry: 'you', size: '51-500', format: 'ofx', reconciliation: 'partial' })` usage and two `// @ts-expect-error` lines: one passing `size: 42` (number rejected) and one passing `format: 'pdf'` (not a Phase 2 format), following the file's existing pattern.

Theme (no new hex values):
- src/theme/tokens.ts: after categoryTint add
```typescript
/** D-35: the 7 distinct prototype swatch pairs, keyed by the colour key stored on categories.color_key. Built only from existing values. */
export const categorySwatch = {
  green: { color: categoryColor.Housing, tint: categoryTint.Housing },
  slate: { color: categoryColor.Utilities, tint: categoryTint.Utilities },
  teal: { color: categoryColor.Groceries, tint: categoryTint.Groceries },
  blue: { color: categoryColor.Insurance, tint: categoryTint.Insurance },
  plum: { color: categoryColor.Subscriptions, tint: categoryTint.Subscriptions },
  rust: { color: categoryColor.Debt, tint: categoryTint.Debt },
  ochre: { color: categoryColor.Business, tint: categoryTint.Business },
} as const;
export type CategorySwatchKey = keyof typeof categorySwatch;
```
- src/theme/layout.ts: add `sheetTop: 28` to `radii` (comment: prototype `border-radius:28px 28px 0 0`, 25 sheets, UI-SPEC) and `rowPadDense: 13` to `space` (entry-sheet field rows).
- src/theme/typography.ts: add `amountDisplay: 44` to `fontSize` (comment: entry-sheet amount figure, display-figure exception alongside netWorth/healthScore).
  </action>
  <verify>
    <automated>npm run typecheck && npx jest src/theme src/services/analytics</automated>
  </verify>
  <acceptance_criteria>
    - typecheck passes (CATALOGUE_IS_FINITE still compiles; the @ts-expect-error line is a real error)
    - `grep -c "import_committed" src/services/analytics/catalogue.ts` returns 1 and `grep -c "format: 'csv' | 'ofx' | 'qfx'" src/services/analytics/catalogue.ts` returns 2
    - `grep -c "'transfer'" src/services/analytics/catalogue.ts` returns >= 1
    - `git diff src/theme/tokens.ts | grep -cE "^\+.*#[0-9A-Fa-f]{6}"` returns 0 (no new hex literal)
    - `grep -c "sheetTop: 28" src/theme/layout.ts` returns 1 and `grep -c "amountDisplay: 44" src/theme/typography.ts` returns 1
  </acceptance_criteria>
  <done>Funnel events and design tokens are declared once for every Phase 2 plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app -> PostHog | Analytics events leave the device only after consent (Phase 0 D-17) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10-01 | Information Disclosure | ANL-05 events | mitigate | Finite literal-union properties only (compile-time AssertAll guard); size banded, never a count or amount; format, decided_by and reconciliation are enums; no bank, header or file names; consent-gated track() |
| T-02-10-02 | Repudiation / compliance | copy | mitigate | Catalogue test forbids advice/recommend/you-should and on-device storage claims (DSG-05) |
</threat_model>

<verification>
- `npx jest src/i18n src/theme src/services/analytics` and `npm run typecheck` green.
</verification>

<success_criteria>
- UI plans can reference any Phase 2 key, event or token without editing shared files.
</success_criteria>

<output>
After completion, create `.planning/phases/02-record/02-10-SUMMARY.md`
</output>
