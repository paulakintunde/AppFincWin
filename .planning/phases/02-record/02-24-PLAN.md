---
phase: 02-record
plan: 24
type: execute
wave: 9
depends_on: ["02-14", "02-17", "02-19", "02-22", "02-37"]
files_modified:
  - src/features/record/accounts/AccountSheet.tsx
  - src/features/record/accounts/AccountBalanceBlock.tsx
  - src/features/record/accounts/AccountsScreen.tsx
  - src/features/record/accounts/AccountDetailScreen.tsx
  - src/features/record/accounts/standingText.ts
  - src/features/record/accounts/__tests__/standingText.test.ts
  - src/features/record/accounts/__tests__/accounts.test.tsx
autonomous: true
requirements: [REC-08, ANL-05, REC-09, REC-17]

must_haves:
  truths:
    - "A user can create an account (name, type, currency, opening balance) and edit its name, type and opening balance; the currency is fixed once created (REC-08)"
    - "Each account shows 'Balance now' = opening balance plus paid lines in its own currency, pending amounts as 'still to come', and any paid lines in other currencies as separate subtotals (D-10)"
    - "An account in a foreign currency also shows an approximate home-currency figure with the rate's date and attribution (Claude's discretion, Phase 1 D-13)"
    - "A user can archive and restore an account; each change is undoable (REC-11)"
    - "An account's detail screen offers Import statement for that account (D-22, D-39)"
    - "The account sheet has an optional Overdraft limit (current and savings) or Credit limit (card) in the account's currency; a loan shows neither (D-48, UI-SPEC)"
    - "A negative opening balance is entered with a segmented sign control beside the unsigned amount field — 'In credit' / 'Overdrawn' for current and savings, 'I owe this' / 'I'm in credit' for a card — and a loan's field reads 'Amount owed' with no control; the strict amount parser is not extended to accept a minus sign (D-48, D-49, UI-SPEC Account Limits & Standing)"
    - "Every account, in every state, shows a standing sentence under its balance on the list and detail screens, in the Label role, with the full sentence as its accessibility label; only 'beyond overdraft' and 'over limit' use warn1, every other tier is plain ink/inkMuted, and nothing about standing is styled or worded as an error (D-49, REC-17, UI-SPEC Color)"
    - "A card with a zero balance reads 'Nothing owing.' (UI-SPEC addendum row)"
    - "Creating an account fires account_created with context onboarding or later (ANL-05)"
  artifacts:
    - path: "src/features/record/accounts/AccountSheet.tsx"
      provides: "AccountSheet (create/edit), reused by onboarding and import"
      exports: ["AccountSheet"]
    - path: "src/features/record/accounts/AccountsScreen.tsx"
      provides: "AccountsScreen"
      exports: ["AccountsScreen"]
    - path: "src/features/record/accounts/AccountDetailScreen.tsx"
      provides: "AccountDetailScreen"
      exports: ["AccountDetailScreen"]
  key_links:
    - from: "src/features/record/accounts/AccountBalanceBlock.tsx"
      to: "src/data/queries/activity.ts useAccountBalances"
      via: "balance per account"
      pattern: "useAccountBalances\\("
    - from: "src/features/record/accounts/AccountBalanceBlock.tsx"
      to: "src/features/record/accounts/standingText.ts"
      via: "standing sentence from the engine Standing discriminant"
      pattern: "standingText\\("
---

<objective>
Build the account list, account detail and create/edit account sheet with per-account balances.

Purpose: REC-08; D-22's "reachable from an account's detail screen"; the AccountSheet is reused by onboarding (02-30) and the import flow (02-27).
Output: four components/screens, the standing-copy helper, tests.

Revised 2026-09-25 for the import extension: limit fields, the opening-balance sign control, and the standing line (D-48, D-49).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-record/02-UI-SPEC.md
@src/data/mutations/accounts.ts
@src/data/queries/currencyOptions.ts
@src/ui/RateAttribution.tsx
@src/features/record/activity/ActivityRow.tsx

<interfaces>
From 02-14: `useAccountBalances(householdId, accounts) -> { balances: Map<id, AccountBalanceView { balance, otherCurrencies, overflow, pendingSum }> }`, `projectionHomeAmount(amount, currency, home, rates)`, `useMonthView`, `useRecordContext`.
From 02-17: `useAddAccount().add(input, undo?) -> id`, `useEditAccount().edit(vars, undo?)` (undo: { stepId, ownerId }).
Existing: `useAccounts(householdId)`, `useCurrencyOptions(userId)`, `useFxLatest()`, `RateAttribution({ rateDate, rateSource, ratePending })`, `useAmountParser`, `useMoneyFormatter`, `AccountRow['kind']` = cash|checking|savings|credit|investment|loan|other.
From 02-06: `filterRows(rows, { ...EMPTY_FILTER, accountIds: [id] })`.
From 02-14 (extension): `AccountBalanceView.standing: Standing | null`.
From 02-37: `Standing` union (in-credit | overdrawn-within | overdrawn-beyond | overdrawn-no-limit | card-in-credit | owing-within | over-limit | loan-owing | loan-in-credit | plain).
From 02-17 (extension): add/edit accept `overdraft_limit`, `credit_limit`, negative `opening_balance`.
Copy (02-10): accounts.sheet.overdraftLimit/overdraftLimitHelp/creditLimit/creditLimitHelp/amountOwed/signInCredit/signOverdrawn/signIOwe/signImInCredit, accounts.standing.*.
Theme: `warn1` (existing token) for the two exceeded tiers only.
Analytics: `getAnalytics().track('account_created', { context })`.
Copy: accounts.* (the entry-point key accounts.importCsv now reads 'Import statement'), undo.label.accountAdded/accountEdited.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: standingText + AccountSheet (limits, sign control) + AccountBalanceBlock (standing line)</name>
  <files>src/features/record/accounts/standingText.ts, src/features/record/accounts/__tests__/standingText.test.ts, src/features/record/accounts/AccountSheet.tsx, src/features/record/accounts/AccountBalanceBlock.tsx, src/features/record/accounts/__tests__/accounts.test.tsx</files>
  <read_first>
    - src/data/mutations/accounts.ts
    - src/ui/money/useAmountParser.ts
    - .planning/phases/02-record/02-UI-SPEC.md (Accounts copy and focal point)
  </read_first>
  <behavior>
    - AccountSheet new: name (required, <= 60 chars), type (OptionPicker over the 7 kinds), currency (defaults to home currency, picker over useCurrencyOptions), opening balance (unsigned strict parser, empty = 0) with its sign control: checking/savings Chips 'In credit' (default) / 'Overdrawn' -> stored positive / negative; credit Chips 'I owe this' (default) / 'I'm in credit' -> stored negative / positive; loan: label 'Amount owed', stored negative, no control; cash/investment/other: 'In credit' / 'Overdrawn' like checking; primary 'Save account'; calls add(…, { stepId, ownerId }) then showToast accountAdded, track('account_created', { context }), onSaved(id)
    - limit field: checking/savings show 'Overdraft limit' with helper 'Optional — leave blank if you don’t have one.'; credit shows 'Credit limit' with helper 'Add it to see how close you are to your limit.'; loan/cash/investment/other show neither; blank -> null; parsed with the same strict parser in the account currency; the value is sent as overdraft_limit or credit_limit (the other left untouched)
    - an Overdrawn opening balance of 240.00 with an overdraft of 500.00 saves opening_balance −24000 and overdraft_limit 50000 without any error or warning copy
    - AccountSheet edit: currency shown read-only with accounts.sheet.currencyFixed; only changed fields patched; 'Save changes'; Archive / Restore account toggles archived_at through edit with undo
    - AccountBalanceBlock: 'Balance now' + balance formatted in the account currency with the minus sign always visible (never parentheses; plain ink — a negative balance is a state, D-49; the UI-SPEC's danger colour applies only to a negative balance with no understood standing, i.e. standing null); beneath it the standing line from standingText (Label role, accessibilityLabel = the sentence, warn1 only for overdrawn-beyond and over-limit); 'Also {amount} in {code}' per other currency; '{amount} still to come' when pendingSum != 0; for a foreign account, '≈ {home amount}' plus RateAttribution for the latest home rate; overflow -> shows nothing numeric and the unconverted meta copy
  </behavior>
  <action>
First create the pure helper `src/features/record/accounts/standingText.ts` (tests in `__tests__/standingText.test.ts`, written first):
```typescript
export interface StandingLine { key: string; params: Record<string, string>; tone: 'plain' | 'warn' }
export function standingText(s: Standing | null, fmt: (minor: number) => string): StandingLine | null;
```
Mapping (fmt formats in the account currency with the sign rules of useMoneyFormatter; amounts passed are magnitudes): in-credit -> accounts.standing.inCredit; overdrawn-within -> overdrawnWithin { amount, limit }; overdrawn-beyond -> overdrawnBeyond { beyond, limit }, tone 'warn'; overdrawn-no-limit -> overdrawnNoLimit { amount }; card-in-credit -> cardInCredit { amount }; owing-within with owed 0 -> nothingOwing; owing-within with limit -> owingWithin { amount, limit }; owing-within with limit null -> owingNoLimit { amount }; over-limit -> overLimit { over, limit }, tone 'warn'; loan-owing -> loanOwing { amount }; loan-in-credit -> loanInCredit { amount }; plain or null -> null. Test every branch, including exactly-at-limit (within) and one minor unit over (warn).
Then create `AccountSheet({ visible, mode: { kind: 'new'; context: 'onboarding' | 'later' } | { kind: 'edit'; account: AccountRow }, onClose, onSaved?(id: string) })` and `AccountBalanceBlock({ account, balance, homeCurrency })` per <behavior>, using the entry-sheet pickers from 02-20 (OptionPicker), Chips for the sign control (same shape as the entry sheet's direction chips, 44px touch target), and primitives. Colours from useTheme() tokens only. Tests with mocked hooks.
  </action>
  <verify>
    <automated>npx jest src/features/record/accounts</automated>
  </verify>
  <acceptance_criteria>
    - tests pass
    - `grep -c "track('account_created'" src/features/record/accounts/AccountSheet.tsx` returns 1
    - `grep -c "standingText(" src/features/record/accounts/AccountBalanceBlock.tsx` returns 1 and `grep -c "warn1" src/features/record/accounts/AccountBalanceBlock.tsx` returns >= 1
    - `grep -cE "#[0-9A-Fa-f]{6}" src/features/record/accounts/AccountSheet.tsx src/features/record/accounts/AccountBalanceBlock.tsx` returns 0 for both
    - `grep -cE "accounts\.sheet\.(signInCredit|signOverdrawn|signIOwe|signImInCredit)" src/features/record/accounts/AccountSheet.tsx` returns 4
  </acceptance_criteria>
  <done>Accounts can be created/edited/archived with undo; balances render per D-10.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AccountsScreen + AccountDetailScreen</name>
  <files>src/features/record/accounts/AccountsScreen.tsx, src/features/record/accounts/AccountDetailScreen.tsx, src/features/record/accounts/__tests__/accounts.test.tsx</files>
  <read_first>
    - src/features/record/activity/ActivityRow.tsx (reuse for the account's lines)
    - src/features/record/accounts/AccountSheet.tsx (Task 1)
  </read_first>
  <behavior>
    - AccountsScreen lists non-archived accounts (name, kind label, currency meta, the standing line beneath the balance; balance is the row's anchor), an 'Archived' section below, 'Add account' opening AccountSheet; empty state 'No accounts yet.' / 'Add one to start logging money in and out.'
    - tapping an account calls onOpenAccount(id)
    - AccountDetailScreen shows the account name, AccountBalanceBlock (with its standing line), 'Import statement' (accounts.importCsv; calls onImport(accountId)), 'Edit account' (opens AccountSheet edit), and this month's lines for the account (useMonthView rows filtered by accountId) rendered with ActivityRow; tapping a line opens TransactionSheet edit
  </behavior>
  <action>
Create `AccountsScreen({ onOpenAccount })` and `AccountDetailScreen({ accountId, onImport })` inside `Screen`, per <behavior>. No navigation chrome beyond what the route provides (Phase 3 owns back-stack). Tests with mocked hooks.
  </action>
  <verify>
    <automated>npx jest src/features/record/accounts && npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - tests, lint and typecheck pass
    - `grep -c "onImport" src/features/record/accounts/AccountDetailScreen.tsx` returns >= 1
  </acceptance_criteria>
  <done>Accounts list and detail screens exist with balances and an import entry point.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> accounts | Name/opening balance become records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-24-01 | Tampering (wrong balance) | AccountBalanceBlock | mitigate | Engine BigInt balance; other currencies shown separately; home figure labelled approximate with rate date |
| T-02-24-02 | Tampering | currency change | mitigate | Currency not editable after creation (SQL grant excludes it; UI shows it read-only) |
| T-02-24-03 | Information Disclosure | analytics | mitigate | account_created carries only context |
| T-02-24-04 | Tampering (sign error on opening balance) | sign control | mitigate | Unsigned strict parser plus an explicit, defaulted sign control per account kind; loan always owed (UI-SPEC, D-49) |
| T-02-24-05 | Repudiation (overdrawn shown as an error) | standing line | mitigate | Declarative sentences only; warn1 for two tiers, never danger; accessibility label carries the full sentence (D-49) |
</threat_model>

<verification>
- `npx jest src/features/record/accounts`, lint, typecheck green.
</verification>

<success_criteria>
- REC-08 satisfied; import entry point exists on account detail.
</success_criteria>

<output>
After completion, create `.planning/phases/02-record/02-24-SUMMARY.md`
</output>
