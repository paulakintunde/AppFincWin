---
phase: 02-record
plan: 28
type: execute
wave: 7
depends_on: ["02-14", "02-16", "02-19"]
files_modified:
  - src/features/record/history/undoCopy.ts
  - src/features/record/history/UndoToastHost.tsx
  - src/features/record/history/HistoryScreen.tsx
  - src/features/record/history/__tests__/undoCopy.test.ts
  - src/features/record/history/__tests__/history.test.tsx
autonomous: true
requirements: [REC-11, REC-12]

must_haves:
  truths:
    - "After any undoable change a toast shows the change and an Undo action; it stays 3.2s for ordinary changes and about 6s for deletes, bulk deletes and imports, and never auto-dismisses while a screen reader is on (D-31)"
    - "Tapping Undo reverses that step as a compensating write, online or offline (REC-11, D-29)"
    - "A refused undo explains who changed what: '{Person} edited {record} after this, so it can't be undone.', or the same-user-other-device / system variants (REC-12, D-26)"
    - "The History screen lists the last twelve changes newest first, lets the user 'Undo to here' (reversing every newer change too), and shows refused steps greyed as 'Can't undo' with the reason (D-27, D-28, D-32)"
    - "History is available to every user now; level gating arrives later (D-32)"
  artifacts:
    - path: "src/features/record/history/UndoToastHost.tsx"
      provides: "UndoToastHost (mounted once in the signed-in layout by 02-29)"
      exports: ["UndoToastHost"]
    - path: "src/features/record/history/HistoryScreen.tsx"
      provides: "HistoryScreen"
      exports: ["HistoryScreen"]
    - path: "src/features/record/history/undoCopy.ts"
      provides: "stepLabel, refusalText (pure over t)"
      exports: ["stepLabel", "refusalText"]
  key_links:
    - from: "src/features/record/history/UndoToastHost.tsx"
      to: "src/state/undoToast.ts useToast + src/data/mutations/undo.ts useUndo"
      via: "toast Undo action"
      pattern: "useUndo\\(\\)"
    - from: "src/features/record/history/undoCopy.ts"
      to: "src/engine/undo describeRefusal"
      via: "refusal attribution"
      pattern: "describeRefusal\\("
---

<objective>
Build the Undo toast host and the History screen, plus the pure copy helpers that turn step labels and refusals into sentences.

Purpose: REC-11 (undo from toast or history) and REC-12 (refusal with explanation) as the user sees them.
Output: copy helpers, toast host, History screen, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-record/02-UI-SPEC.md
@.planning/phases/02-record/02-CONTEXT.md
@src/state/undoToast.ts
@src/data/mutations/undo.ts
@src/data/queries/undoLog.ts

<interfaces>
From 02-15: `useToast() -> ToastState | null` ({ id, kind: 'ordinary'|'destructive'|'refusal'|'info', text: { key, params? } | null, stepId, refusal: UndoConflict | null }), `dismissToast(id)`, `TOAST_MS_ORDINARY`, `TOAST_MS_DESTRUCTIVE`.
From 02-16: `useUndo().undo({ stepId, ownerId, householdId, labelKey, labelParams })`, `useRollbackUndo().rollbackTo({ stepId, ownerId, householdId })`.
From 02-14: `useUndoLog(userId)` -> UndoLogRow[] ({ id, label_key, label_params, status, refusal, created_at }), `useHouseholdMemberNames(householdId)`, `useCategoryLookup(userId)`, `useRecordContext()`.
From 02-05: `describeRefusal(conflict, currentUserId, memberNames) -> { actor, actorName, record: {kind:'name',name}|{kind:'builtin',key}|{kind:'entity',entity} }`, `rollbackRange(stepsNewestFirst, targetId)`.
From 02-19: ToastView, Row, EmptyState, Screen.
Copy: undo.*, history.*, categories.builtin.*.
React Native: `AccessibilityInfo.isScreenReaderEnabled()` and the 'screenReaderChanged' event.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Copy helpers + UndoToastHost</name>
  <files>src/features/record/history/undoCopy.ts, src/features/record/history/UndoToastHost.tsx, src/features/record/history/__tests__/undoCopy.test.ts, src/features/record/history/__tests__/history.test.tsx</files>
  <read_first>
    - src/i18n/locales/en.ts (undo.* keys)
    - src/engine/undo/history.ts (describeRefusal)
    - src/ui/ToastView.tsx
  </read_first>
  <behavior>
    - stepLabel(t, 'deletedMany', {n: 30}) -> '30 deleted'; ('added', {name:'Groceries — week 2'}) -> 'Added · Groceries — week 2'; ('imported', {n:1}) -> 'Imported 1 line'
    - refusalText(t, {actor:'member', actorName:'Sam', record:{kind:'name', name:'Groceries'}}) -> 'Sam edited Groceries after this, so it can’t be undone.'; member with null name -> 'Someone in your household edited …'; self -> '{record} was changed on another device after this, …'; system -> '{record} was changed elsewhere after this, …'; builtin record uses t('categories.builtin.<key>'); entity record uses undo.refusal.record.<entity> ('this line')
    - UndoToastHost: shows the toast text; for kind ordinary/destructive with a stepId shows 'Undo' which calls undo() with the step's labelKey/params (from the toast params) and dismisses; auto-dismisses after 3200 ms (ordinary/info/refusal) or 6000 ms (destructive); with a screen reader enabled it does not auto-dismiss and shows a Dismiss control
    - a 'refusal' toast renders refusalText(describeRefusal(conflict, userId, memberNames))
    - an 'info' toast with key undo.reverted renders t('undo.reverted', { label: stepLabel(t, params.labelKey, params) })
  </behavior>
  <action>
Create src/features/record/history/undoCopy.ts:
```typescript
export function stepLabel(t: TFunction, labelKey: UndoLabelKey, params: UndoLabelParams): string; // plural keys use count = params.n
export function refusalText(t: TFunction, d: RefusalDescription): string;
```
Create `UndoToastHost()` — positioned above the bottom safe area, renders ToastView; timers cleared on unmount and whenever the toast id changes; screen reader state tracked with AccessibilityInfo. Toasts created by 02-20/22/23/24/25/26 put `{ key: 'undo.label.<labelKey>', params }` in `text` and the step id in `stepId`; the host derives labelKey from the key suffix for the undo() call and labelParams as `{ n: params.n ?? params.count, name: params.name }` (so `{ count }`-only toasts still undo correctly). Tests (fake timers for 3200/6000; mocked AccessibilityInfo).
  </action>
  <verify>
    <automated>npx jest src/features/record/history</automated>
  </verify>
  <acceptance_criteria>
    - tests pass
    - `grep -c "TOAST_MS_DESTRUCTIVE" src/features/record/history/UndoToastHost.tsx` returns >= 1 and `grep -c "isScreenReaderEnabled" src/features/record/history/UndoToastHost.tsx` returns >= 1
  </acceptance_criteria>
  <done>Toast undo and refusal copy work, accessibly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HistoryScreen</name>
  <files>src/features/record/history/HistoryScreen.tsx, src/features/record/history/__tests__/history.test.tsx</files>
  <read_first>
    - src/features/record/history/undoCopy.ts (Task 1)
    - .planning/phases/02-record/02-UI-SPEC.md (History copy; danger colour for "can't undo" text only)
  </read_first>
  <behavior>
    - title 'History', label 'Recent changes', note 'The last twelve changes. Rolling back to one undoes everything after it as well.'
    - rows newest first from useUndoLog: stepLabel as the row text, first row sub-label 'Most recent', others the local time/date of created_at
    - available rows show 'Undo to here' with sub-label 'Undoes {n} changes' (n from rollbackRange); tapping calls rollbackTo({stepId, ownerId, householdId})
    - refused rows are greyed (inkFaint), non-actionable, with 'Can’t undo' and the refusalText in the danger colour
    - empty: 'Nothing yet.' / 'Every change that can be undone shows up here.'
  </behavior>
  <action>
Create `HistoryScreen()` inside `Screen scroll` per <behavior>, no level gating (D-32 — the levels phase adds it). Tests with mocked hooks.
  </action>
  <verify>
    <automated>npx jest src/features/record/history && npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - tests, lint and typecheck pass
    - `grep -c "rollbackRange(" src/features/record/history/HistoryScreen.tsx` returns 1
  </acceptance_criteria>
  <done>History shows, rolls back and explains refused steps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| undo_log -> screen | The user's own steps only (owner RLS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-28-01 | Repudiation | refusal copy | mitigate | Names actor and record per D-26 from the server's updated_by; never claims success when refused |
| T-02-28-02 | Tampering (accidental mass rollback) | Undo to here | mitigate | Sub-label states how many changes it undoes; server stops at the first conflict (D-27) |
| T-02-28-03 | Information Disclosure | member names | accept | Only household members' display names already visible to members |
</threat_model>

<verification>
- `npx jest src/features/record/history`, lint, typecheck green.
</verification>

<success_criteria>
- REC-11 and REC-12 are visible and usable.
</success_criteria>

<output>
After completion, create `.planning/phases/02-record/02-28-SUMMARY.md`
</output>
