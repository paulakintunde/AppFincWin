---
phase: 02-record
plan: 21
type: execute
wave: 9
depends_on: ["02-18", "02-20"]
files_modified:
  - src/features/record/entry/recurringForm.ts
  - src/features/record/entry/RepeatsField.tsx
  - src/features/record/entry/EditScopePrompt.tsx
  - src/features/record/entry/OccurrenceActions.tsx
  - src/features/record/entry/TransactionSheet.tsx
  - src/features/record/entry/__tests__/recurringForm.test.ts
  - src/features/record/entry/__tests__/recurringControls.test.tsx
autonomous: true
requirements: [REC-05, REC-06]

must_haves:
  truths:
    - "A user can set Repeats (weekly, every 2 weeks, monthly, quarterly, yearly; ends never, on a date, or after N times) on a new entry or on an existing one-off entry, which becomes the first occurrence (REC-05, D-04, D-09)"
    - "Saving a new entry with Repeats is one undo step that removes the entry and its series together (D-24)"
    - "Editing a recurring occurrence's amount, name, category, account, payment type, currency or date asks 'This one' or 'This and future'; status/note-only edits and marking an occurrence paid with adjusted values never ask (D-06, D-07)"
    - "A pending occurrence can be marked paid in one tap (planned amount, dated today or its due date if earlier), or adjusted first (D-06)"
    - "A user can skip one occurrence or end the series, with a confirmation for ending; paid history is untouched (REC-06, D-08)"
  artifacts:
    - path: "src/features/record/entry/recurringForm.ts"
      provides: "RepeatsValue, repeatsToSchedule, templateFieldsChanged, needsScopePrompt"
      exports: ["repeatsToSchedule", "needsScopePrompt"]
    - path: "src/features/record/entry/OccurrenceActions.tsx"
      provides: "Mark paid / Adjust / Skip / End series actions"
      contains: "useEndSeries"
  key_links:
    - from: "src/features/record/entry/TransactionSheet.tsx"
      to: "useCreateSeries / useEditSeriesFrom"
      via: "save handler branches on Repeats and the scope prompt"
      pattern: "useEditSeriesFrom\\(\\)"
---

<objective>
Add the recurring controls to the transaction sheet: the Repeats field (new entries and D-09 conversion), the "This one / This and future" prompt, and the occurrence actions (mark paid, adjust, skip, end series).

Purpose: REC-05 and REC-06 from the user's side, per D-04..D-09.
Output: pure recurring form rules, three components, TransactionSheet wiring, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-record/02-CONTEXT.md
@.planning/phases/02-record/02-UI-SPEC.md
@src/features/record/entry/TransactionSheet.tsx
@src/features/record/entry/transactionForm.ts
@src/data/mutations/recurringSeries.ts

<interfaces>
From 02-18: `useCreateSeries().create({ series, anchorTransactionId?, anchorIsNew?, linkTransactionIds?, ownerId }) -> stepId`; `useEditSeriesFrom().editFrom({ id, householdId, expectedVersion, patch, effectiveFrom, ownerId, name }) -> stepId`; `useEndSeries().end({ id, householdId, expectedVersion, endDate, ownerId, name }) -> stepId`; `seriesInputFromRow(row, schedule, id)`, `seriesPatchFromOccurrenceEdit(patch)`, `ScheduleInput`.
From 02-15: `useAddTransaction().add(...)` (omit `undo` for a new recurring entry), `useMarkPaid().markPaid(row, ownerId, today, adjust?)`, `useSkipOccurrence().skip(row, ownerId)`, `showToast`, `newStepId`.
From 02-14: `useRecurringSeries(householdId)` (for the series version and name).
From 02-01: `RECURRING_FREQS`, `RecurringFreq`.
Copy (02-10): record.repeats.*, record.recurring.*, undo.label.seriesCreated/seriesEdited/seriesEnded/markedPaid/skipped.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure recurring form rules</name>
  <files>src/features/record/entry/recurringForm.ts, src/features/record/entry/__tests__/recurringForm.test.ts</files>
  <read_first>
    - src/features/record/entry/transactionForm.ts
    - src/data/mutations/recurringSeries.ts (ScheduleInput)
  </read_first>
  <behavior>
    - repeatsToSchedule({freq:'never'}) -> null; ({freq:'monthly', end:{kind:'never'}}) -> {freq:'monthly', endDate:null, occurrenceCount:null}; end on date -> endDate; after 12 -> occurrenceCount 12; count outside 1..1000 -> invalid
    - templateFieldsChanged({note:'x'}) false; ({original_amount:-5}) true; ({status:'paid'}) false
    - needsScopePrompt(row with recurring_series_id, patch {original_amount}) true; same patch plus status pending->paid false (recording the actual payment); row without series -> false
  </behavior>
  <action>
Create src/features/record/entry/recurringForm.ts:
```typescript
export type RepeatsEnd = { kind: 'never' } | { kind: 'date'; date: string } | { kind: 'count'; count: number };
export type RepeatsValue = { freq: 'never' } | { freq: RecurringFreq; end: RepeatsEnd };
export const TEMPLATE_FIELDS = ['original_amount', 'original_currency', 'account_id', 'category_id', 'name', 'payment_type', 'local_date'] as const;
export function repeatsToSchedule(v: RepeatsValue): ScheduleInput | null | 'invalid';
export function templateFieldsChanged(patch: TransactionPatch): boolean;
export function needsScopePrompt(row: Pick<TransactionRow, 'recurring_series_id' | 'status'>, patch: TransactionPatch): boolean;
```
Tests first.
  </action>
  <verify>
    <automated>npx jest src/features/record/entry/__tests__/recurringForm.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests pass
  </acceptance_criteria>
  <done>When to prompt and how Repeats maps to a schedule are explicit rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RepeatsField, EditScopePrompt, OccurrenceActions, sheet wiring</name>
  <files>src/features/record/entry/RepeatsField.tsx, src/features/record/entry/EditScopePrompt.tsx, src/features/record/entry/OccurrenceActions.tsx, src/features/record/entry/TransactionSheet.tsx, src/features/record/entry/__tests__/recurringControls.test.tsx</files>
  <read_first>
    - src/features/record/entry/TransactionSheet.tsx (the '02-21: recurring controls mount here' slot and save handlers)
    - .planning/phases/02-record/02-UI-SPEC.md (recurring copy rows; end-series destructive confirmation)
  </read_first>
  <behavior>
    - new entry with Repeats monthly: add() is called WITHOUT undo, then create({ series: seriesInputFromRow(new row values), anchorTransactionId: newId, anchorIsNew: true }); toast 'undo.label.seriesCreated' with that stepId; track('transaction_added', {kind, recurring:true})
    - existing one-off row, Repeats set, no other change: create({ anchorTransactionId: row.id, anchorIsNew: false }) only
    - recurring occurrence, amount changed: EditScopePrompt appears; 'This one' -> edit() with the patch; 'This and future' -> editFrom({ patch: seriesPatchFromOccurrenceEdit(patch), effectiveFrom: row.occurrence_date, expectedVersion: series.version }); toast seriesEdited
    - OccurrenceActions for a pending row: 'Mark paid' calls markPaid(row, ownerId, today) and closes; 'Adjust, then mark paid' sets the form status to paid (statusTouched) and keeps the sheet open; 'Skip this one' (recurring rows only) calls skip(); 'End this series' opens a ConfirmSheet with record.recurring.endConfirm {name, date: formatted today} and confirming calls end({ endDate: today })
    - the Repeats row is hidden for rows already in a series; OccurrenceActions are hidden for paid rows except none; overdue rows show record.recurring.overdue
  </behavior>
  <action>
Create:
- `RepeatsField({ value, onChange })` — a dense Row 'Repeats' showing the current choice; tapping opens a Sheet with Rows for never + RECURRING_FREQS (record.repeats.*) and, when a freq is chosen, an 'Ends' section: Never / On a date (DateField from 02-20) / After a number of times (numeric TextInput, 1..1000, record.repeats.endCount).
- `EditScopePrompt({ visible, onThisOne, onThisAndFuture, onCancel })` — Sheet with the heading record.recurring.scopeHeading and two Pills (scopeThisOne secondary, scopeThisAndFuture primary) plus Cancel.
- `OccurrenceActions({ row, series, onAdjust, onDone })` — actions per <behavior>; End uses ConfirmSheet (destructive, record.recurring.endProceed).
- Wire into TransactionSheet at the reserved slot and in the save handler following <behavior>; the series for a row comes from useRecurringSeries(householdId) by row.recurring_series_id.
Tests with mocked hooks.
  </action>
  <verify>
    <automated>npx jest src/features/record/entry && npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - tests, lint and typecheck pass
    - `grep -c "anchorIsNew: true" src/features/record/entry/TransactionSheet.tsx` returns 1
    - `grep -c "effectiveFrom" src/features/record/entry/TransactionSheet.tsx` returns >= 1
    - `grep -c "02-21: recurring controls mount here" src/features/record/entry/TransactionSheet.tsx` returns 0 (slot filled)
  </acceptance_criteria>
  <done>Recurring creation, conversion, scoped edits, mark paid, skip and end are available from the sheet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user action -> series RPCs | Scope choice decides whether history-adjacent rows change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-21-01 | Tampering (unintended bulk change) | This and future | mitigate | Explicit prompt before any series-wide edit; server only touches pending rows on/after the date (02-08); one Undo reverses it |
| T-02-21-02 | Repudiation (asserting unseen payments) | Mark paid | mitigate | Only on an explicit tap; nothing auto-marks paid (D-05) |
| T-02-21-03 | Tampering (destructive end) | End series | mitigate | Destructive confirmation sheet; paid history untouched; undoable |
</threat_model>

<verification>
- `npx jest src/features/record/entry`, lint, typecheck green.
</verification>

<success_criteria>
- REC-05 and REC-06 are fully reachable from the entry sheet.
</success_criteria>

<output>
After completion, create `.planning/phases/02-record/02-21-SUMMARY.md`
</output>
