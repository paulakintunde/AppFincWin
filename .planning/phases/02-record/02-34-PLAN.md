---
phase: 02-record
plan: 34
type: tdd
wave: 3
depends_on: ["02-32", "02-33"]
files_modified:
  - src/engine/ofx/date.ts
  - src/engine/ofx/amount.ts
  - src/engine/ofx/parse.ts
  - src/engine/ofx/index.ts
  - src/engine/ofx/__tests__/date.test.ts
  - src/engine/ofx/__tests__/amount.test.ts
  - src/engine/ofx/__tests__/parse.test.ts
  - src/engine/ofx/__tests__/fixtures/bank-sgml.ofx
  - src/engine/ofx/__tests__/fixtures/card-sgml-positive-purchases.qfx
  - src/engine/ofx/__tests__/fixtures/bank-xml-220.ofx
  - src/engine/ofx/__tests__/fixtures/multi-statement.ofx
  - src/engine/ofx/__tests__/fixtures/card-over-limit.ofx
autonomous: true
requirements: [REC-09, REC-14, REC-16]

must_haves:
  truths:
    - "An OFX or QFX file becomes one StatementDraft per statement it contains (bank STMTRS or card CCSTMTRS); a multi-statement file returns them all so the user can pick one, since one import has one target account (D-39, D-12)"
    - "Investment and loan statements return a typed 'unsupported-statement' result, not a crash (D-39)"
    - "DTPOSTED becomes the row's local date from its first 8 characters exactly as written, never converted through an instant, and is validated as a real calendar date (D-39, MON-14, RESEARCH §A1)"
    - "TRNAMT and BALAMT are read as magnitude + marker with '.' or ',' decimals, a leading '+', and zero padding beyond the exponent stripped; non-zero excess is 'too-many-decimals', never rounded; a zero TRNAMT is a 'zero-amount' row issue (D-43, D-24)"
    - "FITID is carried as externalId, TRNTYPE as trnType, LEDGERBAL as the stated closing balance and AVAILBAL as available credit; ACCTID, BANKID and BRANCHID are never extracted or stored (D-39, D-45, RESEARCH Security extension)"
    - "The raw TRNAMT and BALAMT text is kept (<= 64 chars) for provenance (D-45)"
    - "The layout signature is 'ofx|bank|<ACCTTYPE>' or 'ofx|card' and never contains an account number (D-42, RESEARCH §A2)"
    - "No function here throws or logs file content; a no-leak test proves it (RESEARCH Security extension V7)"
  artifacts:
    - path: "src/engine/ofx/parse.ts"
      provides: "parseOfx"
      exports: ["parseOfx"]
    - path: "src/engine/ofx/date.ts"
      provides: "ofxLocalDate"
      exports: ["ofxLocalDate"]
    - path: "src/engine/ofx/amount.ts"
      provides: "parseOfxAmount"
      exports: ["parseOfxAmount"]
  key_links:
    - from: "src/engine/ofx/parse.ts"
      to: "src/engine/statement/types.ts StatementDraft"
      via: "one StatementDraft per STMTRS/CCSTMTRS"
      pattern: "StatementDraft"
    - from: "src/engine/ofx/date.ts"
      to: "src/engine/time/localDate.ts"
      via: "isValidLocalDate on the first 8 chars"
      pattern: "isValidLocalDate"
---

<objective>
Finish the OFX/QFX adapter: date and amount readers and the statement extractor that turns the 02-33 tree into StatementDraft rows (magnitude + marker, FITID, TRNTYPE, raw strings) plus file-level balances, with synthetic fixtures covering bank SGML, card QFX with positive purchases, XML 2.x, multi-statement and a card over its limit.

Purpose: REC-09 (OFX/QFX), REC-14 (notation + provenance), REC-16 (FITID as a duplicate key input). The pipeline (02-26) feeds these drafts into profile inference (02-36) exactly like CSV drafts.
Output: date.ts, amount.ts, parse.ts, fixtures, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/02-record/02-RESEARCH.md
@.planning/phases/02-record/02-32-SUMMARY.md
@.planning/phases/02-record/02-33-SUMMARY.md
@src/engine/statement/types.ts
@src/engine/money/parseNotatedAmount.ts
@src/engine/ofx/tree.ts
@src/engine/time/localDate.ts

<interfaces>
From 02-33: `splitOfxHeader(text)`, `tokenizeOfx(body)`, `buildOfxTree(tokens) -> { ok, root: OfxNode, warnings }`, `findAll(root, name)`, `child(node, name)`, `childText(node, name)`, `OfxNode { name; value; children }`.
From 02-32: `StatementDraft`, `DraftRow`, `StatedBalance`, `DraftWarning`, `MAX_RAW_CELL`, `MAX_DESCRIPTION`, `MAX_EXTERNAL_ID`; `AmountMarker`; `parseNotatedAmount(raw, notation, exponent)`.
From src/engine/time: `isValidLocalDate(s)`.
From src/engine/money: `MinorUnits`, `minorUnits`, `MAX_ABS_AMOUNT_MINOR`.
OFX paths: BANKMSGSRSV1/STMTTRNRS/STMTRS and CREDITCARDMSGSRSV1/CCSTMTTRNRS/CCSTMTRS; per statement CURDEF, BANKACCTFROM/ACCTTYPE (CHECKING|SAVINGS|MONEYMRKT|CREDITLINE|CD) or CCACCTFROM, BANKTRANLIST/DTSTART,DTEND, STMTTRN*, LEDGERBAL/BALAMT,DTASOF, AVAILBAL/BALAMT,DTASOF. Per STMTTRN: TRNTYPE, DTPOSTED, DTUSER, TRNAMT, FITID, NAME or PAYEE/NAME, MEMO, CURRENCY/ORIGCURRENCY. INVSTMTMSGSRSV1 and LOANMSGSRSV1 are unsupported.
</interfaces>
</context>

<feature>
  <name>OFX dates, amounts and statement extraction</name>
  <files>see files_modified</files>
  <behavior>
    ofxLocalDate(raw):
    - '20260912' -> '2026-09-12'; '20260912120000.000[-5:EST]' -> '2026-09-12'; '20260912000000[+5.30:IST]' -> '2026-09-12'; '20260912000000[-3]' -> '2026-09-12'; '20260912235959[0:GMT]' -> '2026-09-12'
    - '20261312' -> null (month 13); '2026091' -> null; '20260230' -> null; '20260912XX' -> null (malformed time part rejected); '20260912120000[abc' -> null
    - property: for any valid local date d and any offset form in {none, [0:GMT], [-5:EST], [+5.30:IST], [-3], [+14]}, ofxLocalDate(render(d, hhmmss, offset)) === d (never shifted a day)
    parseOfxAmount(raw, exponent):
    - '-12.50' 2 -> { magnitude 1250, marker 'minus' }; '12.50' -> none; '+12.50' -> plus; '-12,50' -> minus 1250; '-12.5000' -> 1250; '-12.5001' -> too-many-decimals; '0.00' -> { magnitude 0, marker 'none' }; '1,234.50' -> invalid (no grouping in OFX); '' -> empty
    - property: int a in [-1e11, 1e11], exponent e in {0, 2, 3}, rendered with '.' or ',' and 0-2 extra trailing zeros -> magnitude |a| and marker minus iff a < 0
    parseOfx(text, opts: { exponentFor(code): number | null }):
    - bank-sgml.ofx (CHECKING, GBP, 5 rows, LEDGERBAL -240.00, AVAILBAL 260.00) -> 1 draft: source 'ofx', accountHint 'bank', currency 'GBP', layoutSignature 'ofx|bank|CHECKING', 5 rows with magnitudes/markers, externalId = FITID, trnType, statedClosing { magnitude 24000, marker 'minus', asOf }, available { 26000, none }
    - card-sgml-positive-purchases.qfx (CCSTMTRS, purchases positive, one 'PAYMENT - THANK YOU' negative, INTU.BID present) -> accountHint 'card', layoutSignature 'ofx|card', purchase rows marker 'none', payment row marker 'minus'; INTU tags ignored
    - bank-xml-220.ofx -> same draft shape as the SGML equivalent
    - multi-statement.ofx (one STMTRS + one CCSTMTRS) -> 2 drafts in document order
    - card-over-limit.ofx (LEDGERBAL -1250.00, AVAILBAL -250.00) -> statedClosing 125000 minus, available 25000 minus
    - an INVSTMTMSGSRSV1-only file -> { ok: false, error: 'unsupported-statement' }; no STMTRS/CCSTMTRS at all -> { ok: false, error: 'no-statements' }; tokenizer/tree errors pass through ('no-ofx-root' | 'too-large' | 'too-deep')
    - description = NAME (or PAYEE/NAME) plus ' · ' + MEMO when MEMO adds text not already contained in NAME; whitespace collapsed; <= 200 chars; empty -> 'empty-description' issue
    - a row with TRNAMT 0 -> issue 'zero-amount'; a row with a bad DTPOSTED -> localDate null + 'bad-date'
    - CURDEF unknown to exponentFor -> every row gets 'unknown-currency' and currency CURDEF is kept; a per-row CURRENCY/ORIGCURRENCY does not change the row currency (TRNAMT is always in CURDEF)
    - periodStart/periodEnd from DTSTART/DTEND via ofxLocalDate; tree warnings copied into draft.warnings
    - no draft, row or warning contains the text 'ACCTID' or any ACCTID value from the fixture (assert the fixture's ACCTID '99887766' appears nowhere in JSON.stringify(result))
    No-leak: a body whose TRNAMT is 'TESCO 12.50' and whose NAME is 'DR JONES PHARMACY' with a stray close returns issues/warnings whose JSON (excluding the description field itself) contains neither string
  </behavior>
  <implementation>See tasks.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ofxLocalDate and parseOfxAmount (RED then GREEN)</name>
  <files>src/engine/ofx/date.ts, src/engine/ofx/amount.ts, src/engine/ofx/index.ts, src/engine/ofx/__tests__/date.test.ts, src/engine/ofx/__tests__/amount.test.ts</files>
  <read_first>
    - src/engine/time/localDate.ts (isValidLocalDate)
    - src/engine/money/parseNotatedAmount.ts (NumberNotation, markers)
    - .planning/phases/02-record/02-RESEARCH.md §A1 "Dates" and "Amounts"
  </read_first>
  <behavior>
    - ofxLocalDate and parseOfxAmount cases and properties from the feature block
  </behavior>
  <action>
RED then GREEN.
`src/engine/ofx/date.ts`: `export function ofxLocalDate(raw: string): string | null;` — trim; the first 8 chars must be ASCII digits; build `YYYY-MM-DD`; the remainder must match the grammar `[HHMMSS[.XXX]][[±H[.MM]][:TZ]]` checked with a hand-written scan (digits only where digits are required; bracket content = optional sign, 1-2 digits, optional '.' or ':' parts, optional ':' + letters); return null when the grammar fails or `isValidLocalDate` rejects the date. The time and offset are validated, then ignored: never construct a Date.
`src/engine/ofx/amount.ts`: `export function parseOfxAmount(raw: string, exponent: number): NotatedAmountResult;` — reject any char outside `0-9 + - . ,` with 'invalid' (so grouping is refused); exactly one of '.'/',' may appear; then delegate to `parseNotatedAmount(raw, { decimal: <the mark present, default '.'>, group: <the other mark>, grouping: 'western' }, exponent)`. A zero magnitude is returned as ok (the extractor flags it).
Barrel: append `export * from './date'; export * from './amount';`. Commit test then feat.
  </action>
  <verify>
    <automated>npx jest src/engine/ofx/__tests__/date.test.ts src/engine/ofx/__tests__/amount.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - both test files pass
    - `grep -cE "new Date|Date\.UTC|Date\.parse" src/engine/ofx/date.ts` returns 0
    - `grep -cE "parseFloat|Number\(" src/engine/ofx/amount.ts` returns 0
  </acceptance_criteria>
  <done>OFX dates never shift a day, and OFX amounts are exact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: parseOfx statement extraction + fixtures (RED then GREEN), coverage</name>
  <files>src/engine/ofx/parse.ts, src/engine/ofx/index.ts, src/engine/ofx/__tests__/parse.test.ts, src/engine/ofx/__tests__/fixtures/bank-sgml.ofx, src/engine/ofx/__tests__/fixtures/card-sgml-positive-purchases.qfx, src/engine/ofx/__tests__/fixtures/bank-xml-220.ofx, src/engine/ofx/__tests__/fixtures/multi-statement.ofx, src/engine/ofx/__tests__/fixtures/card-over-limit.ofx</files>
  <read_first>
    - src/engine/ofx/tree.ts, src/engine/ofx/tokenize.ts (02-33)
    - src/engine/statement/types.ts (StatementDraft fields)
    - .planning/phases/02-record/02-RESEARCH.md §A1 steps 5-6, FITID notes, QFX note
  </read_first>
  <behavior>
    - Every parseOfx case from the feature block and the no-leak assertion
  </behavior>
  <action>
Write the five fixtures by hand as SYNTHETIC files (invented payees, ACCTID '99887766', BANKID '000000'; header comment in each file is not possible in OFX, so name them clearly and document them in parse.test.ts). Only synthetic or scrubbed files are ever committed; the redacted real statements the user collects are added later as additional fixtures under the same folder.
Create `src/engine/ofx/parse.ts`:
```typescript
export type ParseOfxError = 'no-ofx-root' | 'too-large' | 'too-deep' | 'unsupported-statement' | 'no-statements';
export function parseOfx(text: string, opts: { exponentFor(code: string): number | null }): { ok: true; drafts: StatementDraft[] } | { ok: false; error: ParseOfxError };
```
Steps: splitOfxHeader -> tokenizeOfx -> buildOfxTree; collect `findAll(root,'STMTRS')` and `findAll(root,'CCSTMTRS')` in document order (walk once and test both names); none and `findAll(root,'INVSTMTRS').length + findAll(root,'LOANSTMTRS').length > 0` -> unsupported-statement; none at all -> no-statements.
Per statement: currency = childText(CURDEF) upper-cased; exponent = exponentFor(currency) ?? 2 (and every row gets 'unknown-currency' when null); accountHint = 'card' for CCSTMTRS else 'bank'; ACCTTYPE read from BANKACCTFROM ONLY to build `layoutSignature = 'ofx|bank|' + ACCTTYPE` (or 'ofx|card'); never read ACCTID/BANKID/BRANCHID. For each STMTTRN in BANKTRANLIST: localDate = ofxLocalDate(DTPOSTED) (null -> 'bad-date'); amount = parseOfxAmount(TRNAMT, exponent) (error 'too-large' -> 'amount-too-large', other errors -> 'bad-amount'; magnitude 0 -> 'zero-amount'); rawAmount = TRNAMT trimmed and sliced to MAX_RAW_CELL; externalId = FITID sliced to MAX_EXTERNAL_ID or null; trnType = TRNTYPE upper-cased or null; description per the feature block; balance fields null (OFX has no running balance). statedClosing from LEDGERBAL (BALAMT via parseOfxAmount + DTASOF via ofxLocalDate; unreadable -> null); available from AVAILBAL; statedOpening null (OFX carries none — RESEARCH §A1); statedLimit null (limits are derived later by the profile step, D-48); balanceLabel null; labels [] (plus 'dr-cr-markers' never applies to OFX); periodStart/End from DTSTART/DTEND; warnings = tree warnings.
Barrel: append `export * from './parse';`. Commit test then feat.
  </action>
  <verify>
    <automated>npx jest src/engine/ofx --coverage --collectCoverageFrom="src/engine/ofx/**/*.ts" --coverageThreshold="{\"global\":{\"branches\":100,\"lines\":100,\"functions\":100,\"statements\":100}}" && npm run depcruise</automated>
  </verify>
  <acceptance_criteria>
    - all src/engine/ofx tests pass at 100% coverage; depcruise passes
    - `grep -cE "'ACCTID'|'BANKID'|'BRANCHID'" src/engine/ofx/parse.ts` returns 0
    - `grep -cE "console\.|throw new" src/engine/ofx/parse.ts` returns 0
    - `ls src/engine/ofx/__tests__/fixtures/*.ofx src/engine/ofx/__tests__/fixtures/*.qfx | wc -l` returns 5
  </acceptance_criteria>
  <done>OFX/QFX files become sign-free StatementDrafts with FITIDs and balances, and never carry account numbers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OFX tree -> StatementDraft | Untrusted values become candidate money rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-34-01 | Tampering (wrong day/month) | ofxLocalDate | mitigate | First 8 chars as written; never through an instant; property test over every offset form |
| T-02-34-02 | Tampering (wrong amount) | parseOfxAmount | mitigate | Strict parse via parseNotatedAmount; excess non-zero decimals rejected; no parseFloat |
| T-02-34-03 | Information Disclosure | account numbers | mitigate | ACCTID/BANKID/BRANCHID never read; signature excludes them; test asserts the fixture ACCTID never appears in output |
| T-02-34-04 | Information Disclosure | errors/warnings | mitigate | Enum codes only; no-leak test |
| T-02-34-05 | Tampering (duplicates) | FITID | mitigate | Carried as a hint only; uniqueness never assumed (02-04 FITID rules, D-54) |
</threat_model>

<verification>
- `npx jest src/engine/ofx` green at 100%; typecheck and depcruise green.
</verification>

<success_criteria>
- Bank, card, XML and multi-statement OFX/QFX files all produce correct StatementDrafts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-record/02-34-SUMMARY.md`
</output>
