---
phase: 02-record
plan: 40
type: execute
wave: 6
depends_on: ["02-11", "02-13", "02-15", "02-37"]
files_modified:
  - src/data/mutations/transfers.ts
  - src/data/mutations/transactions.ts
  - src/data/mutations/__tests__/transfers.test.tsx
autonomous: true
requirements: [REC-18]

must_haves:
  truths:
    - "A user can create a transfer between two of their own accounts as one linked pair: both legs are written in one insert statement with one shared transfer_id, filed under the user's system Transfer category, status paid (D-50, D-51, D-56)"
    - "Creating, editing or deleting a transfer is one undo step covering both legs (transferAdded / transferEdited / transferDeleted) (D-50, D-24)"
    - "Deleting either leg deletes both, in one atomic version-checked call (D-51)"
    - "Editing a transfer's date edits both legs; a same-currency amount edit keeps the legs equal and opposite; a cross-currency pair edits each leg's amount independently; changing an account moves that leg and its currency follows the new account (D-51, planner rule)"
    - "Each leg keeps its own currency and its own FX stamp through the Phase 1 path; nothing converts one leg's amount into the other's (D-50, Phase 1 D-16)"
    - "Transfer writes queue offline like any write, and a conflict or pair-rule rejection lands in the failed-writes list with ids only (D-29, Phase 1 D-18/D-19)"
  artifacts:
    - path: "src/data/mutations/transfers.ts"
      provides: "registerTransferMutations, useAddTransfer, useEditTransfer, useDeleteTransfer"
      exports: ["useAddTransfer", "useEditTransfer", "useDeleteTransfer", "registerTransferMutations"]
  key_links:
    - from: "src/data/mutations/transfers.ts add"
      to: "src/db/transactions.ts insertTransactionsBatch"
      via: "both legs in one upsert statement so the deferred pair check sees two rows"
      pattern: "insertTransactionsBatch\\("
    - from: "src/data/mutations/transfers.ts edit/delete"
      to: "src/db/patches.ts applyPatches"
      via: "both legs' ops plus the undo step in one atomic call"
      pattern: "applyPatches\\("
    - from: "src/data/mutations/transactions.ts registerTransactionMutations"
      to: "registerTransferMutations"
      via: "one call so transfer defaults register with the write queue"
      pattern: "registerTransferMutations\\(qc\\)"
---

<objective>
Add the transfer mutations: create a pair, edit a pair, delete a pair — each atomic, queued, conflict-aware and one undo step (D-50, D-51).

Purpose: REC-18's "record a transfer between their own accounts as one linked pair". The entry sheet (02-20) and Activity (02-22) call these hooks; bulk delete (02-16) handles transfer legs in selections separately.
Output: transfers.ts, a one-line registration hook-in, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-record/02-CONTEXT.md
@.planning/phases/02-record/02-RESEARCH.md
@.planning/phases/02-record/02-15-SUMMARY.md
@src/data/mutations/transactions.ts
@src/data/mutations/undoCapture.ts
@src/data/mutations/categories.ts
@src/db/transactions.ts
@src/db/patches.ts
@src/engine/transfer/pair.ts

<interfaces>
From 02-37 engine/transfer: `buildTransferLegs({ transferId, outId, inId, from: {id, currency}, to: {id, currency}, amountOut, amountIn, localDate }) -> { ok, legs: [out, in] } | { ok: false, error }`, `transferEditPatches(before: TransferPairState, after: TransferPairState) -> { out: Record; in: Record }`.
From 02-05 engine/undo: `inverseOfInserts`, `planBulkPatch`, `buildStep`, labels 'transferAdded' | 'transferEdited' | 'transferDeleted', `NOW_SENTINEL`.
From 02-11 db: `insertTransactionsBatch(client, rows)` (one upsert, returns {id, local_date, version, rate_pending}[]), `fetchTransferLegs(client, householdId, transferIds)`, `NewTransaction` (transfer_id, category_id, status), keys `mutationKeys.addTransfer/editTransfer/deleteTransfer`, `queryKeys.transactionsRoot`.
From 02-13 db: `applyPatches(client, ops, step)` (throws VersionConflictError on refusal).
From 02-15: `recordUndoStepSafely(qc, client, step, ownerId)`, `newStepId()`, `showToast`, and the existing helpers `guardSession`, `markSession`, `writeClient`, `WRITE_SCOPE`, `shouldRetryWrite`, `writeRetryDelay`, `resolveExpectedVersion`, `recordWrittenVersion`, `recordFailedWrite`, `patchMonthCacheIfLoaded`.
From 02-14: `useCategoryLookup(userId).transferCategoryId`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useAddTransfer — one insert, one step</name>
  <files>src/data/mutations/transfers.ts, src/data/mutations/transactions.ts, src/data/mutations/__tests__/transfers.test.tsx</files>
  <read_first>
    - src/data/mutations/transactions.ts (add default, importChunk default, placeRowInMonth, followUpIfRatePending)
    - src/data/mutations/categories.ts (a module that registers its own defaults)
    - src/engine/transfer/pair.ts (02-37)
  </read_first>
  <behavior>
    - add({ householdId, ownerId, from: {id:'a1', currency:'GBP'}, to: {id:'a2', currency:'GBP'}, amountOut: 5000, amountIn: 5000, localDate, timeZone, transferCategoryId, toName }) enqueues ONE addTransfer mutation whose mutationFn calls insertTransactionsBatch once with two rows: [{ account_id a1, original_amount −5000, transfer_id T, category_id transferCat, status 'paid' }, { account_id a2, original_amount +5000, same T, … }]
    - after success, one undo step 'transferAdded' { name: toName } whose ops soft-delete both ids at their returned versions (inverseOfInserts), recorded with recordUndoStepSafely; returns { transferId, stepId }
    - optimistic: both legs appear in their loaded month caches with pending: true
    - buildTransferLegs error (same account / non-positive / same-currency mismatch) -> TypeError before mutate, nothing queued
    - a missing transferCategoryId -> TypeError before mutate
    - a rejected insert (e.g. 23514) removes both optimistic rows and records one failed write { entity: 'transactions', entityId: 'transfer:<T>', kind: 'rejected', attempted: { transfer_id } } — no amounts or names
    - a cross-currency pair (GBP 5000 out, EUR 5800 in) inserts each leg in its own currency with the amounts given; follow-up rate resolution runs per leg like any insert
  </behavior>
  <action>
Create `src/data/mutations/transfers.ts`:
```typescript
export interface AddTransferInput { householdId: string; ownerId: string; from: { id: string; currency: string }; to: { id: string; currency: string }; amountOut: number; amountIn: number; localDate: string; timeZone: string; transferCategoryId: string | null; note?: string | null; toName: string; homeCurrency: string }
export interface AddTransferVars { householdId: string; ownerId: string; stepId: string; rows: [NewTransaction, NewTransaction]; transferId: string; labelName: string; homeCurrency: string }
export function registerTransferMutations(qc: QueryClient): void; // addTransfer, editTransfer, deleteTransfer defaults, WRITE_SCOPE
export function useAddTransfer(): { add(input: AddTransferInput): { transferId: string; stepId: string } };
```
`add` builds ids with Crypto.randomUUID, calls `buildTransferLegs`, maps legs to NewTransaction rows (name null — the list renders "Transfer to/from {account}" from the link, payment_type null, status 'paid', category_id transferCategoryId, note on both legs), then mutates. The default's mutationFn: `guardSession(vars, async () => { const client = await writeClient(); const rows = await insertTransactionsBatch(client, vars.rows); const step = buildStep(vars.stepId, 'transferAdded', { name: vars.labelName }, inverseOfInserts('transactions', rows.map(r => ({ id: r.id, version: r.version })))); await recordUndoStepSafely(qc, client, step, vars.ownerId); return rows; })`; onMutate places both optimistic rows; onSuccess invalidates transactionsRoot and follows up rate_pending rows; onError per <behavior>.
In src/data/mutations/transactions.ts add ONE line at the end of `registerTransactionMutations(qc)`: `registerTransferMutations(qc);` (import from './transfers'). This keeps src/data/mutations/index.ts untouched in this wave. Tests per <behavior>.
  </action>
  <verify>
    <automated>npx jest src/data/mutations/__tests__/transfers.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tests pass
    - `grep -c "insertTransactionsBatch(" src/data/mutations/transfers.ts` returns 1 and `grep -c "registerTransferMutations(qc)" src/data/mutations/transactions.ts` returns 1
    - `grep -cE "convertMinor|crossRate" src/data/mutations/transfers.ts` returns 0 (legs are never derived from each other — D-50)
  </acceptance_criteria>
  <done>A transfer is created as one atomic pair with one undo step.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useEditTransfer and useDeleteTransfer — both legs, one step</name>
  <files>src/data/mutations/transfers.ts, src/data/mutations/__tests__/transfers.test.tsx</files>
  <read_first>
    - src/data/mutations/transfers.ts (Task 1)
    - src/data/mutations/patches.ts (02-16 bulk flow for the planBulkPatch + applyPatches shape; do not import its hooks)
    - src/engine/transfer/pair.ts (transferEditPatches)
  </read_first>
  <behavior>
    - edit({ out: legRow, in: legRow }, after: TransferPairState) sends ONE apply_patches call with only the changed keys per leg (transferEditPatches), expected versions chain-resolved, and a 'transferEdited' step whose inverse restores both legs' before values at version + 1; nothing changed -> no call
    - a date change patches local_date on both legs; a same-currency amount change patches −m / +m; changing the to-account patches account_id and original_currency on the in-leg only
    - remove(leg, ownerId) with only one leg in hand: the mutationFn fetches the pair with fetchTransferLegs(transferId) at flush, then sends ONE apply_patches soft-deleting both with a 'transferDeleted' step whose inverse clears deleted_at on both; optimistic removal of both legs from loaded months
    - VersionConflictError on either leg -> neither changes, cache refetched, failed write { entity:'transactions', entityId:'transfer:<T>', kind:'conflict', code:'version-conflict', attempted:{ transfer_id, action:'edit'|'delete' } }, refusal toast
  </behavior>
  <action>
Add to `src/data/mutations/transfers.ts`:
```typescript
export interface TransferLegRow { id: string; household_id: string; account_id: string; original_amount: number; original_currency: string; local_date: string; version: number; transfer_id: string }
export function useEditTransfer(): { edit(legs: { out: TransferLegRow; in: TransferLegRow }, after: TransferPairState, ctx: { ownerId: string; labelName: string }): string | null }; // null when nothing changed
export function useDeleteTransfer(): { remove(leg: TransferLegRow, ctx: { ownerId: string; labelName: string }): string };
```
Both defaults build BulkPatchItems (edit: from transferEditPatches with `before` read from the leg rows for exactly the patched keys; delete: `{ deleted_at: NOW_SENTINEL }` with before `{ deleted_at: null }` for each fetched leg), run `planBulkPatch`, `buildStep(stepId, label, { name }, inverse)` and `applyPatches(client, forward, step)`; `recordWrittenVersion` for each returned row; invalidate transactionsRoot and undoLog. Extend the tests.
  </action>
  <verify>
    <automated>npx jest src/data && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/data` passes
    - `grep -c "applyPatches(" src/data/mutations/transfers.ts` returns >= 2 and `grep -c "fetchTransferLegs(" src/data/mutations/transfers.ts` returns >= 1
    - `grep -cE "'transferAdded'|'transferEdited'|'transferDeleted'" src/data/mutations/transfers.ts` returns 3
    - `grep -cE "\.delete\(\)" src/data/mutations/transfers.ts` returns 0 (soft delete only — D-30)
  </acceptance_criteria>
  <done>Editing or deleting a transfer always acts on both legs as one undoable step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device queue -> transactions / apply_patches | Transfer writes replay later, possibly after other members' changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-40-01 | Tampering (half a transfer) | add/edit/delete | mitigate | Two legs in one insert statement or one apply_patches call; deferred pair trigger (02-38) rejects any lone leg at commit |
| T-02-40-02 | Tampering (clobber) | edit/delete | mitigate | Expected versions on both legs; any mismatch refuses the whole step (D-26) |
| T-02-40-03 | Tampering (derived amounts) | cross-currency | mitigate | Amounts taken as given; no conversion code in the module (grep gate) |
| T-02-40-04 | Information Disclosure | failed-writes | mitigate | Records transfer id and action only |
</threat_model>

<verification>
- `npx jest src/data`, typecheck, lint green.
</verification>

<success_criteria>
- REC-18's linked pair is created, edited and deleted as one atomic, undoable unit.
</success_criteria>

<output>
After completion, create `.planning/phases/02-record/02-40-SUMMARY.md`
</output>
