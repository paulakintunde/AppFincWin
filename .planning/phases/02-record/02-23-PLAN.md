---
phase: 02-record
plan: 23
type: execute
wave: 9
depends_on: ["02-16", "02-22"]
files_modified:
  - src/features/record/activity/useActivitySelection.ts
  - src/features/record/activity/SearchBar.tsx
  - src/features/record/activity/FilterSheet.tsx
  - src/features/record/activity/BulkBar.tsx
  - src/features/record/activity/ActivityScreen.tsx
  - src/features/record/activity/ActivityRow.tsx
  - src/features/record/activity/__tests__/activityTools.test.tsx
autonomous: true
requirements: [ACT-03, ACT-04, ACT-05, REC-06, REC-18]

must_haves:
  truths:
    - "A user can search the current month instantly, or every month via the server, by name (ACT-03)"
    - "A user can filter the list by category (including Uncategorised), account, direction (All / Money out / Money in / Transfers) and a home-currency amount range, and clear filters back to All (ACT-04, D-50, UI-SPEC filter row)"
    - "Deleting a selection that contains a transfer leg deletes both legs of that transfer in the same step (D-51, via 02-16)"
    - "A user can select several lines and delete them in one confirmed action that is one undo step with the longer toast (ACT-05, D-24, D-31)"
    - "Bulk Mark paid and Mark unpaid work on the selection like the prototype's bulk bar (D-06)"
    - "Selection applies to whatever list is on screen (a month or a search result) (RESEARCH Open Question 3)"
  artifacts:
    - path: "src/features/record/activity/BulkBar.tsx"
      provides: "BulkBar"
      contains: "useBulkDelete"
    - path: "src/features/record/activity/FilterSheet.tsx"
      provides: "FilterSheet"
      exports: ["FilterSheet"]
    - path: "src/features/record/activity/useActivitySelection.ts"
      provides: "useActivitySelection"
      exports: ["useActivitySelection"]
  key_links:
    - from: "src/features/record/activity/ActivityScreen.tsx"
      to: "src/engine/activity filterRows / matchesSearch and useTransactionsSearch"
      via: "search scope + filter state"
      pattern: "filterRows\\("
---

<objective>
Layer search, filters and bulk select onto the Activity screen.

Purpose: ACT-03, ACT-04, ACT-05 and bulk mark paid/unpaid (D-06).
Output: selection hook, three components, edited screen and row, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-record/02-UI-SPEC.md
@src/features/record/activity/ActivityScreen.tsx
@src/features/record/activity/ActivityRow.tsx
@src/data/mutations/patches.ts

<interfaces>
From 02-06: `filterRows(rows, filter)`, `matchesSearch(row, term)`, `EMPTY_FILTER`, `isFilterActive(f)`, `ActivityFilter { categoryIds; accountIds; direction; amountMin; amountMax }`.
From 02-14: `useTransactionsSearch(householdId, term, homeCurrency, today) -> { rows, isLoading, enabled }`, `SEARCH_MIN_CHARS = 2`.
From 02-16: `useBulkDelete().remove(rows, { householdId, ownerId }) -> stepId`, `useBulkMarkPaid().markPaid(rows, ctx, today)`, `useBulkMarkUnpaid().markUnpaid(rows, ctx)`.
From 02-15: `showToast`. From 02-19: Chip, Pill, Sheet, ConfirmSheet, Row, EmptyState. `useAmountParser(region)` for the amount filter inputs.
Copy: activity.search*, activity.filter.*, activity.select/done/selectFirst/selected, activity.bulk*, activity.noMatch*, undo.label.deletedMany/markedPaidMany/markedUnpaidMany.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Selection hook, SearchBar, FilterSheet</name>
  <files>src/features/record/activity/useActivitySelection.ts, src/features/record/activity/SearchBar.tsx, src/features/record/activity/FilterSheet.tsx, src/features/record/activity/__tests__/activityTools.test.tsx</files>
  <read_first>
    - src/engine/activity/filters.ts
    - src/ui/Chip.tsx, src/ui/Sheet.tsx
  </read_first>
  <behavior>
    - useActivitySelection: toggle(id), clear(), selectAll(ids), isSelected(id), count; `active` flag toggled by Select/Done; clearing when the visible list changes drops ids no longer present
    - SearchBar: placeholder 'Search {month}' in month scope, 'Search every month' in all scope; scope Chips 'This month' / 'Every month'; onChange debounced 250 ms; clear button labelled
    - FilterSheet: multi-select category chips (+ 'Uncategorised' = null), account chips, direction chips (All / Money out / Money in / Transfers — 'transfers' shows only transfer legs, the other two exclude them, D-50), At least / At most amount inputs parsed with the strict amount parser in the home currency (invalid input shows the parser's message and is not applied), 'Clear filters' resets to EMPTY_FILTER, 'Show results' applies
  </behavior>
  <action>
Create the hook and components per <behavior>. FilterSheet props: `{ visible, value: ActivityFilter, homeCurrency, categories, accounts, onApply(f), onClose }`. Tests first for the hook and the amount-parse path.
  </action>
  <verify>
    <automated>npx jest src/features/record/activity/__tests__/activityTools.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tests pass
    - `grep -cE "parseFloat|Number\(" src/features/record/activity/FilterSheet.tsx` returns 0
    - `grep -c "activity.filter.transfers" src/features/record/activity/FilterSheet.tsx` returns 1
  </acceptance_criteria>
  <done>Search, filter and selection building blocks exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BulkBar + ActivityScreen/ActivityRow wiring</name>
  <files>src/features/record/activity/BulkBar.tsx, src/features/record/activity/ActivityScreen.tsx, src/features/record/activity/ActivityRow.tsx, src/features/record/activity/__tests__/activityTools.test.tsx</files>
  <read_first>
    - src/features/record/activity/ActivityScreen.tsx (the '02-23: …' slot)
    - src/data/mutations/patches.ts
  </read_first>
  <behavior>
    - 'Select' switches rows into selectable mode (checkbox with accessibilityRole 'checkbox', label a11y.selectRow {name}, 44px target); projection rows are never selectable; 'Done' exits and clears
    - BulkBar shows '{n} selected' and Mark paid / Mark unpaid / Delete; any action with 0 selected shows 'Select some rows first.'
    - Delete opens ConfirmSheet 'Delete {n} transactions?' (plural); confirm calls useBulkDelete().remove(selectedRows, ctx) (rows carry transfer_id so partner legs are deleted too, D-51) then showToast({kind:'destructive', text:{key:'undo.label.deletedMany', params:{count:n}}, stepId}) and exits select mode
    - Mark paid / Mark unpaid call the bulk hooks and show ordinary toasts with markedPaidMany / markedUnpaidMany
    - month scope with a term: rows = filterRows(month rows matching matchesSearch(term)); all scope with >= 2 chars: rows = filterRows(useTransactionsSearch rows), shown as one flat newest-first list; no results -> EmptyState 'Nothing matches.'
    - the Filter chip shows as active when isFilterActive(filter); the list keeps visual priority (chrome uses label role and fill1, never accent)
  </behavior>
  <action>
Create `BulkBar({ count, onMarkPaid, onMarkUnpaid, onDelete })` using the prototype bulk-bar look within tokens (ink surface, surface text, Pills). Replace the ActivityScreen slot with SearchBar, a Filter Chip opening FilterSheet, and the Select/Done toggle; derive the displayed items from search scope + filter; render BulkBar at the bottom when select mode is active. Add `selectable`, `selected`, `onToggleSelect` props to ActivityRow. Extend tests per <behavior>.
  </action>
  <verify>
    <automated>npx jest src/features/record/activity && npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - tests, lint and typecheck pass
    - `grep -c "useBulkDelete()" src/features/record/activity/ActivityScreen.tsx src/features/record/activity/BulkBar.tsx` totals >= 1
    - `grep -c "02-23: search, filters and bulk bar mount here" src/features/record/activity/ActivityScreen.tsx` returns 0
  </acceptance_criteria>
  <done>Search across months, filters and bulk actions work on the Activity screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| search term -> server | User text sent as a query parameter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-23-01 | Tampering | search | mitigate | Term escaped for LIKE and sent as a parameter (02-11); minimum 2 chars; debounced |
| T-02-23-02 | Repudiation (mass deletion) | bulk delete | mitigate | Confirmation with count, one atomic undoable step, 6s toast, History |
| T-02-23-03 | Denial of Service | selection size | mitigate | Bulk hooks reject > 6000 rows before sending |
| T-02-23-04 | Tampering (half-deleted transfer) | bulk delete of a transfer leg | mitigate | Selected rows pass transfer_id to useBulkDelete, which adds the partner leg in the same atomic call (02-16, D-51) |
</threat_model>

<verification>
- `npx jest src/features/record/activity`, lint, typecheck green.
</verification>

<success_criteria>
- ACT-03, ACT-04, ACT-05 are satisfied on the Activity screen.
</success_criteria>

<output>
After completion, create `.planning/phases/02-record/02-23-SUMMARY.md`
</output>
