---
phase: 02-record
plan: 25
type: execute
wave: 8
depends_on: ["02-14", "02-17", "02-19"]
files_modified:
  - src/features/record/categories/CategoriesScreen.tsx
  - src/features/record/categories/CategorySheet.tsx
  - src/features/record/categories/RemoveCategoryPrompt.tsx
  - src/features/record/categories/__tests__/categories.test.tsx
autonomous: true
requirements: [REC-07]

must_haves:
  truths:
    - "A user can create a category and rename or recolour any non-system category, including the built-ins, choosing only from the 7 swatch pairs (REC-07, D-34, D-35)"
    - "Transfer and Settlement are listed as system categories that cannot be changed (D-34)"
    - "Removing a category in use says how many transactions use it and offers Merge into another category or Archive; an unused category archives directly; archived categories can be restored (D-36)"
    - "Built-in names come from i18n until renamed (D-34)"
    - "Every change shows an Undo toast (REC-11)"
  artifacts:
    - path: "src/features/record/categories/CategoriesScreen.tsx"
      provides: "CategoriesScreen"
      exports: ["CategoriesScreen"]
    - path: "src/features/record/categories/RemoveCategoryPrompt.tsx"
      provides: "merge-or-archive prompt"
      contains: "useCategoryUsage"
  key_links:
    - from: "src/features/record/categories/RemoveCategoryPrompt.tsx"
      to: "useMergeCategory / useArchiveCategory"
      via: "Merge / Archive actions"
      pattern: "useMergeCategory\\(\\)"
---

<objective>
Build category management: list (active, system, archived), create/edit sheet with swatch picker, and the merge-or-archive removal prompt.

Purpose: REC-07 and D-34..D-36.
Output: screen, two components, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-record/02-UI-SPEC.md
@src/data/mutations/categories.ts
@src/data/queries/categories.ts

<interfaces>
From 02-17: `useAddCategory().add({ ownerId, name, colorKey }) -> { id, stepId }`, `useEditCategory().edit(row, { name?, colorKey? }) -> stepId`, `useArchiveCategory().archive(row) / .restore(row) -> stepId`, `useMergeCategory().merge({ source, target, targetName, householdId, ownerId }) -> stepId`, `useCategoryUsage(householdId, categoryId, enabled) -> { count, capped, isLoading }`.
From 02-14: `useCategoryLookup(userId) -> { all, active, byId }`, `categoryName(row, t)`, `useRecordContext()`.
From 02-06: `CATEGORY_COLOR_KEYS`. From 02-19: SwatchDot, CategoryGlyph, Row, Pill, Sheet, SheetHeader, ConfirmSheet.
Copy: categories.*, undo.label.category*.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CategorySheet + RemoveCategoryPrompt</name>
  <files>src/features/record/categories/CategorySheet.tsx, src/features/record/categories/RemoveCategoryPrompt.tsx, src/features/record/categories/__tests__/categories.test.tsx</files>
  <read_first>
    - src/data/mutations/categories.ts
    - src/ui/SwatchDot.tsx, src/ui/CategoryGlyph.tsx
  </read_first>
  <behavior>
    - CategorySheet new: name input (1..60 chars after trim, 'Give the category a name.'), a row of 7 SwatchDots (one selected), live CategoryGlyph preview; 'Save category' calls add() then showToast categoryAdded
    - edit: prefilled (built-in shows its i18n name); 'Save changes' sends only changed fields; 'Archive' opens RemoveCategoryPrompt
    - RemoveCategoryPrompt with usage count 0 -> archives directly (toast categoryArchived); count > 0 -> body categories.removeInUse (plural, name, count) with Cancel / Merge / Archive; Merge opens a target list (active categories except the source) titled 'Merge {name} into'; picking one calls merge() and shows categoryMerged toast
    - while usage is loading, Merge/Archive are disabled
  </behavior>
  <action>
Create `CategorySheet({ visible, mode: { kind: 'new' } | { kind: 'edit'; category: CategoryRow }, onClose })` and `RemoveCategoryPrompt({ visible, category, onDone, onCancel })` per <behavior>. Tests with mocked hooks.
  </action>
  <verify>
    <automated>npx jest src/features/record/categories</automated>
  </verify>
  <acceptance_criteria>
    - tests pass
    - `grep -c "CATEGORY_COLOR_KEYS" src/features/record/categories/CategorySheet.tsx` returns >= 1
    - `grep -rcE "#[0-9A-Fa-f]{6}" src/features/record/categories/*.tsx` returns 0 for every file
  </acceptance_criteria>
  <done>Category create/edit and merge-or-archive work with undo toasts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CategoriesScreen</name>
  <files>src/features/record/categories/CategoriesScreen.tsx, src/features/record/categories/__tests__/categories.test.tsx</files>
  <read_first>
    - src/features/record/categories/CategorySheet.tsx (Task 1)
    - .planning/phases/02-record/02-UI-SPEC.md (Categories focal point: name + glyph anchor, usage secondary)
  </read_first>
  <behavior>
    - lists active categories (glyph + name as the anchor) in lookup order; tapping opens CategorySheet edit; 'Add category' opens new
    - a system section lists Transfer and Settlement with categories.systemNote and no edit action
    - an 'Archived' section lists archived categories with a 'Restore' action (toast)
  </behavior>
  <action>
Create `CategoriesScreen()` inside `Screen scroll` per <behavior>. Tests with mocked hooks.
  </action>
  <verify>
    <automated>npx jest src/features/record/categories && npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - tests, lint and typecheck pass
  </acceptance_criteria>
  <done>REC-07 is usable end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> categories | Names become per-user records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-25-01 | Elevation of Privilege | system categories | mitigate | No edit affordance; mutation hooks throw; server policy rejects |
| T-02-25-02 | Tampering (orphaned history) | removal | mitigate | No delete path exists: merge (atomic, undoable) or archive only (D-36) |
| T-02-25-03 | Tampering (design fidelity) | colour | mitigate | Only the 7 swatch keys selectable; DB check constraint enforces the same set |
</threat_model>

<verification>
- `npx jest src/features/record/categories`, lint, typecheck green.
</verification>

<success_criteria>
- Users manage their own categories within the design system and without orphaning rows.
</success_criteria>

<output>
After completion, create `.planning/phases/02-record/02-25-SUMMARY.md`
</output>
