---
phase: 02-record
plan: 31
type: execute
wave: 13
depends_on: ["02-09", "02-30"]
files_modified:
  - docs/acceptance/phase-02-record.md
  - docs/acceptance/fixtures/sample-bank-export.csv
autonomous: false
requirements: [REC-01, REC-02, REC-03, REC-04, REC-05, REC-06, REC-07, REC-08, REC-09, REC-10, REC-11, REC-12, ACT-01, ACT-02, ACT-03, ACT-04, ACT-05, ANL-05]

must_haves:
  truths:
    - "Every Phase 2 migration (20260926000100 to 20260926000600) is applied to the production Supabase project after the full local gate is green (schema push requirement)"
    - "Existing production users have their seeded categories (back-fill ran), and the recurring-materialise-daily and record-tombstone-purge-daily cron jobs exist in production"
    - "On a fresh development build (new native modules: document picker, date picker) a user can run the whole Record flow on a device: first account, import with mapping correction, add/edit/delete, recurring with skip/this-and-future/end, Activity month switch/search/filter/bulk delete, undo from toast and History, and a refused undo with its explanation"
  artifacts:
    - path: "docs/acceptance/phase-02-record.md"
      provides: "production + device acceptance record for Phase 2"
      contains: "20260926000600"
    - path: "docs/acceptance/fixtures/sample-bank-export.csv"
      provides: "device-test CSV (DD/MM dates, comma decimals, a duplicate pair, a monthly repeat)"
  key_links:
    - from: "npm run supabase:db:push"
      to: "production project (SUPABASE_PROD_PROJECT_REF)"
      via: "preflight ref check then supabase db push"
      pattern: "supabase:db:push"
---

<objective>
Roll Phase 2 out to the single (production) Supabase project and prove it on a device: full local gate, schema push [BLOCKING], live checks, then a human device walkthrough of every success criterion.

Purpose: the phase cannot pass verification without the schema on production; REC-09 file picking, toast timing and the refusal flow need a real device or emulator (02-VALIDATION.md manual-only rows).
Output: production state changed, one acceptance doc, one CSV fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-record/02-VALIDATION.md
@.planning/phases/01-money-core/01-16-PLAN.md
@scripts/supabase-preflight.mjs
@supabase/config.toml

<interfaces>
- One Supabase project; it is production. NEVER use the Supabase MCP connector (wrong account). Use the Supabase CLI / Management API with `SUPABASE_ACCESS_TOKEN` from .env.local (never printed, never committed).
- `npm run supabase:db:push` = preflight + `supabase db push`; `-- --yes` skips the interactive confirm. Precedent 01-16 Task 1: if .env.local is not shell-sourceable, read SUPABASE_ACCESS_TOKEN and SUPABASE_DB_PASSWORD with a silent node one-liner into a single child process.
- Precedent 00-09/01-16: if the sandbox refuses a production command, STOP and return a checkpoint with the exact remaining commands; do not change targets or improvise.
- Management API SQL: `POST https://api.supabase.com/v1/projects/<ref>/database/query` with `{ "query": "…" }` and `Authorization: Bearer $SUPABASE_ACCESS_TOKEN`.
- Phase 1 plan 01-16 must already have pushed 20260924000100..000700; this push adds only the 202609260001..0006 files.
- Android device loop (00-04 precedent): `npx expo run:android --device <AVD name>`; verify with `adb shell uiautomator dump` when screenshots are blank.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Full local gate, CSV fixture, schema push to production, live checks</name>
  <files>docs/acceptance/phase-02-record.md, docs/acceptance/fixtures/sample-bank-export.csv</files>
  <read_first>
    - CLAUDE.md (Supabase access section)
    - scripts/supabase-preflight.mjs
    - .planning/phases/01-money-core/01-16-PLAN.md (push procedure and secret hygiene)
  </read_first>
  <action>
0. Precondition: `npx supabase migration list --linked` must show 20260924000700 applied remotely (Phase 1 rollout, 01-16). If not, STOP and return a checkpoint: "Phase 1 plan 01-16 must push first."
1. Full local gate (all must pass before any production command):
```bash
npm run lint && npm run typecheck && npm run depcruise && npm run check:ignores && npm run test:coverage && npm run verify:gates \
 && npm run lint:migrations && npm run verify:migrations && npm run check:money-mirror && npm run check:recurring-mirror \
 && npx supabase db reset --local && npx supabase test db
```
2. Write `docs/acceptance/fixtures/sample-bank-export.csv` (UTF-8, comma-separated, quoted descriptions): header `Date,Description,Paid out,Paid in,Balance`; about 40 rows across three months with DD/MM/YYYY dates (include at least one day > 12 and a few ambiguous ones), amounts with comma thousands and point decimals, one exact duplicate pair on the same date, a 'NETFLIX.COM' debit of 10.99 on roughly the same day each month for three months, a 'Salary' credit monthly, and one description containing a quoted comma.
3. Push non-interactively (token never printed): `npm run supabase:db:push -- --yes`, then `npx supabase migration list --linked`; confirm 20260926000100, 000200, 000300, 000400, 000500 and 000600 appear in both Local and Remote.
4. Live checks via the Management API (print only non-secret results): `select count(*) from public.profiles p where (select count(*) from public.categories c where c.owner_id = p.id) < 15` = 0; `select jobname, schedule from cron.job where jobname in ('recurring-materialise-daily','record-tombstone-purge-daily')` returns 2 rows; `select extname from pg_extension where extname = 'pg_trgm'` returns 1 row; `select count(*) from information_schema.views where table_name = 'transactions_active'` = 1.
5. Start docs/acceptance/phase-02-record.md with: date, gate command + result, push summary (no secrets), migration list excerpt, live check results. Commit `docs(02-31): record production rollout`.
If the sandbox refuses the push, STOP and return a `checkpoint:human-action` listing steps 3-4 verbatim.
  </action>
  <verify>
    <automated>npx supabase migration list --linked | grep -c "20260926000600"</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase migration list --linked` shows all six 202609260001xx-0006xx migrations applied remotely
    - docs/acceptance/phase-02-record.md contains "20260926000600", "recurring-materialise-daily" and "record-tombstone-purge-daily"
    - `git diff --cached | grep -cE "sbp_|SUPABASE_ACCESS_TOKEN=|SUPABASE_DB_PASSWORD="` returns 0
  </acceptance_criteria>
  <done>The production schema matches git for every Phase 2 migration and the jobs are scheduled.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Device walkthrough of the Phase 2 success criteria</name>
  <files>docs/acceptance/phase-02-record.md</files>
  <read_first>
    - .planning/phases/02-record/02-VALIDATION.md (manual-only verifications)
    - docs/acceptance/phase-02-record.md
  </read_first>
  <action>
Claude first builds and installs a fresh development build on the Android emulator (`npx expo run:android --device <AVD>`; the document picker and date picker are new native modules), pushes the CSV fixture with `adb push docs/acceptance/fixtures/sample-bank-export.csv /sdcard/Download/`, and signs in. The user then performs the checks below and replies.
  </action>
  <what-built>Record end to end against production: entry sheet, recurring series, CSV import, Activity (list, months, search, filters, bulk), accounts, categories, undo toast and History.</what-built>
  <how-to-verify>
    1. Sign in with a fresh test account: you are taken to "Your first account"; create "Current account" (GBP). Choose "Bring your history".
    2. Import: pick sample-bank-export.csv from Downloads. Confirm "Check the columns" shows Date, Description, Money out, Money in; flip the date format if prompted to DD/MM/YYYY; Continue.
    3. Review: the duplicate pair shows one "Possible duplicate" unticked; categories are guessed (Salary -> Income, NETFLIX -> Subscriptions); commit "Import N transactions". A toast "Imported N lines" with Undo stays about 6 s.
    4. Accept "Looks like NETFLIX.COM, £10.99 monthly. Make it recurring?"; Activity for next month shows a pending Netflix line.
    5. Tap Add -> Money out: log "Coffee", £3.40, Dining, today -> toast "Added · Coffee" (about 3 s); tap Undo -> the line disappears.
    6. Mark the pending Netflix line paid in one tap; then open an occurrence, change the amount, choose "This and future"; later months show the new amount; the paid line did not change. Skip one occurrence; End the series (confirm).
    7. Switch months with the chevrons and the month list; search "salary" with "Every month" and see lines from all three months; filter by Dining and by account.
    8. Select 3 lines -> Delete -> confirm; toast "3 deleted" -> Undo restores all three.
    9. Categories: rename Groceries to "Food shop", recolour it, add "Pets"; remove "Pets" after assigning one line to it -> Merge into Dining; undo.
    10. Accounts: balance equals opening balance plus paid lines; pending shown as "still to come".
    11. History: shows recent changes; "Undo to here" on the third item undoes three changes.
    12. Refusal: edit a line in the app; Claude then updates the same row via the Management API (as a system write); tap Undo on your edit in History -> it is refused with "… was changed elsewhere after this, so it can’t be undone." and stays greyed.
    13. Airplane mode: add a line and tap its Undo; both show as queued; turn the network back on; they apply in order.
    14. With TalkBack on, a toast stays until dismissed.
    Record pass/fail per step in docs/acceptance/phase-02-record.md. REC-12's cross-member case is proven by pgTAP 29 (refusal names the other member); a two-person device test waits for Phase 8 invites.
  </how-to-verify>
  <resume-signal>Type "approved", or list the failing step numbers with what you saw</resume-signal>
  <verify>
    <automated>grep -c "approved" docs/acceptance/phase-02-record.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/acceptance/phase-02-record.md records a pass/fail line for steps 1-14 and the user's approval
  </acceptance_criteria>
  <done>The user has confirmed the Record flow on a device.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator machine -> production DB | Schema push and Management API queries with a personal access token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-31-01 | Tampering (wrong project) | db push | mitigate | Preflight asserts config.toml project_id == SUPABASE_PROD_PROJECT_REF; MCP connector never used |
| T-02-31-02 | Information Disclosure | access token | mitigate | Read from .env.local into a child process only; never echoed; staged-diff secret grep gate |
| T-02-31-03 | Repudiation (no rollback path) | production push without backups | accept | ENV-16 moved to Phase 10; every migration is additive and pgTAP-proven locally first; accepted dogfooding risk recorded in STATE.md |
</threat_model>

<verification>
- Remote migration list shows all Phase 2 migrations; live checks recorded; user approval recorded.
</verification>

<success_criteria>
- Production runs the Phase 2 schema and the user has walked through every Phase 2 success criterion on a device.
</success_criteria>

<output>
After completion, create `.planning/phases/02-record/02-31-SUMMARY.md`
</output>
