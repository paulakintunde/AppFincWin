---
phase: 02-record
plan: 29
type: execute
wave: 12
depends_on: ["02-21", "02-23", "02-24", "02-25", "02-27", "02-28"]
files_modified:
  - app/(app)/_layout.tsx
  - app/(app)/activity.tsx
  - app/(app)/accounts/index.tsx
  - app/(app)/accounts/[id].tsx
  - app/(app)/categories.tsx
  - app/(app)/history.tsx
  - app/(app)/import.tsx
  - src/features/record/__tests__/routes.test.tsx
autonomous: true
requirements: [ACT-01, REC-07, REC-08, REC-09, REC-11]

must_haves:
  truths:
    - "Every Record screen is reachable as a plain Expo Router route in the signed-in group, without bespoke navigation chrome (UI-SPEC; Phase 3 owns tabs/back-stack)"
    - "The Undo toast host is mounted once for the whole signed-in app, so every undoable change anywhere shows its toast (D-31)"
    - "Import can be opened with an entry context (onboarding, you, account) and an optional pre-selected account (D-22)"
  artifacts:
    - path: "app/(app)/activity.tsx"
      provides: "Activity route"
      contains: "ActivityScreen"
    - path: "app/(app)/import.tsx"
      provides: "Import route with entry/account params"
      contains: "ImportScreen"
    - path: "app/(app)/_layout.tsx"
      provides: "signed-in layout with UndoToastHost"
      contains: "UndoToastHost"
  key_links:
    - from: "app/(app)/_layout.tsx"
      to: "src/features/record/history/UndoToastHost.tsx"
      via: "rendered once beside the stack"
      pattern: "<UndoToastHost"
---

<objective>
Expose the Record screens as routes in the signed-in `(app)` group and mount the Undo toast host once.

Purpose: turns the Phase 2 screens into a navigable app. Onboarding, You-screen entry points and the landing route follow in 02-30.
Output: six route files, an edited signed-in layout, a routing test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-record/02-UI-SPEC.md
@app/_layout.tsx
@app/(app)/_layout.tsx
@src/features/record/activity/ActivityScreen.tsx
@src/features/record/accounts/AccountsScreen.tsx
@src/features/record/accounts/AccountDetailScreen.tsx
@src/features/record/categories/CategoriesScreen.tsx
@src/features/record/history/HistoryScreen.tsx
@src/features/record/history/UndoToastHost.tsx
@src/features/record/import/ImportScreen.tsx

<interfaces>
Screens: `ActivityScreen({ onOpenAccounts, onOpenHistory, onOpenYou })`, `AccountsScreen({ onOpenAccount })`, `AccountDetailScreen({ accountId, onImport })`, `CategoriesScreen()`, `HistoryScreen()`, `ImportScreen({ entry, accountId, onDone })`, `UndoToastHost()`.
Expo Router (~57): `router.push('/accounts/' + id)`, `useLocalSearchParams<{ id: string }>()`, `Stack`, `Redirect`.
Prerequisites from earlier phases (not in this phase): Phase 0 plan 00-17 (root layout with auth routing), 00-18 (`app/(app)/_layout.tsx`, `app/(app)/you.tsx`, `src/features/you/YouScreen.tsx`), Phase 1 plan 01-15 (`QueryProvider` mounted in app/_layout.tsx).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Precondition check, then Record routes</name>
  <files>app/(app)/activity.tsx, app/(app)/accounts/index.tsx, app/(app)/accounts/[id].tsx, app/(app)/categories.tsx, app/(app)/history.tsx, app/(app)/import.tsx</files>
  <read_first>
    - app/_layout.tsx and app/(app)/_layout.tsx (as landed by 00-17/00-18/01-15)
    - .planning/phases/01-money-core/01-15-SUMMARY.md and .planning/phases/00-foundation/00-18-SUMMARY.md (if present)
  </read_first>
  <action>
1. Precondition: confirm `app/(app)/_layout.tsx` exists, `src/features/you/YouScreen.tsx` exists, and `grep -c "QueryProvider" app/_layout.tsx` >= 1. If any is missing, STOP and return a `checkpoint:human-action` naming the missing prerequisite plans (00-17, 00-18, 01-15) — do not create substitutes.
2. Create thin route files (default export components, no styling, no literal JSX text):
   - `app/(app)/activity.tsx` -> `<ActivityScreen onOpenAccounts={() => router.push('/accounts')} onOpenHistory={() => router.push('/history')} onOpenYou={() => router.push('/you')} />`
   - `app/(app)/accounts/index.tsx` -> `<AccountsScreen onOpenAccount={(id) => router.push(\`/accounts/${id}\`)} />`
   - `app/(app)/accounts/[id].tsx` -> reads `id`; `<AccountDetailScreen accountId={id} onImport={(accountId) => router.push({ pathname: '/import', params: { entry: 'account', accountId } })} />`
   - `app/(app)/categories.tsx` -> `<CategoriesScreen />`
   - `app/(app)/history.tsx` -> `<HistoryScreen />`
   - `app/(app)/import.tsx` -> reads `entry` ('onboarding' | 'you' | 'account', default 'you') and optional `accountId`; `<ImportScreen entry={entry} accountId={accountId ?? null} onDone={() => router.replace('/activity')} />` (validate entry against the three literals; anything else becomes 'you').
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - all six route files exist and typecheck
    - `grep -c "ImportScreen" "app/(app)/import.tsx"` returns >= 1
    - lint passes (no literal JSX strings in app/)
  </acceptance_criteria>
  <done>Every Record screen has a route.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount UndoToastHost in the signed-in layout + routing test</name>
  <files>app/(app)/_layout.tsx, src/features/record/__tests__/routes.test.tsx</files>
  <read_first>
    - app/(app)/_layout.tsx (keep its consent gating and Stack exactly as they are)
  </read_first>
  <behavior>
    - the signed-in layout renders its existing Stack plus exactly one UndoToastHost overlay (absolute, above the bottom inset)
    - import route maps an unknown entry param to 'you'
  </behavior>
  <action>
Wrap the existing returned tree of app/(app)/_layout.tsx in a `View style={{ flex: 1 }}` (only if it is not already a single container) and render `<UndoToastHost />` after the Stack. Do not change consent or auth logic. Write src/features/record/__tests__/routes.test.tsx rendering the import route component with mocked ImportScreen to assert the entry mapping, and the layout with mocked children to assert one UndoToastHost.
  </action>
  <verify>
    <automated>npx jest src/features/record/__tests__/routes.test.tsx && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - test, typecheck, lint pass
    - `grep -c "<UndoToastHost" "app/(app)/_layout.tsx"` returns 1
  </acceptance_criteria>
  <done>The toast host is live app-wide and routes are wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deep link params -> screens | Route params are untrusted strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-29-01 | Tampering | import route params | mitigate | entry validated against three literals; accountId only pre-selects and must match an account the user can see (RLS) |
| T-02-29-02 | Elevation of Privilege | route access | mitigate | Routes live inside the existing signed-in (app) group behind Phase 0 auth gating |
</threat_model>

<verification>
- typecheck, lint, routing test green.
</verification>

<success_criteria>
- Record is navigable; undo toasts appear everywhere in the signed-in app.
</success_criteria>

<output>
After completion, create `.planning/phases/02-record/02-29-SUMMARY.md`
</output>
