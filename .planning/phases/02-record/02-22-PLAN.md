---
phase: 02-record
plan: 22
type: execute
wave: 8
depends_on: ["02-14", "02-15", "02-19", "02-20"]
files_modified:
  - package.json
  - src/features/record/activity/activitySections.ts
  - src/features/record/activity/ActivityScreen.tsx
  - src/features/record/activity/ActivityRow.tsx
  - src/features/record/activity/MonthSwitcher.tsx
  - src/features/record/activity/MonthTotalsBar.tsx
  - src/features/record/activity/__tests__/activitySections.test.ts
  - src/features/record/activity/__tests__/ActivityScreen.test.tsx
autonomous: true
requirements: [ACT-01, ACT-02, REC-06, REC-03, REC-18]

must_haves:
  truths:
    - "A user sees all of a month's transactions in one list, grouped as Still to come (pending + expected projections), Paid and Skipped, like the prototype's month sheet (ACT-01, D-10)"
    - "A user can switch to any month that has data, plus the current and next month, from chevrons or a month list (ACT-02)"
    - "The month shows paid In/Out/Net and a separate Still to come figure; lines without a home-currency figure are counted, not guessed (D-10)"
    - "Pending lines dated before today are flagged overdue and stay pending; a pending line can be marked paid in one tap (D-05, D-06)"
    - "Tapping a line opens it in the transaction sheet; an Add button opens a new expense or income (REC-03; Shell's FAB replaces the button in Phase 3)"
    - "The list is the visual anchor; search, filters and bulk controls never outweigh it (UI-SPEC focal point)"
    - "A transfer shows as two ordinary rows, one per leg, each with the Transfer category's glyph and tint, named 'Transfer to {account}' on the paying leg and 'Transfer from {account}' on the receiving leg, with its signed amount; the month's In/Out/Net and Still to come leave transfers out (D-50, D-44, UI-SPEC Transfers)"
    - "Tapping a transfer leg opens the pair in the entry sheet; transfer legs never show a Mark paid control (D-51, D-56)"
  artifacts:
    - path: "src/features/record/activity/ActivityScreen.tsx"
      provides: "ActivityScreen"
      exports: ["ActivityScreen"]
    - path: "src/features/record/activity/activitySections.ts"
      provides: "buildActivityItems (pure)"
      exports: ["buildActivityItems"]
  key_links:
    - from: "src/features/record/activity/ActivityScreen.tsx"
      to: "src/data/queries/activity.ts useMonthView / useTransactionMonths"
      via: "hooks"
      pattern: "useMonthView\\("
    - from: "src/features/record/activity/ActivityScreen.tsx"
      to: "src/features/record/entry/TransactionSheet.tsx"
      via: "sheet state (new/edit)"
      pattern: "<TransactionSheet"
---

<objective>
Build the Activity month screen: grouped list with projections, month switcher, totals line, overdue flags, one-tap mark paid, and the entry points into the transaction sheet.

Purpose: ACT-01, ACT-02 and the user-facing half of D-05/D-06/D-10. Search, filters and bulk select are layered on by plan 02-23.
Output: pure item builder, screen and three components, tests; `@shopify/flash-list` v2 installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-record/02-UI-SPEC.md
@src/ui/Screen.tsx
@src/ui/money/useMoneyFormatter.ts
@src/features/record/entry/TransactionSheet.tsx
@src/data/queries/activity.ts

<interfaces>
From 02-14: `useMonthView({ householdId, homeCurrency, today }, month) -> { rows: ActivityRowView[]; projections: ProjectionView[]; totals: MonthTotals; isLoading; isError; refetch }`, `useTransactionMonths(householdId, today) -> { months }`, `useCategoryLookup(userId)`, `categoryName(row, t)`, `useRecordContext()`.
From 02-15: `useMarkPaid().markPaid(row, ownerId, today) -> stepId`, `showToast`.
From 02-20: `TransactionSheet({ visible, mode: EntryMode, onClose })`.
From 02-19: Row, Pill, Chip, CategoryGlyph, EmptyState, Sheet.
From 02-06: `nextMonth(month)`, `monthOf(today)`.
`useMoneyFormatter(showCents) -> { formatMoney({ amount, currency }), formatDate(localDate, style), locale }`.
Copy: activity.* (including transferTo/transferFrom/transferOther), record.recurring.overdue/dueOn/projected/markPaid, sync.pendingRow, undo.label.markedPaid.
From 02-14 (extension): `ActivityRowView.counterpartAccountId` (set on transfer legs), totals already exclude transfers (monthTotals isTransfer).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure section builder + FlashList</name>
  <files>package.json, src/features/record/activity/activitySections.ts, src/features/record/activity/__tests__/activitySections.test.ts</files>
  <read_first>
    - src/data/queries/activity.ts (ActivityRowView, ProjectionView)
  </read_first>
  <behavior>
    - buildActivityItems(rows, projections) -> flat list for FlashList: header 'pending' then pending rows and projections merged by date ascending, header 'paid' then paid rows by date descending (created_at desc tie-break), header 'skipped' then skipped rows; headers omitted for empty sections
    - item keys are stable: row id, projection key, 'header:<section>'
    - getItemType returns 'header' | 'row' | 'projection'
  </behavior>
  <action>
Run `npx expo install @shopify/flash-list` (v2, JS-only on the New Architecture per CLAUDE.md). Create:
```typescript
export type ActivitySection = 'pending' | 'paid' | 'skipped';
export type ActivityItem =
  | { type: 'header'; key: string; section: ActivitySection }
  | { type: 'row'; key: string; row: ActivityRowView }
  | { type: 'projection'; key: string; projection: ProjectionView };
export function buildActivityItems(rows: readonly ActivityRowView[], projections: readonly ProjectionView[]): ActivityItem[];
```
Tests first.
  </action>
  <verify>
    <automated>npx jest src/features/record/activity/__tests__/activitySections.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests pass; `grep -c "@shopify/flash-list" package.json` returns 1
  </acceptance_criteria>
  <done>List structure is a tested pure function.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ActivityScreen, ActivityRow, MonthSwitcher, MonthTotalsBar</name>
  <files>src/features/record/activity/ActivityScreen.tsx, src/features/record/activity/ActivityRow.tsx, src/features/record/activity/MonthSwitcher.tsx, src/features/record/activity/MonthTotalsBar.tsx, src/features/record/activity/__tests__/ActivityScreen.test.tsx</files>
  <read_first>
    - .planning/phases/02-record/02-UI-SPEC.md (Activity copy, focal point, colours: accent only for primary CTA/Mark paid, danger for negative figures only where specified)
    - src/ui/SyncStatusLine.tsx (label/meta text styling)
    - src/features/record/entry/TransactionSheet.tsx (EntryMode)
  </read_first>
  <behavior>
    - header: title 'Activity', MonthSwitcher, an 'Add' Pill that offers Money out / Money in (opens TransactionSheet new mode with that direction), and small text links 'Accounts', 'History', 'You' that call props onOpenAccounts/onOpenHistory/onOpenYou
    - MonthSwitcher shows the month as 'September 2026' (Intl month+year in the device locale), prev/next chevrons move through useTransactionMonths' list (disabled at the ends), tapping the label opens a Sheet listing all months; chevrons have accessibility labels activity.monthSwitcher.previous/next
    - MonthTotalsBar shows In, Out, Net (formatted in home currency) and 'Still to come' with its amount; when unconvertedCount > 0 a meta line activity.totals.unconverted
    - ActivityRow shows the CategoryGlyph (uncategorised uses the 'slate' swatch and letter from the name), name, meta line (category name · account name · short date), amount in its own currency (outflow with U+2212 via the formatter), home figure beneath when currencies differ, 'queued' tag when pending sync, 'Overdue' tag when overdue, 'Due {date}' for pending, and a 'Mark paid' Pill for pending rows that calls markPaid then showToast ordinary with the stepId
    - projection rows are muted with the 'Expected' tag and are not tappable
    - tapping a row opens TransactionSheet in edit mode for that row
    - empty month shows EmptyState 'Nothing in {month} yet.' / 'Add the first one with the Add button.'
    - a row with transfer_id renders the Transfer category glyph/tint (categorySwatch of the system Transfer category), the name t('activity.transferTo', { account }) when its amount is negative or t('activity.transferFrom', { account }) when positive, where account is the counterpart account's name (t('activity.transferOther') when counterpartAccountId is null), and no Mark paid Pill
    - the 'Add' Pill also offers Transfer (opens TransactionSheet new mode with direction 'transfer')
    - MonthTotalsBar figures come from totals that already exclude transfer legs; a month holding only a transfer pair shows In 0, Out 0, Net 0
  </behavior>
  <action>
Create `ActivityScreen({ onOpenAccounts, onOpenHistory, onOpenYou, initialMonth? })` inside `Screen` (not scroll — FlashList scrolls): state `month` (default monthOf(today)), sheet mode state; data from useRecordContext, useMonthView, useTransactionMonths, useAccounts, useCategoryLookup. Render header, MonthTotalsBar, then `FlashList` over buildActivityItems with getItemType; section headers use the label role (activity.section.*). Leave a commented slot `/* 02-23: search, filters and bulk bar mount here */` between the totals and the list, and pass `selectable={false}` to ActivityRow (02-23 turns it on).
Create the three components per <behavior> using only primitives and tokens. Tests with RNTL and mocked hooks (month navigation, empty state, mark paid, row opens sheet, projection not tappable).
  </action>
  <verify>
    <automated>npx jest src/features/record/activity && npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - tests, lint and typecheck pass
    - `grep -c "FlashList" src/features/record/activity/ActivityScreen.tsx` returns >= 1
    - `grep -c "02-23: search, filters and bulk bar mount here" src/features/record/activity/ActivityScreen.tsx` returns 1
    - `grep -cE "activity\.transferTo|activity\.transferFrom" src/features/record/activity/ActivityRow.tsx` returns 2
    - `grep -rcE "#[0-9A-Fa-f]{6}" src/features/record/activity/*.tsx` returns 0 for every file
  </acceptance_criteria>
  <done>The Activity month screen works end to end with the entry sheet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cache -> screen | Displays the household's own data only (RLS upstream) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-22-01 | Tampering (misleading totals) | MonthTotalsBar | mitigate | Totals from engine monthTotals; unconverted lines counted and labelled, never estimated |
| T-02-22-02 | Repudiation | Mark paid | mitigate | Explicit tap only, undo toast, history fallback |
| T-02-22-03 | Tampering (misleading totals) | transfer legs | mitigate | Rendered as ordinary rows but excluded from totals by transfer_id upstream (02-06/02-14) |
</threat_model>

<verification>
- `npx jest src/features/record/activity`, lint, typecheck green.
</verification>

<success_criteria>
- ACT-01/ACT-02 satisfied on the Activity screen.
</success_criteria>

<output>
After completion, create `.planning/phases/02-record/02-22-SUMMARY.md`
</output>
