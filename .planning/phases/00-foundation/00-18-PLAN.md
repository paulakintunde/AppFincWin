---
phase: 00-foundation
plan: 18
type: execute
wave: 6
depends_on: ["00-17", "00-13", "00-10"]
files_modified:
  - src/features/you/useProfile.ts
  - src/features/auth/signOut.ts
  - src/features/consent/useConsent.ts
  - src/features/consent/ConsentScreen.tsx
  - src/features/you/YouScreen.tsx
  - src/features/you/components/AccentSwitcher.tsx
  - src/features/you/components/FontPairingSwitcher.tsx
  - src/features/you/components/AnalyticsToggle.tsx
  - src/features/you/components/ConnectionStatus.tsx
  - src/features/you/components/SettingsGroup.tsx
  - app/(app)/_layout.tsx
  - app/(app)/you.tsx
  - app/(app)/consent.tsx
  - src/features/you/__tests__/useProfile.test.tsx
  - src/features/auth/__tests__/signOut.test.ts
  - src/features/consent/__tests__/ConsentScreen.test.tsx
  - src/features/you/__tests__/YouScreen.test.tsx
autonomous: true
requirements: [ANL-02, ENV-03, FND-06, ACC-05, DSG-03]

must_haves:
  truths:
    - "After first sign-in, a one-screen consent prompt appears with two equal-weight choices; nothing is sent to PostHog unless the user taps Share usage (D-17, ANL-02)"
    - "The choice is stored on the profile (analytics_consent + timestamp) and can be changed any time from the You screen toggle (ANL-02)"
    - "The You screen shows signed-in identity, a 4-accent switcher, a 4-font-pairing switcher, the analytics toggle, a live Supabase connection indicator and Sign out, all in theme tokens (D-13)"
    - "Accent and font choices save to the profile row and the local cache; on sign-in the profile's saved theme is applied (D-14)"
    - "Sign-out warns with the pending-change count when unsynced writes exist, then wipes the encrypted session, its key, every fincwin: cache key and queued writes, resets PostHog identity and resets the theme; with nothing pending it signs out without a dialog (D-15)"
    - "No copy claims data stays on the device (D-15)"
  artifacts:
    - path: "src/features/auth/signOut.ts"
      provides: "requestSignOut / performSignOut"
      exports: ["requestSignOut", "performSignOut"]
    - path: "src/features/you/useProfile.ts"
      provides: "Profile load + theme/consent updates"
      exports: ["useProfile"]
    - path: "src/features/consent/ConsentScreen.tsx"
      provides: "D-17 consent prompt"
    - path: "src/features/you/YouScreen.tsx"
      provides: "D-13 settings screen"
    - path: "app/(app)/_layout.tsx"
      provides: "Signed-in group routing: consent first if unanswered"
  key_links:
    - from: "src/features/auth/signOut.ts"
      to: "wipeDeviceData + getPendingWriteCount + analytics.disable + supabase.auth.signOut"
      via: "ordered calls"
      pattern: "wipeDeviceData"
    - from: "src/features/you/useProfile.ts"
      to: "public.profiles"
      via: "select/update on own row"
      pattern: "from\\('profiles'\\)"
    - from: "src/features/you/components/ConnectionStatus.tsx"
      to: "checkConnection"
      via: "on mount and on AppState active"
      pattern: "checkConnection"
---

<objective>
Build the signed-in surface: the consent prompt, the You/settings screen with theme switching, the analytics toggle, the live connection check, and the sign-out flow that wipes the device.

Purpose: D-13 (the screen becomes the real You tab later), D-14 (theme on profile), D-15 (sign-out wipe), D-17 (consent). This is the on-device proof surface for FND-06, ANL-02 and ENV-03.
Output: The (app) route group, consent and you features, sign-out logic, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-foundation/00-UI-SPEC.md
@.planning/phases/00-foundation/00-CONTEXT.md

<interfaces>
src/services/storage/wipe.ts (00-10): registerWipeHandler, getPendingWriteCount(), wipeDeviceData()
src/services/supabase (00-10): supabase, checkConnection(): Promise<{ok:true, latencyMs} | {ok:false, reason:'offline'|'error'}>
src/services/analytics (00-13): getAnalytics() -> { enable(uuid), disable(), track(e, p), isEnabled() }
src/theme (00-11): useTheme() -> { accent, pairing, setAccent, setPairing, applyRemote(accent, pairing), reset(), colors, fonts }, ACCENTS, ACCENT_KEYS, FONT_PAIRINGS, radii, space, textRole, useMotion; src/ui/Screen
src/features/auth/AuthProvider (00-15): useAuth() -> { status, user }
profiles columns (00-06): id, full_name, email, accent, font_pairing, analytics_consent ('granted'|'declined'|null), analytics_consent_at
i18n keys (00-12): consent.*, you.*, signOut.confirm.body (count plural), signOut.confirm.cancel/proceed
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Profile hook (theme + consent persistence) and the sign-out flow</name>
  <files>src/features/you/useProfile.ts, src/features/consent/useConsent.ts, src/features/auth/signOut.ts, src/features/you/__tests__/useProfile.test.tsx, src/features/auth/__tests__/signOut.test.ts</files>
  <read_first>
    - src/services/storage/wipe.ts
    - src/services/analytics/posthog.ts
    - src/theme/ThemeProvider.tsx
    - .planning/phases/00-foundation/00-CONTEXT.md (D-14, D-15, D-17)
  </read_first>
  <behavior>
    - useProfile: when signed in, it selects the own profile row and calls theme.applyRemote(accent, font_pairing); setAccent('rust') updates the theme immediately, updates profiles.accent and tracks theme_accent_changed (a no-op unless consented); a failed update keeps the local choice and surfaces `saveError`
    - useConsent: consent is null means `needsPrompt = true`; grant() updates analytics_consent='granted' plus analytics_consent_at=now, then calls analytics.enable(user.id) and tracks analytics_opted_in; decline() writes 'declined' and calls analytics.disable(); on sign-in with stored 'granted', analytics.enable(user.id) runs once
    - requestSignOut(): with getPendingWriteCount() = 3 it returns {needsConfirm:true, count:3}; with 0 it calls performSignOut and returns {needsConfirm:false}
    - performSignOut() calls, in order: analytics.disable(), supabase.auth.signOut({scope:'local'}), wipeDeviceData(), theme.reset(); if supabase signOut rejects (offline), it still wipes locally and resolves
  </behavior>
  <action>
1. `useProfile.ts`: `useProfile(): { profile: Profile | null; loading: boolean; saveError: boolean; setAccent(k: AccentKey): Promise<void>; setPairing(k: FontPairingKey): Promise<void>; refresh(): Promise<void> }`. It uses plain hooks. TanStack Query arrives in Phase 1, so this hook is the seam Phase 1 swaps. Queries: `supabase.from('profiles').select('id, full_name, email, accent, font_pairing, analytics_consent, analytics_consent_at').eq('id', user.id).single()`. Updates send only the changed columns (the column grants from 00-06).
2. `useConsent.ts`: `useConsent(): { consent: 'granted' | 'declined' | null; needsPrompt: boolean; grant(): Promise<void>; decline(): Promise<void>; setEnabled(on: boolean): Promise<void> }`, built on useProfile's data.
3. `signOut.ts`: `requestSignOut(deps?)` and `performSignOut(deps?)` with an injectable deps object ({ getPendingWriteCount, wipeDeviceData, analytics, supabase, resetTheme }) for testing. Phase 0 has no write queue yet, so the pending count is 0 unless a Phase 1 handler registers. The warning path is still implemented and tested with a provider returning 3 (D-15).
  </action>
  <verify>
    <automated>npx jest src/features/you/__tests__/useProfile.test.tsx src/features/auth/__tests__/signOut.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both test files pass, covering every behaviour line
    - `grep -q "wipeDeviceData" src/features/auth/signOut.ts` and `grep -q "getPendingWriteCount" src/features/auth/signOut.ts` succeed
    - `grep -q "scope: 'local'" src/features/auth/signOut.ts` succeeds
  </acceptance_criteria>
  <done>D-14 remote sync, D-15 wipe and D-17 persistence logic are proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consent screen and the signed-in group layout</name>
  <files>src/features/consent/ConsentScreen.tsx, src/features/consent/__tests__/ConsentScreen.test.tsx, app/(app)/_layout.tsx, app/(app)/consent.tsx</files>
  <read_first>
    - .planning/phases/00-foundation/00-UI-SPEC.md (Copywriting: consent heading/body/CTAs; Color: accent on "Share usage")
    - src/features/consent/useConsent.ts
  </read_first>
  <behavior>
    - It renders the heading, the body, a "Never sent" list with the 5 items, and two buttons of identical width and height (equal weight, D-17)
    - Tapping Share usage calls grant() once; Not now calls decline() once; afterwards the screen navigates to /you
    - There is no close/skip control other than the two choices, and neither button is pre-selected
  </behavior>
  <action>
1. `ConsentScreen.tsx`: `Screen` with the heading role (26/700) for `consent.heading`, the body role in inkMuted for `consent.body`, then a surface card (radius 26, card-pad 16) titled `consent.neverSentHeading` (label role) listing the five `consent.neverSent.*` items as rows, then two stacked full-width pills with the same height and padding: "Share usage" in accent fill with a white label, and "Not now" as a surface fill with a 1.5px line3 border and an ink label. Both are 15/600 with minHeight 50. This is equal weight in size and position. No dark patterns: no tiny text link, no pre-selection, no countdown.
2. `app/(app)/_layout.tsx`: uses useConsent. While the profile loads it returns null (the splash has already hidden, so render a `Screen` with nothing). If `needsPrompt`, `<Redirect href="/consent" />` unless already on /consent. Then `<Stack screenOptions={{ headerShown: false }}><Stack.Screen name="you" /><Stack.Screen name="consent" options={{ gestureEnabled: false }} /></Stack>`.
3. `app/(app)/consent.tsx` renders ConsentScreen.
  </action>
  <verify>
    <automated>npx jest src/features/consent</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/features/consent` passes
    - `grep -q "consent.neverSent.amounts" src/features/consent/ConsentScreen.tsx` succeeds
    - `grep -rn "Redirect" "app/(app)/_layout.tsx"` finds the consent redirect
  </acceptance_criteria>
  <done>D-17's one-screen, equal-weight consent is in the flow right after first sign-in.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: You screen: identity, accent and font switchers, analytics toggle, connection status, sign out</name>
  <files>src/features/you/YouScreen.tsx, src/features/you/components/AccentSwitcher.tsx, src/features/you/components/FontPairingSwitcher.tsx, src/features/you/components/AnalyticsToggle.tsx, src/features/you/components/ConnectionStatus.tsx, src/features/you/components/SettingsGroup.tsx, src/features/you/__tests__/YouScreen.test.tsx, app/(app)/you.tsx</files>
  <read_first>
    - .planning/phases/00-foundation/00-UI-SPEC.md (Spacing: group-gap 18, row-pad, gap-md; Typography roles; Color: accent reserved for swatch ring, toggle on, connection dot, focus ring; destructive copy)
    - src/features/you/useProfile.ts, src/features/consent/useConsent.ts, src/features/auth/signOut.ts
    - src/theme/motion.ts
  </read_first>
  <behavior>
    - It renders the user's full_name, or the email when the name is null, and the email as a sub-label
    - Pressing the Navy swatch calls setAccent('navy'), and the swatch ring colour changes to #1F3A5F in the same render tree (live, FND-06)
    - Each font option label renders in its own pairing's display face; pressing Grotesk calls setPairing('grotesk')
    - The analytics toggle reflects consent==='granted'; toggling calls setEnabled(!on)
    - ConnectionStatus shows 'Checking connection…', then 'Connected to Supabase' with an accent dot when checkConnection returns ok, or the error copy in inkMuted when not
    - Sign out with 0 pending signs out without Alert; with 3 pending, Alert.alert is called with the plural body for count 3 and the Cancel/Sign out anyway buttons, and only "Sign out anyway" calls performSignOut
  </behavior>
  <action>
1. `SettingsGroup.tsx`: a group heading in the label role, all-caps, letter-spacing .11em (13 × 0.11), inkDim, margin-top 18 (group-gap), above a surface card (radius 26, card shadow token) whose rows have 15px padding and 1px line1 separators.
2. `YouScreen.tsx` (Screen, scroll). A `you.title` heading (26/700). An identity row: a 44px avatar circle (fill1 background, initial letter in ink, body role), name and email, 12px gap. Groups: Appearance (AccentSwitcher, FontPairingSwitcher), Privacy (AnalyticsToggle + `you.analytics.hint`), Connection (ConnectionStatus), Account (a "Sign out" row in ink; the destructive colour is reserved for the confirm-dialog copy per UI-SPEC).
3. `AccentSwitcher.tsx`: four 32px circles filled with ACCENTS values, 44px touch targets, the selected one with a 2px ring in the current accent at a 2px offset, `accessibilityRole="radio"`, `accessibilityState={{ selected }}` and `accessibilityLabel={t('you.accent.<key>')}`.
4. `FontPairingSwitcher.tsx`: four rows, each label `t('you.font.<key>')` rendered in that pairing's display family (FONT_PAIRINGS[k].display) at 15px, with a check indicator in accent for the selected one and radio a11y.
5. `AnalyticsToggle.tsx`: a custom switch (track 44×26: fill1 off, accent on; white thumb 22px) animated with Reanimated `withTiming(x, { duration: motion.duration(DURATION.entrance), easing: EASING })`, so it collapses under reduce-motion (FND-07). `accessibilityRole="switch"`.
6. `ConnectionStatus.tsx`: calls `checkConnection()` on mount and on AppState 'active'. An 8px dot (accent when ok, inkSoft1 otherwise) plus text; latency is not shown.
7. Sign-out row: `requestSignOut()`. When needsConfirm, `Alert.alert('', t('signOut.confirm.body', { count }), [{ text: t('signOut.confirm.cancel'), style: 'cancel' }, { text: t('signOut.confirm.proceed'), style: 'destructive', onPress: performSignOut }])`.
8. `app/(app)/you.tsx` renders YouScreen.
9. On the emulator (Metro running), sign in with Google once the 00-15 clients exist; otherwise verify with the unit tests only and leave device checks to 00-19. Check that switching accent and font updates the screen instantly with no reload.
  </action>
  <verify>
    <automated>npx jest src/features/you && npx jest src/theme/__tests__/noRawColours.test.ts && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/features/you` passes, covering every behaviour line
    - The noRawColours test passes (no raw hex in the new components)
    - `grep -q "checkConnection" src/features/you/components/ConnectionStatus.tsx` succeeds
    - `grep -q "motion.duration" src/features/you/components/AnalyticsToggle.tsx` succeeds
    - `grep -rniE "stay(s)? on (this|your) device" src app` returns nothing
  </acceptance_criteria>
  <done>The D-13 You screen works live: theme, consent toggle, connection indicator and D-15 sign-out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> profiles table | Own-row updates through column grants |
| device state across users | A shared device and a returning different user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-18-01 | I | Residual data after sign-out | mitigate | performSignOut wipes session, secure keys, every fincwin: key and registered queues; runs even when offline (D-15) |
| T-00-18-02 | I | Analytics identity carried to the next user | mitigate | analytics.disable() (optOut + reset) before signOut; consent is re-read from the next user's profile |
| T-00-18-03 | R | Proof of consent | mitigate | analytics_consent + analytics_consent_at stored server-side per user |
| T-00-18-04 | T | Tampering with other users' profiles | mitigate | RLS own-row update + column grants (00-06); client sends only changed allowed columns |
| T-00-18-05 | R | Dark-pattern consent | mitigate | Equal-size, equal-position buttons, no pre-selection, changeable in settings (D-17); component test asserts equal dimensions |
</threat_model>

<verification>
- `npx jest` full suite, tsc and lint pass
</verification>

<success_criteria>
D-13, D-14, D-15 and D-17 are implemented and unit-proven; device acceptance runs in 00-19.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-18-SUMMARY.md`
</output>
