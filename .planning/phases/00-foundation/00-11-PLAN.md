---
phase: 00-foundation
plan: 11
type: execute
wave: 3
depends_on: ["00-05"]
files_modified:
  - src/theme/tokens.ts
  - src/theme/accents.ts
  - src/theme/fonts.ts
  - src/theme/typography.ts
  - src/theme/layout.ts
  - src/theme/motion.ts
  - src/theme/themeCache.ts
  - src/theme/ThemeProvider.tsx
  - src/theme/index.ts
  - src/ui/Screen.tsx
  - src/theme/__tests__/themeSwitch.test.tsx
  - src/theme/__tests__/reducedMotion.test.tsx
  - src/theme/__tests__/noRawColours.test.ts
  - src/theme/__tests__/screenInsets.test.tsx
  - docs/design/token-exceptions.md
  - app/index.tsx
autonomous: true
requirements: [FND-06, FND-07, DSG-02, DSG-03]

must_haves:
  truths:
    - "Switching among the 4 accents and 4 font pairings re-renders consumers live with no reload (FND-06)"
    - "The last chosen accent and pairing are cached locally and applied before the splash hides, so there is no flash of the default green (D-14 local half)"
    - "All animation durations collapse to 0 when the OS reports reduce-motion, including live changes mid-session (FND-07)"
    - "No hex/rgb colour literal exists outside src/theme/tokens.ts and the D-12 brand-mark folder; a test enforces this (DSG-02)"
    - "Screen top padding is insets.top + 21 and never a hardcoded 68 or 874 (DSG-03)"
    - "The Apple/Google brand-mark exception to the token palette is documented (D-12)"
  artifacts:
    - path: "src/theme/tokens.ts"
      provides: "The complete BUILD-PROMPT §2 palette, category COL/TINT maps, member colours, shadows"
      contains: "#FBFAF7"
    - path: "src/theme/ThemeProvider.tsx"
      provides: "Theme context: accent, pairing, setters, resolved colours/fonts, ready flag"
      exports: ["ThemeProvider", "useTheme"]
    - path: "src/theme/motion.ts"
      provides: "Easing, durations, useMotion() honouring reduce-motion"
      exports: ["useMotion", "EASING", "DURATION"]
    - path: "src/ui/Screen.tsx"
      provides: "Safe-area-aware screen container"
      exports: ["Screen"]
    - path: "docs/design/token-exceptions.md"
      provides: "D-12 brand-mark exception record"
  key_links:
    - from: "src/theme/ThemeProvider.tsx"
      to: "src/theme/themeCache.ts"
      via: "hydrate on mount, persist on change"
      pattern: "fincwin:theme"
    - from: "src/theme/motion.ts"
      to: "AccessibilityInfo reduceMotionChanged"
      via: "event listener"
      pattern: "reduceMotionChanged"
---

<objective>
Build the design-token theme system: the full palette, 4 accents, 4 font pairings, type scale, radii/shadows, safe-area layout, reduce-motion-aware motion, and a live-switching provider with a local cache.

Purpose: FND-06, FND-07, DSG-02 and DSG-03 are standards every later screen inherits. The design-fidelity constraint says the §2 token set is complete and closed.
Output: The src/theme module, src/ui/Screen, four test files, and the token-exception doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@BUILD-PROMPT.md
@.planning/phases/00-foundation/00-UI-SPEC.md
@.planning/phases/00-foundation/00-CONTEXT.md
@.planning/phases/00-foundation/00-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static tokens: palette, accents, font pairings, type scale, layout</name>
  <files>src/theme/tokens.ts, src/theme/accents.ts, src/theme/fonts.ts, src/theme/typography.ts, src/theme/layout.ts, src/theme/index.ts</files>
  <read_first>
    - BUILD-PROMPT.md §2 (lines 25-90: colour tokens, accents, typography, shape/depth/motion, layout)
    - FincWin United.dc.html lines 3217-3218 (Component.COL / Component.TINT maps) and 3359-3364 (Component.FONTS). Read those exact lines only.
    - .planning/phases/00-foundation/00-UI-SPEC.md (Spacing, Typography, Color tables)
    - node_modules/@expo-google-fonts/*/index.d.ts (exact exported font names and available weights)
  </read_first>
  <action>
1. `tokens.ts`: `export const colors = { surface: '#FFFFFF', canvas: '#FBFAF7', shell: '#E7E4DC', ink: '#14150F', inkMuted: '#6E6A5E', inkFaint: '#767161', inkDim: '#5C5A50', inkSoft1: '#A8A79C', inkSoft2: '#BFBEB4', inkSoft3: '#C6C2B6', accentTint1: '#EAF1EC', accentTint2: '#E9F0EB', accentTint3: '#F4F7F4', danger: '#B4472A', dangerTint1: '#F6EAE6', dangerTint2: '#F3E6E1', dangerTint3: '#F6EAE5', warn1: '#8A5A1B', warn2: '#7E6020', line1: '#EDEAE1', line2: '#E2DED2', line3: '#E5E2D7', line4: '#D9D5C9', fill1: '#F1EFE8', fill2: '#F2EFE7', fill3: '#F5F3ED', fill4: '#F4F2EB', fill5: '#FAF8F3', fill6: '#F6F4EE', tabBar: 'rgba(251,250,247,.94)' } as const;`, then `categoryColor` and `categoryTint`, copied verbatim from HTML lines 3217-3218 (15 keys each, including Income, Settlement, Transfer), `memberColors = ['#1B4D3E','#B4472A','#3E5C6B','#7E6020','#6E4A63','#2F6E68'] as const`, `memberColorNames = ['Green','Rust','Slate','Ochre','Plum','Teal'] as const`, and `shadowColor = '#14150F'`. Write the shadows as RN style objects approximating §2 (`card`: 0 1px 2px .05 plus 0 12px 30px -18px .22 → `{ shadowColor, shadowOpacity: 0.12, shadowRadius: 15, shadowOffset: { width: 0, height: 6 }, elevation: 3 }`, documented as the nearest RN equivalent), and do the same for `fab`. No colour is added beyond §2 and the HTML maps.
2. `accents.ts`: `export const ACCENTS = { green: '#1B4D3E', navy: '#1F3A5F', rust: '#7A4B2A', slate: '#3E5C6B' } as const; export type AccentKey = keyof typeof ACCENTS; export const DEFAULT_ACCENT: AccentKey = 'green'; export const ACCENT_KEYS = Object.keys(ACCENTS) as AccentKey[];`. The keys match the profiles.accent check constraint (00-06).
3. `fonts.ts`: `export type FontPairingKey = 'bold' | 'modern' | 'grotesk' | 'neutral'` (matches profiles.font_pairing). `FONT_PAIRINGS: Record<FontPairingKey, { label: 'Bold'|'Modern'|'Grotesk'|'Neutral'; display: string; body: Record<400|500|600|700|800, string> }>`: bold is display `ArchivoBlack_400Regular` with body Archivo 400/500/600/700/800; modern is display `Manrope_800ExtraBold` with body Manrope 400-800; grotesk is display `SpaceGrotesk_700Bold` with body Space Grotesk 400-700, 800 mapped to 700; neutral is display `IBMPlexSans_700Bold` with body IBM Plex Sans 400-700, 800 mapped to 700. Use the exact export names from the installed packages. `export function useThemeFonts(): [loaded: boolean, error: Error | null]` loads every face of all four pairings at boot through `useFonts` from expo-font. Claude's Discretion choice: load all at boot so a switch is instant, with no async gap and no reload. Record this choice in a comment.
4. `typography.ts`: `export const fontSize = { tabLabel: 10.5, eyebrow: 11.5, meta: 12.5, label: 13, s13_5: 13.5, body: 15, sheetTitle: 16, heading: 26, healthScore: 32, netWorth: 42, display: 34 } as const;`, plus role builders `textRole(pairing, role)` returning `{ fontFamily, fontSize, lineHeight, letterSpacing }` for the UI-SPEC roles: display 34 / lh 1.0 / display face; heading 26 / 700 / lh 1.16 / ls -0.01em; body 15 / 500 / lh 1.5; label 13 / 600 / lh 1.4. Convert em letter-spacing as `size * em`.
5. `layout.ts`: `radii = { card: 26, pill: 999, glyphTile: 11 }`, `space = { screenH: 22, cardPad: 16, headerExtra: 21, sheetPadBottom: 30, sheetCornerPad: 22, groupGap: 18, rowPad: 15, gapSm: 8, gapMd: 12, pillPadV: 15, touchMin: 44 }`, and `export function useScreenInsets() { const i = useSafeAreaInsets(); return { headerTop: i.top + space.headerExtra, bottom: i.bottom, left: i.left, right: i.right }; }`.
6. `index.ts` re-exports everything.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "'#1F3A5F'" src/theme/accents.ts` and the other three accent hexes are present
    - `grep -c "#" src/theme/tokens.ts` is at least 40 (full palette + maps + member colours)
    - `grep -q "i.top + space.headerExtra" src/theme/layout.ts` succeeds
    - `grep -q "ArchivoBlack_400Regular" src/theme/fonts.ts` succeeds
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Every §2 token is typed and importable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ThemeProvider with live switching and local cache, motion with reduce-motion, Screen container</name>
  <files>src/theme/themeCache.ts, src/theme/ThemeProvider.tsx, src/theme/motion.ts, src/ui/Screen.tsx, src/theme/__tests__/themeSwitch.test.tsx, src/theme/__tests__/reducedMotion.test.tsx, src/theme/__tests__/screenInsets.test.tsx</files>
  <read_first>
    - src/theme/accents.ts, src/theme/fonts.ts, src/theme/layout.ts
    - .planning/phases/00-foundation/00-RESEARCH.md (Pattern 6: ReducedMotionConfig vs useReducedMotion)
    - .planning/phases/00-foundation/00-CONTEXT.md (D-14)
  </read_first>
  <behavior>
    - themeSwitch: a consumer rendering `theme.colors.accent` shows '#1B4D3E' by default; after `setAccent('navy')` inside act() the same mounted consumer shows '#1F3A5F' without remounting (a render-count ref stays on the same instance); `setPairing('grotesk')` changes `theme.fonts.display` to the SpaceGrotesk family; the choice is written to AsyncStorage key 'fincwin:theme' as {accent, pairing}
    - hydrate: with 'fincwin:theme' pre-set to {"accent":"rust","pairing":"modern"}, `ready` flips true and the first ready render already shows rust/modern; corrupt JSON or unknown keys fall back to green/bold
    - applyRemote(accent, pairing) (used by 00-18 for the profile row) updates the theme and the cache
    - reducedMotion: with AccessibilityInfo.isReduceMotionEnabled resolving true, useMotion().duration(220) === 0; with false it returns 220; firing the 'reduceMotionChanged' listener with true flips it to 0 live
    - screenInsets: Screen with mocked insets {top: 47} renders a container whose paddingTop is 68
  </behavior>
  <action>
1. `themeCache.ts`: `THEME_CACHE_KEY = 'fincwin:theme'`, `readThemeCache(): Promise<{accent: AccentKey; pairing: FontPairingKey}>`, validated with fallback to defaults, and `writeThemeCache(v)`. The `fincwin:` prefix means sign-out wipes it (D-15).
2. `ThemeProvider.tsx`: context value `{ ready: boolean; accent: AccentKey; pairing: FontPairingKey; colors: typeof colors & { accent: string }; fonts: { display: string; body: Record<400|500|600|700|800,string> }; setAccent(k): void; setPairing(k): void; applyRemote(accent, pairing): void; reset(): void }`. Keep state in `useState`; memoise the value; setters update state synchronously, then persist. `reset()` returns to defaults (used after sign-out). `useTheme()` throws outside the provider. The provider does not load fonts. The root layout (00-17) combines `ready` with `useThemeFonts()` before hiding the splash.
3. `motion.ts`: `EASING = Easing.bezier(0.2, 0.8, 0.3, 1)` from react-native-reanimated, `DURATION = { entrance: 220, progress: 550, fade: 200 }`, `useReduceMotion(): boolean` (initial `AccessibilityInfo.isReduceMotionEnabled()` plus the `reduceMotionChanged` subscription, cleaned up on unmount), and `useMotion() => { reduced, duration: (ms: number) => reduced ? 0 : ms, easing: EASING }`. Add a doc comment: the root layout also mounts `<ReducedMotionConfig mode={ReduceMotion.System} />` (00-17), so Reanimated worklets collapse globally. `useMotion` covers any duration computed in JS.
4. `src/ui/Screen.tsx`: `Screen({ children, scroll?: boolean, style? })`, a View (or ScrollView) with `backgroundColor: theme.colors.canvas`, `paddingTop: headerTop`, `paddingBottom: bottom`, `paddingHorizontal: space.screenH`.
5. Tests: wrap in `SafeAreaProvider initialMetrics` or mock `react-native-safe-area-context` per its jest docs, and mock AccessibilityInfo with jest.spyOn. Do not edit jest.setup.ts.
  </action>
  <verify>
    <automated>npx jest src/theme</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/theme/__tests__/themeSwitch.test.tsx src/theme/__tests__/reducedMotion.test.tsx src/theme/__tests__/screenInsets.test.tsx` passes
    - `grep -q "fincwin:theme" src/theme/themeCache.ts` succeeds
    - `grep -q "reduceMotionChanged" src/theme/motion.ts` succeeds
    - `grep -rnE "\b(68|874)\b" src/ui/Screen.tsx src/theme/layout.ts` returns nothing
  </acceptance_criteria>
  <done>FND-06 and FND-07 are proven by component tests; DSG-03 is enforced by Screen.</done>
</task>

<task type="auto">
  <name>Task 3: DSG-02 raw-colour guard test and the D-12 exception record</name>
  <files>src/theme/__tests__/noRawColours.test.ts, docs/design/token-exceptions.md, app/index.tsx</files>
  <read_first>
    - .planning/phases/00-foundation/00-UI-SPEC.md ("Token exception", Platform-Specific Components)
    - src/theme/tokens.ts
  </read_first>
  <action>
1. `noRawColours.test.ts`: recursively read every `.ts`/`.tsx` under `src/` and `app/` (skipping `__tests__`). Fail if any file other than `src/theme/tokens.ts`, `src/theme/accents.ts` or anything under `src/features/auth/brand/` contains `/#[0-9a-fA-F]{3,8}\b/` or `/rgba?\(/`. The failure message lists file:line. A second test asserts that every hex in tokens.ts/accents.ts is in an allow-list array written in the test, copied from BUILD-PROMPT §2 plus the HTML COL/TINT maps and member colours. This means adding a new colour needs a deliberate edit in two places.
2. The scaffold boot screen `app/index.tsx` (00-02) hardcodes `#FBFAF7`. Change it to `colors.canvas` from `@/theme` so the guard passes. Plan 00-17 rewrites the file later.
3. `docs/design/token-exceptions.md`: "DSG-02 exception (D-12): the official Sign in with Apple button (Apple's black, Apple logo) and the Google 'G' mark (Google's four brand colours) render per Apple HIG and Google branding guidelines, not the FincWin palette. They live only in `src/features/auth/brand/` (the one folder the raw-colour test skips) and the native AppleAuthenticationButton. No other exception exists. Adding one needs a new CONTEXT decision."
  </action>
  <verify>
    <automated>npx jest src/theme/__tests__/noRawColours.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/theme/__tests__/noRawColours.test.ts` passes on the current tree
    - Adding `const x = '#123456'` to any file in src/features makes the test fail. Check this once by hand, then revert
    - `grep -q "src/features/auth/brand/" docs/design/token-exceptions.md` succeeds
  </acceptance_criteria>
  <done>DSG-02 is enforced automatically, and the only exception is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage theme cache -> UI | Locally stored, user-modifiable preference |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-11-01 | T | fincwin:theme cache | mitigate | Parsed and validated against ACCENT_KEYS / FontPairingKey; invalid → defaults, never crashes |
| T-00-11-02 | I | theme cache contents | accept | Holds only two enum values, no personal data; wiped on sign-out through the fincwin: prefix |
</threat_model>

<verification>
- `npx jest src/theme` green; tsc and lint clean
</verification>

<success_criteria>
FND-06, FND-07, DSG-02 and DSG-03 are implemented and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-11-SUMMARY.md`
</output>
