---
phase: 00-foundation
plan: 07
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - docs/enrolment/website-audit.md
  - docs/dependency-register.md
  - docs/enrolment/apple-org-checklist.md
autonomous: false
requirements: [FND-08, ENV-10, ENV-20, ENV-15, ENV-09]
user_setup:
  - service: apple-developer
    why: "Organisation enrolment (FND-08, ENV-10); $99/yr"
    dashboard_config:
      - task: "Enrol as an organisation with the company's D-U-N-S"
        location: "https://developer.apple.com/programs/enroll/"
  - service: google-play-console
    why: "Organisation developer account pulled forward to Phase 0 (D-04); $25 once"
    dashboard_config:
      - task: "Create an organisation developer account with the same D-U-N-S"
        location: "https://play.google.com/console/signup"

must_haves:
  truths:
    - "The existing company website has been audited against Apple's organisation-enrolment bar, with a written pass/fail per criterion and remediation copy for every gap (D-06, ENV-20)"
    - "Apple Developer Program enrolment is submitted as an organisation, or its exact blocker (D-U-N-S pending with ETA) is recorded; there is no individual-account fallback (FND-08, D-03)"
    - "A Google Play Console organisation account is registered with the same D-U-N-S, or its blocker is recorded (D-04)"
    - "The dependency register reflects the submission dates and statuses (ENV-09)"
  artifacts:
    - path: "docs/enrolment/website-audit.md"
      provides: "Criterion-by-criterion audit of the company site plus remediation copy"
      contains: "| Criterion |"
  key_links:
    - from: "docs/enrolment/website-audit.md"
      to: "docs/dependency-register.md Company website row"
      via: "status updated from audit outcome"
      pattern: "Company website"
---

<objective>
Audit the company website against Apple's bar, then submit Apple organisation enrolment and register the Google Play Console organisation account.

Purpose: FND-08 requires enrolment submitted before any store-dependent work begins. Apple rejects parked or placeholder sites (ENV-20, D-06). D-04 pulls Play registration into Phase 0. iOS work waits on the outcome (D-03). Android does not.
Output: website-audit.md, and the register updated with enrolment status and dates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-foundation/00-CONTEXT.md
@.planning/phases/00-foundation/00-01-SUMMARY.md
@docs/dependency-register.md
@docs/enrolment/apple-org-checklist.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit the company website against Apple's organisation-enrolment criteria</name>
  <files>docs/enrolment/website-audit.md</files>
  <read_first>
    - docs/dependency-register.md (Domain row: the domain name recorded by 00-01)
    - docs/enrolment/apple-org-checklist.md
    - .planning/phases/00-foundation/00-01-SUMMARY.md (website URL, work email)
  </read_first>
  <action>
1. Fetch the site (WebFetch on the homepage, plus `/about`, `/contact`, `/privacy` if linked) and `curl -sI https://<domain>`, which should return 200 over HTTPS with a valid certificate.
2. Write `docs/enrolment/website-audit.md` with a table `| Criterion | Result (pass/fail/partial) | Evidence | Remediation |` covering:
   - Site loads over HTTPS on the company's own domain (not a registrar parking page, not "coming soon")
   - The legal company name appears and matches the D-U-N-S/registration name (D-02)
   - Real content describing the company and what it does, including the FincWin app as a personal money-management tool, with no claims of being a bank, lender or adviser (CLAUDE.md compliance voice)
   - A contact route: a contact email on the domain (the work email from 00-01) and/or a contact form
   - A physical or registered address, or at least country of registration. Apple does not strictly require it, but it strengthens verification
   - No broken primary navigation and no lorem ipsum
   - Room for future privacy/terms/support pages (same site, later phases). Note what exists now
3. For every fail or partial row, draft the replacement copy in the Remediation column or in a "Drafted copy" section beneath: an About paragraph and a Contact block in the prototype's voice (declarative, plain, British-ish spelling, typographic apostrophes). The copy must not use "advice", "recommendation" or "you should", and must not claim data stays on the device.
4. Overall verdict line: `Verdict: READY` or `Verdict: REMEDIATE (n items)`.
  </action>
  <verify>
    <automated>grep -q "| Criterion |" docs/enrolment/website-audit.md && grep -qE "^Verdict: (READY|REMEDIATE)" docs/enrolment/website-audit.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^| " docs/enrolment/website-audit.md` returns at least 8
    - `grep -qE "^Verdict: (READY|REMEDIATE)" docs/enrolment/website-audit.md` succeeds
    - `grep -iE "you should|recommendation|financial advice from|stays? on (this|your) device" docs/enrolment/website-audit.md` returns nothing inside the drafted copy
  </acceptance_criteria>
  <done>A concrete audit with verdict and ready-to-paste remediation copy exists.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Remediate the site if needed, submit Apple organisation enrolment, register Google Play Console</name>
  <read_first>
    - docs/enrolment/website-audit.md
    - docs/enrolment/apple-org-checklist.md
  </read_first>
  <action>Human-only steps: legal agreements, payments and identity verification with Apple and Google.</action>
  <what-built>Claude audited the website and drafted remediation copy in docs/enrolment/website-audit.md.</what-built>
  <how-to-verify>
    1. If the verdict is REMEDIATE, publish the drafted copy on the site, then reply "site updated" so the audit can be re-run before enrolling.
    2. Apple (organisation only, D-03): at https://developer.apple.com/programs/enroll/, sign in with an Apple ID on the work email, choose Organization, and enter the legal name exactly as on D&B, the D-U-N-S, the website URL and the work email. Accept the Program License Agreement as owner/director (D-02) and pay $99 when prompted. If D-U-N-S is still pending, stop here and report "Apple blocked: D-U-N-S pending".
    3. Google Play (D-04): at https://play.google.com/console/signup, choose Organisation and use the same D-U-N-S, legal name and website. Pay $25 and complete identity/organisation verification.
    4. Reply with: the Apple status (submitted with date and enrolment ID, or blocked with reason); the Play status (registered or verification pending, with date, or blocked); and any follow-up Apple has requested, such as a verification call.
  </how-to-verify>
  <acceptance_criteria>
    - Reply states an Apple status and a Play status, each with a date or a named blocker
  </acceptance_criteria>
  <resume-signal>Reply with the Apple and Play statuses</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record enrolment outcomes in the register and checklist</name>
  <files>docs/dependency-register.md, docs/enrolment/apple-org-checklist.md, docs/enrolment/website-audit.md</files>
  <read_first>
    - docs/dependency-register.md
    - docs/enrolment/apple-org-checklist.md
  </read_first>
  <action>
1. If the user reported "site updated", re-run Task 1's fetch checks and update the audit table and verdict before anything else.
2. Register rows, each with Last checked = today:
   - Company website: `provisioned` if READY, otherwise `pending` with the remaining items.
   - Apple Developer Program: `pending` with "submitted YYYY-MM-DD, enrolment ID <id>, awaiting Apple review" (or the blocker). Must land by: "Phase 0 submission done; ENV-10 active gates 00-20 and Phase 11".
   - Google Play Console: `pending` or `provisioned` with date. Must land by: "Phase 0 (D-04)".
   - Sign in with Apple row stays `deferred` with blocker "ENV-10 active".
3. Tick "Enrolment submitted" in apple-org-checklist.md with the date. If submission was blocked, add a line "Resume when D-U-N-S issued (ETA YYYY-MM-DD)".
Never record payment details or Apple ID passwords.
  </action>
  <verify>
    <automated>grep -E "^\| Apple Developer Program" docs/dependency-register.md | grep -qE "20[0-9]{2}-[0-9]{2}-[0-9]{2}" && grep -E "^\| Google Play Console" docs/dependency-register.md | grep -qE "20[0-9]{2}-[0-9]{2}-[0-9]{2}"</automated>
  </verify>
  <acceptance_criteria>
    - Apple Developer Program row contains "submitted" with a date, or a named blocker with an ETA
    - Google Play Console row contains a status and date
  </acceptance_criteria>
  <done>FND-08 is either satisfied (submitted) or its external blocker is explicit and dated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public website | First impression for Apple/Google reviewers and users |
| user -> Apple/Google consoles | Legal and payment actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-07-01 | R | Website claims | mitigate | Drafted copy avoids advice/lender/bank claims and on-device-storage claims (compliance voice) |
| T-00-07-02 | I | docs/enrolment/* | mitigate | Enrolment ID and dates only; no payment details, passwords or personal identifiers |
| T-00-07-03 | S | Enrolling as the wrong entity type | mitigate | Organisation only, no individual fallback (D-03); legal name copied from the D&B record |
</threat_model>

<verification>
- website-audit.md has a verdict; the register shows Apple and Play statuses with dates
</verification>

<success_criteria>
Apple enrolment is submitted (or blocked with a dated reason), Play is registered, and the site is audited.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-07-SUMMARY.md`
</output>
