---
phase: 00-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/dependency-register.md
  - docs/enrolment/apple-org-checklist.md
  - docs/enrolment/email-setup.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [ENV-09, ENV-15, ENV-20, FND-08]
user_setup:
  - service: dnb-duns
    why: "D-U-N-S number gates Apple and Google organisation enrolment (ENV-15)"
    dashboard_config:
      - task: "Look up the company's D-U-N-S; request one if missing"
        location: "https://developer.apple.com/enroll/duns-lookup/"
  - service: cloudflare
    why: "Inbound work email via Email Routing; DNS records for Resend (D-05, D-07)"
    dashboard_config:
      - task: "Enable Email Routing for the company domain and create a forwarding address"
        location: "Cloudflare Dashboard -> <domain> -> Email -> Email Routing"
  - service: resend
    why: "Outbound mail from the company domain (D-07)"
    dashboard_config:
      - task: "Create a Resend team under the company, add the domain, copy the SPF/DKIM records into Cloudflare DNS, add a DMARC record"
        location: "https://resend.com/domains"

must_haves:
  truths:
    - "A dependency register exists that lists every external service as provisioned, pending or deferred, with its blocker and the phase it must land by (ENV-09)"
    - "The company's D-U-N-S status is known on day one and either confirmed or requested, with the date recorded (ENV-15, D-01)"
    - "The D&B record has been checked against the government registration and the legal name matches the intended seller name (D-02)"
    - "A work email on the company domain receives mail (Cloudflare Email Routing inbound), with outbound via Resend documented including the send-as caveat (D-07, ENV-20)"
    - "ROADMAP.md's dependency table shows Google Play Console registered in Phase 0, not Phase 9 (D-04)"
    - "The default branch is main and origin/main exists (D-09)"
    - "Email and domain DNS changes are made in Cloudflare, where the already-owned company domain's DNS lives; no domain purchase or DNS migration is planned (D-05)"
    - "Every service account in the register is owned by a company organisation/team created inside the service under the user's personal login, and the register's owner column says so (D-08)"
  artifacts:
    - path: "docs/dependency-register.md"
      provides: "Living status register for every external dependency"
      contains: "| Service |"
    - path: "docs/enrolment/apple-org-checklist.md"
      provides: "Apple organisation enrolment prerequisites and D-02 verification record"
    - path: "docs/enrolment/email-setup.md"
      provides: "Work email setup record, including the reply-as-domain caveat"
  key_links:
    - from: "docs/dependency-register.md"
      to: ".planning/ROADMAP.md dependency provisioning table"
      via: "same service rows; Play Console row reflects D-04"
      pattern: "Google Play Console"
---

<objective>
Start every external clock on day one and create the dependency register that later plans update.

Purpose: D-U-N-S (~28 days if missing) is the first link in the longest chain in the project (D-01). Apple organisation enrolment (FND-08, ENV-10) also needs a real website and a work email on the company domain (ENV-20). None of this blocks Android work, but all of it has to be moving before any code lands.
Output: docs/dependency-register.md, docs/enrolment/apple-org-checklist.md, docs/enrolment/email-setup.md, the ROADMAP.md Play Console row updated per D-04, and D-U-N-S/domain/email status recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-foundation/00-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the dependency register, Apple enrolment checklist, and update the ROADMAP Play Console row</name>
  <files>docs/dependency-register.md, docs/enrolment/apple-org-checklist.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (section "Dependency provisioning — what blocks on what" under Phase 0)
    - .planning/phases/00-foundation/00-CONTEXT.md (D-01 to D-09)
    - .planning/PROJECT.md (section "Running Costs")
  </read_first>
  <action>
Create `docs/dependency-register.md` (ENV-09, format chosen under Claude's Discretion: one Markdown table, git-tracked, updated by plans 00-07, 00-19 and 00-20). Header text: "Every external service FincWin depends on. Status is one of provisioned / pending / deferred. Update the row and the Last checked date whenever status changes." Table columns exactly: `| Service | Needed for | Status | Account / org | Blocker | Must land by | Last checked | Notes |`. One row per item, initial values:
- D-U-N-S number | Apple + Google org enrolment | pending | company | none yet: lookup not done | Phase 0 | 2026-09-22 | Check via Apple's lookup tool on day one; ~28 days if a new one must be issued (D-01)
- Domain | website, email, Apple enrolment | provisioned | company, DNS on Cloudflare | — | Phase 0 | 2026-09-22 | Already owned (D-05); domain name recorded in Task 3
- Company website | Apple org enrolment; later hosts privacy/terms/support | pending | company | Not yet audited against Apple's bar | Phase 0 | 2026-09-22 | Audit in plan 00-07 (D-06)
- Work email (inbound: Cloudflare Email Routing) | Apple verification mail | pending | company | Not yet set up | Phase 0 | 2026-09-22 | D-07
- Work email (outbound: Resend) | replying as the domain, later transactional mail | pending | company team in Resend | Not yet set up | Phase 0 | 2026-09-22 | D-07; SPF/DKIM/DMARC in Cloudflare
- Apple Developer Program (organisation) | Sign in with Apple, iOS device builds, TestFlight, submission | pending | company (enrol as organisation only, D-03) | D-U-N-S, website, work email | Phase 0 submission; Phase 11 hard gate | 2026-09-22 | $99/yr. No individual fallback (D-03)
- Google Play Console (organisation) | Play submission, IAP products | pending | company | D-U-N-S | Phase 0 (pulled forward from Phase 9 per D-04) | 2026-09-22 | $25 one-off
- GitHub repo | CI, source of truth | provisioned | paulakintunde/AppFincWin | — | Phase 0 | 2026-09-22 | Default branch main (D-09)
- Supabase dev project | on-device testing | pending | company org in Supabase (D-08) | — | Phase 0 | 2026-09-22 | Region US East (D-22); plan 00-03
- Supabase prod project | real user data | pending | company org in Supabase | — | Phase 0 (Free), Pro by Phase 2 (ENV-16) | 2026-09-22 | plan 00-03
- EAS project + profiles | all builds, OTA | pending | company org in Expo (D-08) | — | Phase 0 | 2026-09-22 | plan 00-14
- Google OAuth client IDs (Web/Android/iOS) | Google Sign-In | pending | company org in Google Cloud | Android SHA-1s need the app identifier (00-02) and EAS keystore (00-14) | Phase 0 | 2026-09-22 | plan 00-15
- Sign in with Apple (Services ID + key) | Apple sign-in on iOS and Android | deferred | Apple org account | Apple Developer Program (ENV-10) | when enrolment clears | 2026-09-22 | plan 00-20; client secret JWT expires every 6 months
- Frankfurter v2 | FX rates | pending | none (no key) | — | Phase 0 | 2026-09-22 | Pin v2 (`https://api.frankfurter.dev/v2`); plan 00-09
- open.er-api | FX fallback | deferred | none (no key) | — | Phase 1 (MON-12) | 2026-09-22 | Attribution required in-app
- PostHog (EU host) | product analytics | pending | company org in PostHog | — | Phase 0 | 2026-09-22 | EU host kept despite US DB (D-23); plan 00-13
- Sentry | error reporting | pending | — | D-19 spike outcome | Phase 0 decision | 2026-09-22 | Dropped if PostHog error tracking holds up (D-19); plan 00-16
- APNs + FCM push credentials | server-sent alerts | deferred | Apple org / Firebase | APNs waits on ENV-10 | Phase 10 (ENV-19) | 2026-09-22 |
- RevenueCat | subscriptions | deferred | company | Apple + Play accounts | Phase 9 | 2026-09-22 |
- Cloudflare | DNS, inbound email | provisioned | company | — | Phase 0 | 2026-09-22 | D-05
- App identifier (bundle ID / Android package) | every store registration, Google OAuth clients | pending | — | decision in plan 00-02 | Phase 0 | 2026-09-22 | Cannot be changed after the first store registration

Create `docs/enrolment/apple-org-checklist.md` listing Apple's organisation-enrolment prerequisites as checkboxes: legal entity status (D-01: registered); D-U-N-S number assigned to the legal entity; D&B record legal name and address match the government registration exactly (D-02: verify, record the date checked); registered legal name matches desired App Store seller name, no DBA needed (D-02); enrolling person is owner/director with authority to accept the Program License Agreement (D-02); public website on the company domain with real company content and a contact route (ENV-20, D-06); work email on the company domain that receives mail (D-07); Apple ID for enrolment uses the work email; organisation only, no individual fallback (D-03). Include the enrolment URL `https://developer.apple.com/programs/enroll/` and the lookup URL `https://developer.apple.com/enroll/duns-lookup/`.

Edit `.planning/ROADMAP.md`: in the Phase 0 "Dependency provisioning" table, change the Google Play Console row's "Defer condition" cell to: "**Register in Phase 0** (D-04) using the same D-U-N-S as Apple. Products and first upload still land by Phase 9/11." Change no other row.

Verify D-09 and record the result in the register's GitHub row notes. First, if `git branch --show-current` does not print `main`, run `git branch -m main` (safe: local rename only, no push). Then run `git branch --show-current` (expect `main`) and `git ls-remote --heads origin main` (expect one line). If either check fails, note it in the row and do not push. Pushing needs user approval under the workspace rules.
  </action>
  <verify>
    <automated>test -f docs/dependency-register.md && grep -c "^| " docs/dependency-register.md && grep -q "Register in Phase 0" .planning/ROADMAP.md && git branch --show-current</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^| " docs/dependency-register.md` returns at least 23 (header + separator + 21 rows)
    - `grep -q "| Service | Needed for | Status | Account / org | Blocker | Must land by | Last checked | Notes |" docs/dependency-register.md` succeeds
    - `grep -q "D-U-N-S number" docs/dependency-register.md` and `grep -q "Google Play Console" docs/dependency-register.md` succeed
    - `grep -q "Register in Phase 0" .planning/ROADMAP.md` succeeds
    - `grep -q "duns-lookup" docs/enrolment/apple-org-checklist.md` succeeds
    - `git branch --show-current` prints `main`
  </acceptance_criteria>
  <done>Register and checklist exist; ROADMAP Play row reflects D-04; branch state is recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Look up the D-U-N-S number and set up work email on the company domain</name>
  <read_first>
    - docs/enrolment/apple-org-checklist.md
    - .planning/phases/00-foundation/00-CONTEXT.md (D-01, D-02, D-05, D-07)
  </read_first>
  <action>Human-only steps. D&B lookups, Cloudflare dashboard changes and Resend account creation need the user's own logins (D-08: personal login, company org inside each service).</action>
  <what-built>Claude created the dependency register and the Apple organisation checklist. These steps have no API route Claude can use without the user's credentials.</what-built>
  <how-to-verify>
    1. Open https://developer.apple.com/enroll/duns-lookup/ and search for the company's exact registered legal name and address.
       - If a D-U-N-S is found: note the number, and check that the D&B legal name and address match the government registration exactly (D-02). If they differ, request a correction from D&B now, because it restarts the clock.
       - If not found: submit the request through the same tool and note the submission date. Allow up to ~28 days.
    2. In Cloudflare (DNS is already there, D-05): Email -> Email Routing -> enable for the domain -> create an address such as `hello@<domain>` forwarding to your inbox. Send a test mail from another account and confirm it arrives.
    3. In Resend: create a team for the company (D-08), add the domain, and add the SPF and DKIM records it shows into Cloudflare DNS. Also add `_dmarc` TXT `v=DMARC1; p=none; rua=mailto:hello@<domain>`. Wait for Resend to show the domain as Verified.
    4. Reply here with: the domain, the website URL, the work email address, the D-U-N-S number or request date, whether the D&B record matches, and whether Resend shows Verified.
  </how-to-verify>
  <acceptance_criteria>
    - The user's reply includes the domain, website URL, work email address and D-U-N-S status (number or request date)
  </acceptance_criteria>
  <resume-signal>Reply with the six items above, or describe what is blocked</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record D-U-N-S, domain and email status in the register and email-setup doc</name>
  <files>docs/dependency-register.md, docs/enrolment/email-setup.md, docs/enrolment/apple-org-checklist.md</files>
  <read_first>
    - docs/dependency-register.md
    - docs/enrolment/apple-org-checklist.md
  </read_first>
  <action>
Using the values from Task 2's resume message:
- Register: set the D-U-N-S row Status to `provisioned` with the number in Notes (or keep it `pending` with "requested YYYY-MM-DD, ETA YYYY-MM-DD (+28 days)"). Set the Domain row Notes to the domain name. Set the inbound and outbound email rows to `provisioned` (or `pending` with the specific blocker). Set Last checked to today's date on every row you touch.
- Checklist: tick every satisfied item and add "Checked YYYY-MM-DD" next to the D-02 D&B match item.
- Create `docs/enrolment/email-setup.md` recording: the inbound address and its forwarding target (name the target as "owner's personal inbox" and do not write the personal address); the Resend domain status; the DNS records added (record types and names only, never values); and the D-07 caveat verbatim: "Email Routing only forwards. To reply as the domain address, configure Resend SMTP (smtp.resend.com, port 465, user `resend`, password = a Resend API key) in the mail client, or use Gmail 'Send mail as' with the same SMTP settings. Apple's verification emails only need inbound."
Never write an API key, SMTP password or DKIM value into any tracked file.
  </action>
  <verify>
    <automated>grep -q "Email Routing only forwards" docs/enrolment/email-setup.md && grep -E "^\| D-U-N-S number" docs/dependency-register.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Email Routing only forwards" docs/enrolment/email-setup.md` succeeds
    - The D-U-N-S row in docs/dependency-register.md has a Last checked date and either a number or "requested" in Notes
    - `grep -iE "re_[A-Za-z0-9]{10,}|p=MIG" docs/enrolment/email-setup.md` returns nothing (no Resend key, no DKIM public-key value)
  </acceptance_criteria>
  <done>Register reflects real day-one status for D-U-N-S, domain and email; plan 00-07 can audit the website and submit enrolment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> public GitHub | Enrolment docs are committed and may become visible |
| user -> third-party consoles | Credentials entered by the user, never relayed through Claude |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01-01 | I | docs/enrolment/email-setup.md | mitigate | Record DNS record types and names only, never values; never write SMTP passwords or API keys; acceptance grep rejects `re_` keys |
| T-00-01-02 | I | docs/dependency-register.md | mitigate | Personal inbox address never written; forwarding target described as "owner's personal inbox" |
| T-00-01-03 | S | Work email domain | mitigate | SPF + DKIM + DMARC (`p=none` to start) published so the domain cannot be trivially spoofed |
| T-00-01-04 | R | D-U-N-S / D&B mismatch | accept | Human check recorded with date; a mismatch is fixed at D&B, not in code |
</threat_model>

<verification>
- docs/dependency-register.md lists every service in ROADMAP's dependency table plus D-U-N-S, domain, email, GitHub, Cloudflare and the app identifier
- ROADMAP.md Play Console row states Phase 0 registration (D-04)
- The D-U-N-S outcome is recorded with a date
</verification>

<success_criteria>
The D-U-N-S clock is running or already done, work email receives mail, and the register exists as the single status source for ENV-09.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-01-SUMMARY.md`
</output>
