---
phase: 00-foundation
plan: 12
type: execute
wave: 3
depends_on: ["00-05"]
files_modified:
  - src/i18n/locales/en.ts
  - src/i18n/index.ts
  - src/i18n/i18next.d.ts
  - src/i18n/copyStatus.ts
  - src/i18n/__tests__/catalogue.test.ts
  - src/i18n/__tests__/keys.typecheck.ts
autonomous: true
requirements: [DSG-04]

must_haves:
  truths:
    - "Every Phase 0 user-facing string lives in one typed catalogue; t() rejects unknown keys at compile time (DSG-04)"
    - "Welcome copy the user has not supplied ships as clearly marked placeholder keys, listed in AWAITING_COPY_KEYS (D-16)"
    - "Consent and settings copy is drafted in the prototype's voice, lists exactly what is never sent, and is marked awaiting user review (D-20)"
    - "No catalogue string uses straight apostrophes, 'advice', 'recommendation', 'you should', or any 'stays on this device' claim (CLAUDE.md compliance, D-15)"
  artifacts:
    - path: "src/i18n/locales/en.ts"
      provides: "English catalogue (as const)"
      contains: "auth.welcome"
    - path: "src/i18n/index.ts"
      provides: "i18next init + useT"
      exports: ["i18n", "useT"]
    - path: "src/i18n/i18next.d.ts"
      provides: "CustomTypeOptions module augmentation"
      contains: "CustomTypeOptions"
    - path: "src/i18n/copyStatus.ts"
      provides: "AWAITING_COPY_KEYS and DRAFT_COPY_KEYS"
      exports: ["AWAITING_COPY_KEYS", "DRAFT_COPY_KEYS"]
  key_links:
    - from: "src/i18n/i18next.d.ts"
      to: "src/i18n/locales/en.ts"
      via: "resources: { common: typeof en }"
      pattern: "typeof en"
---

<objective>
Create the typed i18n catalogue holding every Phase 0 string, with placeholder keys for user-supplied welcome copy and drafted consent/settings copy.

Purpose: DSG-04. The eslint no-literal-string rule from 00-05 forces screens to use this catalogue. D-16 and D-20 define the copy ownership.
Output: The src/i18n module with type augmentation and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-foundation/00-UI-SPEC.md
@.planning/phases/00-foundation/00-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalogue, i18next init and type augmentation</name>
  <files>src/i18n/locales/en.ts, src/i18n/index.ts, src/i18n/i18next.d.ts, src/i18n/copyStatus.ts</files>
  <read_first>
    - .planning/phases/00-foundation/00-UI-SPEC.md (Copywriting Contract table: copy these strings verbatim, converting apostrophes to ’)
    - node_modules/react-i18next/README.md and the i18next TypeScript docs (CustomTypeOptions shape for i18next 26)
  </read_first>
  <action>
1. `src/i18n/locales/en.ts`: `const en = { ... } as const; export default en;` with nested keys (keySeparator '.'). Use the typographic apostrophe ’ everywhere:
   - `auth.welcome.wordmark`: "FincWin"
   - `auth.welcome.tagline`: "[one line under the wordmark — user to supply]"  // AWAITING USER COPY (D-16)
   - `auth.apple.cta`: "Sign in with Apple" (used only by the Android custom button; iOS uses Apple's native localized label)
   - `auth.apple.pendingEnrolment`: "Apple sign-in arrives once our developer account is verified."
   - `auth.google.cta`: "Continue with Google"
   - `auth.error.signInFailed`: "Sign-in didn’t go through. Try again."
   - `consent.heading`: "Share anonymous usage?"
   - `consent.body`: "Helps us see where the app is confusing — never your amounts, payees or account names. Change this anytime in settings."
   - `consent.neverSentHeading`: "Never sent"
   - `consent.neverSent.amounts`: "Amounts and balances"; `.payees`: "Payees and merchants"; `.accounts`: "Account names"; `.notes`: "Notes, or anything you type"; `.identity`: "Your name or email"
   - `consent.share`: "Share usage"; `consent.decline`: "Not now"
   - `you.title`: "You"; `you.section.appearance`: "Appearance"; `you.accent.label`: "Accent"; `you.accent.green|navy|rust|slate`: "Green" | "Navy" | "Rust" | "Slate"; `you.font.label`: "Font pairing"; `you.font.bold|modern|grotesk|neutral`: "Bold" | "Modern" | "Grotesk" | "Neutral"
   - `you.section.privacy`: "Privacy"; `you.analytics.label`: "Share anonymous usage"; `you.analytics.hint`: "Never amounts, payees or account names."
   - `you.section.connection`: "Connection"; `you.connection.ok`: "Connected to Supabase"; `you.connection.checking`: "Checking connection…"; `you.connection.error`: "Not connected — check your internet connection."
   - `you.section.account`: "Account"; `you.signOut`: "Sign out"
   - `signOut.confirm.body_one`: "{{count}} change not yet saved. Signing out clears the encrypted cache and everything queued on this device."; `signOut.confirm.body_other`: "{{count}} changes not yet saved. Signing out clears the encrypted cache and everything queued on this device."; `signOut.confirm.cancel`: "Cancel"; `signOut.confirm.proceed`: "Sign out anyway"
   - `update.heading`: "Update needed"; `update.body`: "This version is behind. Update FincWin to keep using it."; `update.cta`: "Update now"
   - `a11y.close`: "Close"; `a11y.back`: "Back"
2. `copyStatus.ts`: `export const AWAITING_COPY_KEYS = ['auth.welcome.tagline'] as const;` (D-16) and `export const DRAFT_COPY_KEYS = [every consent.*, you.*, signOut.*, update.* key] as const;` (D-20: Claude-drafted, awaiting user review). Add a header comment saying the release checklist (Phase 11) must see AWAITING_COPY_KEYS empty.
3. `index.ts`: init i18next with `initReactI18next`, `resources: { en: { common: en } }`, `defaultNS: 'common'`, `lng` = `getLocales()[0]?.languageCode` from expo-localization if a catalogue exists for it, else 'en'; `fallbackLng: 'en'`, `interpolation: { escapeValue: false }`, `returnNull: false`. Export `i18n` and `export const useT = () => useTranslation().t;`. Init synchronously (`initImmediate: false` or the i18next 26 equivalent), so the first render has strings.
4. `i18next.d.ts`: `import 'i18next'; import type en from './locales/en'; declare module 'i18next' { interface CustomTypeOptions { defaultNS: 'common'; resources: { common: typeof en }; returnNull: false } }`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "AWAITING USER COPY (D-16)" src/i18n/locales/en.ts` succeeds
    - `grep -q "CustomTypeOptions" src/i18n/i18next.d.ts` succeeds
    - `grep -q "auth.welcome.tagline" src/i18n/copyStatus.ts` succeeds
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The catalogue and typed t() are available to every screen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Catalogue tests: typed keys, voice rules, copy status</name>
  <files>src/i18n/__tests__/catalogue.test.ts, src/i18n/__tests__/keys.typecheck.ts</files>
  <read_first>
    - src/i18n/locales/en.ts
    - src/i18n/copyStatus.ts
    - CLAUDE.md (Compliance constraint: no "advice", "recommendation", "you should")
  </read_first>
  <behavior>
    - Every leaf string contains no straight apostrophe (')
    - No leaf matches /\badvice\b|recommend|you should|stays? on (this|your) device|figures stay/i
    - Every key in AWAITING_COPY_KEYS and DRAFT_COPY_KEYS resolves to a string in en
    - i18n.t('update.heading') returns 'Update needed'; t('signOut.confirm.body', {count: 3}) starts with '3 changes'; with count 1 it starts with '1 change not'
    - keys.typecheck.ts: `// @ts-expect-error` on t('does.not.exist') and on t('you') (non-leaf) compiles only if those are errors; a valid key compiles
  </behavior>
  <action>
Write `catalogue.test.ts` walking the nested object for leaves, implementing the behaviours above. Write `keys.typecheck.ts` (excluded from coverage by the 00-05 jest config), which is checked by `tsc --noEmit`: `import { i18n } from '../index'; const t = i18n.t; t('update.heading'); // @ts-expect-error unknown key\n t('does.not.exist'); // @ts-expect-error non-leaf key\n t('you');`. If i18next 26 types allow non-leaf keys by default, drop that line and record why.
  </action>
  <verify>
    <automated>npx jest src/i18n && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/i18n` passes
    - `grep -c "@ts-expect-error" src/i18n/__tests__/keys.typecheck.ts` is at least 1, and `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>DSG-04 type safety and the voice rules are enforced by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copy -> store review / users | Inaccurate claims feed App Privacy answers and compliance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-12-01 | R | Catalogue copy | mitigate | Test forbids advice/recommendation/"you should" and on-device-storage claims (D-15, CLAUDE.md compliance) |
| T-00-12-02 | T | Interpolation injection | mitigate | Only a numeric {{count}} is interpolated; React Native Text does not render HTML, so escapeValue:false is safe |
</threat_model>

<verification>
- `npx jest src/i18n` and `npx tsc --noEmit` pass
</verification>

<success_criteria>
DSG-04 holds, with D-16 placeholders and D-20 drafts clearly tracked.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-12-SUMMARY.md`
</output>
