---
phase: 00-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/config.toml
  - supabase/.gitignore
  - docs/decisions/supabase-environments.md
autonomous: false
requirements: [ENV-03, ENV-17, ENV-18]
user_setup:
  - service: supabase
    why: "Organisation + personal access token so the CLI can create and link projects (D-08)"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens -> Generate new token (name: fincwin-cli)"
    dashboard_config:
      - task: "Create organisation 'FincWin United' (Free plan) from your personal login"
        location: "https://supabase.com/dashboard/new"

must_haves:
  truths:
    - "The Supabase CLI and Docker Desktop work on the dev machine: supabase --version succeeds and docker info shows a Server section"
    - "A development cloud project and a separate production cloud project exist under the company's Supabase organisation, both in US East (us-east-1) (ENV-17, ENV-18, D-22)"
    - "The region choice and its reasoning are recorded in the repo (ENV-18, D-22)"
    - "The local Supabase CLI stack starts, and the repo is linked to the dev project (ENV-17)"
    - "The dev project's REST endpoint answers with the publishable key (ENV-03 provisioned half; the in-app check lands in 00-18)"
    - "Every Supabase credential lives only in gitignored .env.local / .env.production.local"
  artifacts:
    - path: "supabase/config.toml"
      provides: "Supabase CLI project config for the local stack"
      contains: "project_id"
    - path: "docs/decisions/supabase-environments.md"
      provides: "Environment map (local / dev / prod), region reasoning, migrations-only rule"
      contains: "us-east-1"
  key_links:
    - from: "supabase CLI"
      to: "dev cloud project"
      via: "supabase link --project-ref $SUPABASE_DEV_PROJECT_REF"
      pattern: "SUPABASE_DEV_PROJECT_REF"
---

<objective>
Provision Supabase: install the CLI toolchain, create the company organisation's dev and prod projects in US East, initialise the local stack, and link the repo to dev.

Purpose: Every phase from Record onward is hard-blocked on Supabase. ENV-17 needs a local CLI stack plus separate cloud dev and prod projects, changed only through migrations. ENV-18 needs a deliberate, recorded region choice (D-22: US East).
Output: supabase/config.toml, docs/decisions/supabase-environments.md, and gitignored env files holding project refs and keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-foundation/00-CONTEXT.md
@.planning/phases/00-foundation/00-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Install Supabase CLI, start Docker Desktop, create the Supabase organisation and access token</name>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md (Pitfall 6, Environment Availability)
  </read_first>
  <action>Human-only steps: GUI installs, Docker Desktop start-up, and a Supabase login. `npm install -g supabase` is not supported by Supabase, so use Scoop.</action>
  <what-built>Nothing yet. Research found Docker's daemon stopped and the Supabase CLI missing on this machine.</what-built>
  <how-to-verify>
    1. Install Scoop if absent (PowerShell): `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser; irm get.scoop.sh | iex`
    2. `scoop bucket add supabase https://github.com/supabase/scoop-bucket.git` then `scoop install supabase`. Confirm `supabase --version` prints a version.
    3. Start Docker Desktop and turn on Settings -> General -> "Start Docker Desktop when you sign in". Confirm `docker info` shows a "Server:" section.
    4. At https://supabase.com/dashboard, create the organisation "FincWin United" on the Free plan (D-08: your personal login, company org).
    5. Account -> Access Tokens -> generate a token named `fincwin-cli`. Put it in a new file `.env.local` at the repo root as `SUPABASE_ACCESS_TOKEN=<token>`. `.env.local` is gitignored. Do not paste the token into chat.
    6. Reply "ready", which also approves creating two Free projects, `fincwin-dev` and `fincwin-prod`, in us-east-1 (modifying an external service needs approval).
  </how-to-verify>
  <acceptance_criteria>
    - User replies "ready" (approval to create both projects)
  </acceptance_criteria>
  <resume-signal>Type "ready" or describe what failed</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Init the local stack, create dev + prod projects in us-east-1, link dev, capture keys into gitignored env files</name>
  <files>supabase/config.toml, supabase/.gitignore, docs/decisions/supabase-environments.md</files>
  <read_first>
    - .gitignore (confirm `.env.local` and `.env.*.local` are ignored before writing any secret)
    - .planning/phases/00-foundation/00-CONTEXT.md (D-22, D-23, D-08)
  </read_first>
  <action>
All commands run from the repo root with `SUPABASE_ACCESS_TOKEN` exported from `.env.local`, e.g. `set -a; . ./.env.local; set +a`.
1. `git check-ignore .env.local .env.production.local` must print both paths. Stop if it does not.
2. `supabase init` (answer no to the VS Code/Deno settings prompts, or pass `--with-vscode-settings=false` if the flag exists). Set `project_id = "fincwin"` in `supabase/config.toml`. Leave all other defaults.
3. `supabase orgs list -o json` and take the id of the org named "FincWin United".
4. Generate two DB passwords: `node -e "console.log(require('crypto').randomBytes(24).toString('base64url'))"` (run it twice).
5. `supabase projects create fincwin-dev --org-id <ORG_ID> --region us-east-1 --db-password <DEV_PW>` and `supabase projects create fincwin-prod --org-id <ORG_ID> --region us-east-1 --db-password <PROD_PW>`. Then `supabase projects list -o json` to get both refs. Wait until status is ACTIVE_HEALTHY: poll every 20s, up to 10 minutes.
6. `supabase projects api-keys --project-ref <DEV_REF> -o json`. Use the publishable key (`sb_publishable_...`) if present, otherwise the `anon` key. Take the `service_role` / secret key too.
7. Append to `.env.local` (dev; the emulator build reads this): `SUPABASE_DEV_PROJECT_REF=<DEV_REF>`, `SUPABASE_DEV_DB_PASSWORD=<DEV_PW>`, `SUPABASE_DB_PASSWORD=<DEV_PW>` (the name `supabase db push` reads non-interactively), `EXPO_PUBLIC_SUPABASE_URL=https://<DEV_REF>.supabase.co`, `EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=<dev publishable/anon>`, `SUPABASE_SERVICE_ROLE_KEY=<dev service_role>`.
   Create `.env.production.local` with `SUPABASE_PROD_PROJECT_REF`, `SUPABASE_PROD_DB_PASSWORD`, `EXPO_PUBLIC_SUPABASE_URL=https://<PROD_REF>.supabase.co`, `EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=<prod publishable/anon>`, `SUPABASE_SERVICE_ROLE_KEY=<prod service_role>`.
8. `supabase link --project-ref <DEV_REF> --password <DEV_PW>`. Dev is the default link target; prod is only ever pushed explicitly in 00-19.
9. Write `docs/decisions/supabase-environments.md` with:
   (a) Environments table: `local` (Supabase CLI in Docker, used by pgTAP and CI) | `dev` (fincwin-dev, Free, us-east-1, on-device testing) | `prod` (fincwin-prod, Free now, moves to Pro before first real data in Phase 2 per ENV-16). Record refs by name only, as "see .env.local". Refs are not secret, but keeping them out of the doc means one source of truth.
   (b) ENV-18 region reasoning: "US East (us-east-1) chosen at project creation because launch users are mostly North American (D-22). Region cannot be moved later without a dump/restore migration. GDPR still applies everywhere: EU users are covered by Supabase's DPA and Standard Contractual Clauses, which the privacy policy will state. PostHog stays on its EU host (D-23), an accepted split geography."
   (c) ENV-17 rule: "Schema reaches any environment only through `supabase/migrations/*.sql` in git: `supabase db reset` locally, `supabase db push` to dev, and an explicitly approved `supabase db push` to prod. No dashboard SQL edits to schema, ever. Migrations must stay compatible with the oldest supported app version (see app_config.min_supported_version)."
  </action>
  <verify>
    <automated>test -f supabase/config.toml && grep -q 'project_id = "fincwin"' supabase/config.toml && supabase projects list -o json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const p=JSON.parse(s);const n=p.filter(x=>['fincwin-dev','fincwin-prod'].includes(x.name)&&/us-east-1/.test(x.region));process.exit(n.length===2?0:1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase projects list -o json` shows `fincwin-dev` and `fincwin-prod`, each with region `us-east-1`
    - `grep -q "us-east-1" docs/decisions/supabase-environments.md` and `grep -q "D-22" docs/decisions/supabase-environments.md` succeed
    - `git check-ignore .env.local .env.production.local` prints both
    - `git ls-files | grep -E "^\.env"` returns nothing
    - `grep -rE "sb_secret_|service_role\"|eyJhbGci" docs/ supabase/config.toml` returns nothing
  </acceptance_criteria>
  <done>Dev and prod projects exist in us-east-1, the repo is linked to dev, and credentials exist only in gitignored files.</done>
</task>

<task type="auto">
  <name>Task 3: Start the local stack and verify the cloud dev REST endpoint</name>
  <files>supabase/config.toml</files>
  <read_first>
    - supabase/config.toml
  </read_first>
  <action>
1. `supabase start` (Docker must be running). Then `supabase status` must list API URL `http://127.0.0.1:54321` and DB URL `postgresql://postgres:postgres@127.0.0.1:54322/postgres`. If ports clash on Windows, change only the conflicting `port` values in `supabase/config.toml` and record the change in the SUMMARY.
2. Cloud dev check (ENV-03 provisioned half): `curl -s -o /dev/null -w "%{http_code}" "$EXPO_PUBLIC_SUPABASE_URL/rest/v1/" -H "apikey: $EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY"` must print `200`. If a publishable key returns 401 on the root, also try `-H "Authorization: Bearer $EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY"`, and record which header set works.
3. `supabase stop` is optional. Leave the stack running for 00-06 if it is next.
  </action>
  <verify>
    <automated>supabase status && set -a && . ./.env.local && set +a && test "$(curl -s -o /dev/null -w '%{http_code}' "$EXPO_PUBLIC_SUPABASE_URL/rest/v1/" -H "apikey: $EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY")" = "200"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase status` exits 0 and prints an API URL
    - The curl against the dev project's `/rest/v1/` returns HTTP 200
  </acceptance_criteria>
  <done>Local stack runs; cloud dev answers with the publishable key.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine -> Supabase Management API | Personal access token grants org-wide control |
| repo -> GitHub | Anything tracked may leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-03-01 | I | SUPABASE_ACCESS_TOKEN, DB passwords, service_role keys | mitigate | Stored only in `.env.local` / `.env.production.local`; `git check-ignore` asserted before writing; `git ls-files` must list no `.env*`; gitleaks CI in 00-08 |
| T-00-03-02 | E | Accidental prod changes | mitigate | Repo linked to dev only; prod is pushed only by an explicitly approved step in 00-19 |
| T-00-03-03 | I | Data residency | accept | US East chosen deliberately (D-22); EU coverage via Supabase DPA/SCCs, disclosed in privacy policy |
| T-00-03-04 | S | Weak DB password | mitigate | 24 random bytes base64url from Node crypto (CSPRNG) |
</threat_model>

<verification>
- Both projects are listed in us-east-1; local stack status is OK; dev REST returns 200
</verification>

<success_criteria>
ENV-17 and ENV-18 are satisfied, and the provisioned half of ENV-03 is done.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-03-SUMMARY.md`
</output>
