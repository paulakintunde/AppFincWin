---
phase: 00-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/config.toml
  - supabase/.gitignore
  - docs/decisions/supabase-environments.md
autonomous: false
requirements: [ENV-03, ENV-17, ENV-18]
user_setup:
  - service: supabase
    why: "Organisation + personal access token so the CLI can link and manage the existing project (D-08)"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens -> Generate new token (name: fincwin-cli)"
    dashboard_config:
      - task: "Confirm the organisation 'FincWin United' (Free plan) and its project are visible under your personal login"
        location: "https://supabase.com/dashboard/new"

must_haves:
  truths:
    - "The Supabase CLI and Docker Desktop work on the dev machine: supabase --version succeeds and docker info shows a Server section"
    - "The one production cloud project exists under the company's Supabase organisation in US West (us-west-2): Fincwin United, ref cohmcbdfgqmiwykztrdg (ENV-17, ENV-18, D-22)"
    - "The region choice and its reasoning are recorded in the repo (ENV-18, D-22)"
    - "The local Supabase CLI stack starts, and the repo is linked to the production project (ENV-17)"
    - "The production project's REST endpoint answers with the publishable key (ENV-03 provisioned half; the in-app check lands in 00-18)"
    - "Every Supabase credential lives only in the gitignored .env.local"
  artifacts:
    - path: "supabase/config.toml"
      provides: "Supabase CLI project config for the local stack"
      contains: "project_id"
    - path: "docs/decisions/supabase-environments.md"
      provides: "Environment map (local / production), region reasoning, migrations-only rule"
      contains: "us-west-2"
  key_links:
    - from: "supabase CLI"
      to: "production cloud project"
      via: "supabase link --project-ref $SUPABASE_PROD_PROJECT_REF"
      pattern: "SUPABASE_PROD_PROJECT_REF"
---

<objective>
Provision Supabase: install the CLI toolchain, adopt and link the company organisation's single production project in US West, and initialise the local stack.

Purpose: Every phase from Record onward is hard-blocked on Supabase. ENV-17 needs a local CLI stack plus the cloud production project, changed only through migrations in git. ENV-18 needs a deliberate, recorded region choice (D-22: US West).
Output: supabase/config.toml, docs/decisions/supabase-environments.md, and a gitignored .env.local holding the project ref and keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-foundation/00-CONTEXT.md
@.planning/phases/00-foundation/00-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Install Supabase CLI, start Docker Desktop, create the Supabase organisation and access token</name>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md (Pitfall 6, Environment Availability)
  </read_first>
  <action>Human-only steps: GUI installs, Docker Desktop start-up, and a Supabase login. `npm install -g supabase` is not supported by Supabase, so use Scoop.</action>
  <what-built>Nothing yet. Research found Docker's daemon stopped and the Supabase CLI missing on this machine.</what-built>
  <how-to-verify>
    1. Install Scoop if absent (PowerShell): `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser; irm get.scoop.sh | iex`
    2. `scoop bucket add supabase https://github.com/supabase/scoop-bucket.git` then `scoop install supabase`. Confirm `supabase --version` prints a version.
    3. Start Docker Desktop and turn on Settings -> General -> "Start Docker Desktop when you sign in". Confirm `docker info` shows a "Server:" section.
    4. At https://supabase.com/dashboard, confirm the organisation "FincWin United" (Free plan, D-08) and its project "Fincwin United" are both present. Both already exist — nothing is created here.
    5. Account -> Access Tokens -> generate a token named `fincwin-cli`. Put it in a new file `.env.local` at the repo root as `SUPABASE_ACCESS_TOKEN=<token>`. `.env.local` is gitignored. Do not paste the token into chat.
    6. Reply "ready". The production project already exists (`Fincwin United`, ref `cohmcbdfgqmiwykztrdg`, us-west-2), so no project is created in this plan — "ready" confirms the CLI and Docker are installed and that `SUPABASE_ACCESS_TOKEN` is in `.env.local`.
  </how-to-verify>
  <acceptance_criteria>
    - User replies "ready" (CLI, Docker and access token confirmed)
  </acceptance_criteria>
  <resume-signal>Type "ready" or describe what failed</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Init the local stack, link the existing production project, capture keys into .env.local</name>
  <files>supabase/config.toml, supabase/.gitignore, docs/decisions/supabase-environments.md</files>
  <read_first>
    - .gitignore (confirm `.env.local` and `.env.*.local` are ignored before writing any secret)
    - .planning/phases/00-foundation/00-CONTEXT.md (D-22, D-23, D-08)
  </read_first>
  <action>
All commands run from the repo root with `SUPABASE_ACCESS_TOKEN` exported from `.env.local`, e.g. `set -a; . ./.env.local; set +a`.
1. `git check-ignore .env.local` must print the path. Stop if it does not.
2. `supabase init` only if `supabase/config.toml` is absent; it already exists in this repo pinning `project_id = "cohmcbdfgqmiwykztrdg"`. If you do run init, set `project_id = "cohmcbdfgqmiwykztrdg"` (the project ref, which is what `scripts/supabase-preflight.mjs` asserts). Leave all other defaults.
3. `supabase orgs list -o json` must show the org "FincWin United", confirming the token works.
4. The project's DB password was set at creation and lives in `.env.local` as `SUPABASE_DB_PASSWORD` plus the password manager. If it is missing, reset it in Dashboard -> Project Settings -> Database -> Reset database password and update `.env.local`.
5. `supabase projects list -o json` must show the project `Fincwin United` with id `cohmcbdfgqmiwykztrdg`, region `us-west-2`, status `ACTIVE_HEALTHY`. No project is created.
6. `supabase projects api-keys --project-ref "$SUPABASE_PROD_PROJECT_REF" -o json`. Use the publishable key (`sb_publishable_...`) if present, otherwise the `anon` key. Take the `service_role` / secret key too.
7. Append to `.env.local`: `SUPABASE_ACCESS_TOKEN`, `SUPABASE_PROD_PROJECT_REF=cohmcbdfgqmiwykztrdg`, `SUPABASE_DB_PASSWORD` (the name `supabase db push` reads non-interactively), `EXPO_PUBLIC_SUPABASE_URL=https://<REF>.supabase.co`, `EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SERVICE_ROLE_KEY`. There is only one project, so `.env.local` carries its values.
8. `supabase link --project-ref "$SUPABASE_PROD_PROJECT_REF" --password "$SUPABASE_DB_PASSWORD"`. There is one project and it is production, so it is the only link target. Pushes are guarded by scripts/supabase-preflight.mjs — run `npm run supabase:preflight` before any `supabase db push`, or use `npm run supabase:db:push`, which chains the check (CLAUDE.md).
9. Write `docs/decisions/supabase-environments.md` with:
   (a) Environments table: `local` (Supabase CLI in Docker, used by pgTAP and CI) | `prod` (Fincwin United, Free now, moves to Pro before first real data in Phase 2 per ENV-16). Record refs by name only, as "see .env.local". Refs are not secret, but keeping them out of the doc means one source of truth.
   (b) ENV-18 region reasoning: "US West (us-west-2) chosen at project creation because launch users are mostly North American (D-22). Region cannot be moved later without a dump/restore migration. GDPR still applies everywhere: EU users are covered by Supabase's DPA and Standard Contractual Clauses, which the privacy policy will state. PostHog stays on its EU host (D-23), an accepted split geography."
   (c) ENV-17 rule: "Schema reaches the database only through supabase/migrations/*.sql in git: supabase db reset locally, and npm run supabase:db:push (preflight + supabase db push) to production. There is no separate dev project, so every push is a production push. No dashboard SQL edits to schema, ever. Migrations must stay compatible with the oldest supported app version (see app_config.min_supported_version)."
  </action>
  <verify>
    <automated>test -f supabase/config.toml && grep -q 'project_id = "cohmcbdfgqmiwykztrdg"' supabase/config.toml && supabase projects list -o json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const p=JSON.parse(s);const m=p.filter(x=>x.id==='cohmcbdfgqmiwykztrdg'&&/us-west-2/.test(x.region));process.exit(m.length===1?0:1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase projects list -o json` shows `Fincwin United` (id `cohmcbdfgqmiwykztrdg`) in region `us-west-2`
    - `grep -q "us-west-2" docs/decisions/supabase-environments.md` and `grep -q "D-22" docs/decisions/supabase-environments.md` succeed
    - `git check-ignore .env.local` prints the path
    - `git ls-files | grep -E "^\.env"` returns nothing
    - `grep -rE "sb_secret_|service_role\"|eyJhbGci" docs/ supabase/config.toml` returns nothing
  </acceptance_criteria>
  <done>The production project is linked, the local stack is configured, and credentials exist only in .env.local.</done>
</task>

<task type="auto">
  <name>Task 3: Start the local stack and verify the production REST endpoint</name>
  <files>supabase/config.toml</files>
  <read_first>
    - supabase/config.toml
  </read_first>
  <action>
1. `supabase start` (Docker must be running). Then `supabase status` must list API URL `http://127.0.0.1:54321` and DB URL `postgresql://postgres:postgres@127.0.0.1:54322/postgres`. If ports clash on Windows, change only the conflicting `port` values in `supabase/config.toml` and record the change in the SUMMARY.
2. Cloud production check (ENV-03 provisioned half): `curl -s -o /dev/null -w "%{http_code}" "$EXPO_PUBLIC_SUPABASE_URL/rest/v1/" -H "apikey: $EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY"` must print `200`. If a publishable key returns 401 on the root, also try `-H "Authorization: Bearer $EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY"`, and record which header set works.
3. `supabase stop` is optional. Leave the stack running for 00-06 if it is next.
  </action>
  <verify>
    <automated>supabase status && set -a && . ./.env.local && set +a && test "$(curl -s -o /dev/null -w '%{http_code}' "$EXPO_PUBLIC_SUPABASE_URL/rest/v1/" -H "apikey: $EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY")" = "200"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase status` exits 0 and prints an API URL
    - The curl against the production project's `/rest/v1/` returns HTTP 200
  </acceptance_criteria>
  <done>Local stack runs; the production project answers with the publishable key.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine -> Supabase Management API | Personal access token grants org-wide control |
| repo -> GitHub | Anything tracked may leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-03-01 | I | SUPABASE_ACCESS_TOKEN, DB passwords, service_role keys | mitigate | Stored only in `.env.local`; `git check-ignore` asserted before writing; `git ls-files` must list no `.env*`; gitleaks CI in 00-08 |
| T-00-03-02 | E | Accidental prod changes | mitigate | There is one project and it is production; schema reaches it only through git migrations, and scripts/supabase-preflight.mjs asserts config.toml's project_id matches SUPABASE_PROD_PROJECT_REF before any push (npm run supabase:db:push) |
| T-00-03-03 | I | Data residency | accept | US West chosen deliberately (D-22); EU coverage via Supabase DPA/SCCs, disclosed in privacy policy |
| T-00-03-04 | S | Weak DB password | mitigate | 24 random bytes base64url from Node crypto (CSPRNG) |
</threat_model>

<verification>
- The production project is listed in us-west-2; local stack status is OK; its REST endpoint returns 200
</verification>

<success_criteria>
ENV-17 and ENV-18 are satisfied, and the provisioned half of ENV-03 is done.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-03-SUMMARY.md`
</output>
