---
phase: 00-foundation
plan: 19
type: execute
wave: 7
depends_on: ["00-07", "00-08", "00-09", "00-14", "00-15", "00-16", "00-18"]
files_modified:
  - docs/dependency-register.md
  - docs/acceptance/phase-00-android.md
autonomous: false
requirements: [ACC-05, ACC-02, ENV-06, ENV-03, ENV-09, ENV-17, ANL-01, FND-07, DSG-03, FND-06]

must_haves:
  truths:
    - "On the Android emulator, a user signs in with Google against the production project; auth.users, profiles, households and household_members rows exist for that UUID (ACC-02, ENV-06, ACC-04)"
    - "The user stays signed in after force-quit and relaunch (ACC-05)"
    - "The You screen shows 'Connected to Supabase' from a live check (ENV-03)"
    - "After opting in, events reach the PostHog EU project keyed by the Supabase UUID only; before opting in, none arrive (ANL-01, ANL-02)"
    - "Accent and font switch live; reduce-motion collapses the toggle animation; safe-area insets are respected (FND-06, FND-07, DSG-03)"
    - "Raising min_supported_version on the production project shows the update-required screen; restoring it clears the screen (FND-09)"
    - "The production project carries all four migrations and a working fx-sync function, applied through git migrations in 00-06 and 00-09 (ENV-17)"
    - "The dependency register shows every service as provisioned, pending or deferred with its blocker and landing phase (ENV-09)"
  artifacts:
    - path: "docs/acceptance/phase-00-android.md"
      provides: "Executed acceptance checklist with evidence"
      contains: "ACC-05"
    - path: "docs/dependency-register.md"
      provides: "Consolidated end-of-phase statuses"
  key_links:
    - from: "docs/dependency-register.md"
      to: "plan SUMMARYs 00-03..00-18 and docs/decisions/error-tracking.md"
      via: "status consolidation"
      pattern: "Sentry"
---

<objective>
Prove Phase 0 end to end on Android, consolidate the dependency register, and confirm the production project's schema and fx-sync are current.

Purpose: The success criteria are observable behaviours on a device. There is one Supabase project and it is production; 00-06 and 00-09 already migrated it from git, so this plan confirms that state rather than repeating the push (ENV-17). ENV-09 needs the register consolidated. iOS-specific criteria follow in 00-20 once Apple clears.
Output: docs/acceptance/phase-00-android.md, the updated register, and the production project confirmed current.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/00-foundation/00-VALIDATION.md
@docs/dependency-register.md
@docs/decisions/error-tracking.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated suite, fresh emulator dev build, register consolidation, acceptance checklist</name>
  <files>docs/dependency-register.md, docs/acceptance/phase-00-android.md</files>
  <read_first>
    - docs/dependency-register.md
    - All .planning/phases/00-foundation/00-*-SUMMARY.md files present (statuses, dates, deviations)
    - docs/decisions/error-tracking.md (the ENV-13 line)
    - .planning/phases/00-foundation/00-VALIDATION.md (Manual-Only Verifications)
  </read_first>
  <action>
1. Run the full suite: `npx tsc --noEmit && npx eslint . && npx depcruise src --config .dependency-cruiser.cjs && npx jest --coverage --ci && npm run verify:gates && supabase test db`, plus the Docker gitleaks scan from 00-08. Record the results.
2. `npx expo run:android` with `.env.local` pointing at the production project (the only project), with all of 00-10 to 00-18 in place.
3. Update the register (Last checked = today) from the SUMMARYs: Supabase production project provisioned ("Free; Pro before Phase 2, ENV-16"); EAS provisioned; Google OAuth provisioned; PostHog provisioned; Frankfurter provisioned ("fx-sync deployed in 00-09"); Sentry per error-tracking.md ("deferred, not needed (D-19)" or provisioned); App identifier provisioned (value); Apple/Play/D-U-N-S/website as last recorded, re-checked with the user in Task 2; Sign in with Apple deferred (blocker ENV-10). Add rows for any service a SUMMARY introduced (e.g. Resend SMTP, the gitleaks image) if missing.
4. Write `docs/acceptance/phase-00-android.md`: a checklist table `| # | Requirement | Step | Expected | Result | Evidence |`, pre-filled with the steps in Task 2 and the automated results from step 1.
  </action>
  <verify>
    <automated>npx jest --coverage --ci && supabase test db && test -f docs/acceptance/phase-00-android.md && grep -c "^| " docs/dependency-register.md</automated>
  </verify>
  <acceptance_criteria>
    - Every automated command in step 1 exits 0
    - `grep -q "ACC-05" docs/acceptance/phase-00-android.md` succeeds
    - Every register row has a Status in {provisioned, pending, deferred} and a Last checked date of today, or an explicit earlier date for untouched external rows
  </acceptance_criteria>
  <done>The automated gate is green, the register is consolidated, and the checklist is ready.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device acceptance on the Android emulator</name>
  <read_first>
    - docs/acceptance/phase-00-android.md
  </read_first>
  <action>The user performs the device checks. Claude runs every Management-API or SQL step and records the evidence in the checklist.</action>
  <what-built>The whole of Phase 0 running on the emulator against the production project.</what-built>
  <how-to-verify>
    1. Welcome screen: wordmark, placeholder tagline, Google button, Apple button disabled with the note. No email/password fields.
    2. Continue with Google and pick your test account. Expect the consent screen next. Claude verifies with a Management API query that `select p.id, p.email, h.id, m.role, m.weight from profiles p join household_members m on m.user_id = p.id join households h on h.id = m.household_id where p.email = '<you>'` returns one row with role owner and weight 1.
    3. Consent: before tapping anything, Claude confirms via the PostHog API that there are no events for your UUID. Tap "Share usage". Claude confirms an `analytics_opted_in` event whose distinct_id equals your Supabase UUID, with no email or name properties (ANL-01).
    4. You screen: shows "Connected to Supabase" (ENV-03). Switch to Navy then Rust, and to Grotesk then Neutral. Changes apply instantly with no reload (FND-06). Claude confirms profiles.accent and font_pairing changed on the production project.
    5. Force-quit (swipe away), reopen: you are still signed in, the chosen accent shows immediately, and there is no green flash (ACC-05, D-14).
    6. Android Settings -> Accessibility -> Remove animations ON. Toggle analytics off/on: the switch jumps without animating (FND-07). Turn the setting back off.
    7. Safe areas: nothing sits under the status bar or the gesture bar (DSG-03).
    8. Version gate: Claude runs `update public.app_config set value = '9.9.9' where key = 'min_supported_version'` through the Management API against the production project — the only project. This is a controlled test update; revert immediately after. Relaunch: the update-required screen shows and Back does not dismiss it. Claude restores '0.1.0'; relaunch shows the You screen (FND-09).
    9. Sign out: no dialog (0 pending). You land on welcome, and a relaunch stays on welcome with the default green (D-15).
    10. Confirm or correct the Apple / Play / D-U-N-S / website statuses in the register.
    11. Confirm or correct the register's Supabase row: the production project is migrated (00-06, 00-09) and fx-sync is deployed.
  </how-to-verify>
  <acceptance_criteria>
    - The user replies "approved" with results for steps 1-9
  </acceptance_criteria>
  <resume-signal>Reply "approved", or list the failing step numbers</resume-signal>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Confirm production's schema and fx-sync, then finalise the register and acceptance doc</name>
  <files>docs/dependency-register.md, docs/acceptance/phase-00-android.md</files>
  <read_first>
    - docs/decisions/supabase-environments.md
    - .env.local (load with `set -a; . ./.env.local; set +a`; never echo values)
  </read_first>
  <action>
1. `set -a; . ./.env.local; set +a`. Confirm the link target is the production project: `grep -q "$SUPABASE_PROD_PROJECT_REF" supabase/.temp/project-ref`; relink with `supabase link --project-ref "$SUPABASE_PROD_PROJECT_REF" --password "$SUPABASE_DB_PASSWORD"` if it differs.
2. `supabase migration list --linked` must show 20260922000100-20260922000400 as remote. If any is missing, run `npm run supabase:preflight` then `supabase db push --linked` to bring it up to date.
3. Confirm fx-sync is live: invoking `https://$SUPABASE_PROD_PROJECT_REF.supabase.co/functions/v1/fx-sync` with the correct `x-fx-sync-secret` from `.env.local` returns `ok:true`, and a wrong secret returns 403. If the function is absent, deploy it exactly as 00-09 Task 3 steps 3-5 describe.
4. Mark the acceptance doc results from Task 2 and the register rows (Supabase: "migrated YYYY-MM-DD", Frankfurter: "fx-sync deployed").
  </action>
  <verify>
    <automated>grep -q "$(date +%Y-%m-%d)" docs/dependency-register.md && grep -q "Result" docs/acceptance/phase-00-android.md && (set -a; . ./.env.local; set +a; grep -q "$SUPABASE_PROD_PROJECT_REF" supabase/.temp/project-ref)</automated>
  </verify>
  <acceptance_criteria>
    - `supabase migration list --linked` shows all four migrations on the production project, and an fx-sync invocation returns ok:true
    - `supabase/.temp/project-ref` equals `SUPABASE_PROD_PROJECT_REF`
    - Every step in docs/acceptance/phase-00-android.md has a Result filled in
  </acceptance_criteria>
  <done>The Android half of Phase 0 is accepted, the production project is confirmed current, and the register is final.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> production database | Schema changes on real-data infrastructure |
| test manipulation of app_config | Controlled update used to test the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-19-01 | T | Prod schema drift or unapproved change | mitigate | All schema changes come from git migrations, with npm run supabase:preflight asserting the link target and a --dry-run before any push; no dashboard SQL edits |
| T-00-19-02 | E | FX_SYNC_SECRET | mitigate | Single 32-byte random secret held only in .env.local, the function secret store and Vault; never in git |
| T-00-19-03 | D | Leaving min_supported_version raised | mitigate | Test update applied to the production project and reverted in the same step; verified by relaunch |
| T-00-19-04 | I | Test account data | accept | Test user on the production project; no real financial data exists in Phase 0 |
</threat_model>

<verification>
- The acceptance doc is fully filled in; the register is consolidated; the production project is confirmed current
</verification>

<success_criteria>
Roadmap success criteria 2 (Google half), 4, 5, 6, 7 and 8 are demonstrated on Android.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-19-SUMMARY.md`
</output>
