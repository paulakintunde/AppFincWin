---
phase: 01-money-core
plan: 15
type: execute
wave: 6
depends_on: ["01-12", "01-13", "01-14"]
files_modified:
  - app/_layout.tsx
  - src/features/you/YouScreen.tsx
  - src/features/you/__tests__/YouScreen.test.tsx
  - src/features/you/components/DevSyncProbe.tsx
  - src/features/you/__tests__/DevSyncProbe.test.tsx
  - src/i18n/locales/en.ts
  - src/i18n/copyStatus.ts
autonomous: true
requirements: [SYN-06, SYN-01, SYN-02]

must_haves:
  truths:
    - "The whole signed-in app runs inside QueryProvider, so the encrypted cache restores on launch and queued writes replay after restore (SYN-01, SYN-02)"
    - "The You screen shows the sync status line in its Connection section: 'offline · N changes queued' or 'synced N minutes ago' (SYN-06, D-14)"
    - "The You screen carries a permanent 'Rates By Exchange Rate API' credit line (D-13)"
    - "Permanently failed or conflicting writes are reported to error tracking as scrubbed entity/kind/code events with no amounts (D-19, Phase 0 D-18)"
    - "Development builds (never release builds) show a 'Queue a test entry' row that writes a real foreign-currency test transaction through the queue, so the offline flow can be exercised on device before Record ships (supports SYN-02 device verification)"
    - "Sign-out's unsynced-changes warning counts queued writes, because the query-cache wipe handler reports them (Phase 0 D-15)"
  artifacts:
    - path: "app/_layout.tsx"
      provides: "QueryProvider in the provider tree + failure reporter wiring"
      contains: "QueryProvider"
    - path: "src/features/you/YouScreen.tsx"
      provides: "SyncStatusLine + credits line"
      contains: "SyncStatusLine"
  key_links:
    - from: "app/_layout.tsx"
      to: "src/data/QueryProvider.tsx"
      via: "<QueryProvider> wraps the auth/route tree"
      pattern: "<QueryProvider>"
    - from: "app/_layout.tsx"
      to: "src/services/errors captureError"
      via: "setFailureReporter(f => captureError(new Error(`write-failed:${f.entity}:${f.kind}:${f.code}`), { area: 'sync' }))"
      pattern: "setFailureReporter"
---

<objective>
Wire Phase 1 into the running app: mount `QueryProvider` in the root layout, connect failed-write reporting to Phase 0's error tracker, and place the sync status line and the open.er-api credit on the You screen.

Purpose: makes SYN-01/02/06 real on device. These files are owned by Phase 0 plans 00-16 (error reporter), 00-17 (root layout) and 00-18 (You screen), so this plan runs only after those land.
Output: three edited files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-money-core/01-CONTEXT.md
@.planning/phases/00-foundation/00-16-SUMMARY.md
@.planning/phases/00-foundation/00-17-SUMMARY.md
@.planning/phases/00-foundation/00-18-SUMMARY.md
@app/_layout.tsx
@src/features/you/YouScreen.tsx
@src/data/QueryProvider.tsx
@src/data/sync/failedWrites.ts
@src/ui/SyncStatusLine.tsx

<interfaces>
From 01-12: `QueryProvider({ children })` in src/data/QueryProvider.tsx.
From 01-09: `setFailureReporter(reporter | null)` in src/data/sync/failedWrites.ts; reporter receives `{ entity, kind, code }`.
From 01-14: `SyncStatusLine()` in src/ui/SyncStatusLine.tsx; i18n key `credits.exchangeRateApi`.
From Phase 0 00-16 (per its plan): `captureError(error: unknown, context?: { area: 'auth' | 'theme' | 'sync' | 'boot' | 'unknown' }): void` exported from '@/services/errors'.
From Phase 0 00-17: app/_layout.tsx provider tree `SafeAreaProvider > ThemeProvider > AuthProvider > ...`.
From Phase 0 00-18: YouScreen groups Appearance, Privacy, Connection (ConnectionStatus), Account; SettingsGroup component.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Precondition check, QueryProvider mount and failure reporting</name>
  <files>app/_layout.tsx</files>
  <read_first>
    - .planning/phases/00-foundation/00-16-SUMMARY.md, 00-17-SUMMARY.md, 00-18-SUMMARY.md (must exist; they describe the files being edited)
    - app/_layout.tsx (current provider tree as 00-17 left it)
    - src/services/errors/index.ts (confirm captureError's actual exported signature)
    - src/data/QueryProvider.tsx, src/data/sync/failedWrites.ts
  </read_first>
  <action>
1. Precondition: run `test -f .planning/phases/00-foundation/00-16-SUMMARY.md && test -f .planning/phases/00-foundation/00-17-SUMMARY.md && test -f .planning/phases/00-foundation/00-18-SUMMARY.md && test -f src/features/you/YouScreen.tsx && test -f src/services/errors/index.ts`. If any is missing, STOP and return a checkpoint: "Phase 0 plans 00-16/00-17/00-18 must complete before 01-15 can wire the app root and You screen." Do not create stand-in versions of those files.
2. In `app/_layout.tsx`, import `{ QueryProvider } from '@/data/QueryProvider'` and wrap the existing tree inside `ThemeProvider` and around `AuthProvider`: `SafeAreaProvider > ThemeProvider > QueryProvider > AuthProvider > ...`. Keep every existing provider, splash gating, ReducedMotionConfig and error-reporting init exactly as 00-17 left them.
3. At module scope, after the existing error-reporting init call, add:
```typescript
setFailureReporter((f) => captureError(new Error(`write-failed:${f.entity}:${f.kind}:${f.code}`), { area: 'sync' }));
```
The message carries only the entity, kind and code (no amounts, ids or notes). If 00-16 exported a different context union, use its 'sync'-equivalent value and note it in the SUMMARY.
4. Run the existing root-layout tests from 00-17 (`npx jest app src/features/system`) and keep them green.
  </action>
  <verify>
    <automated>npx jest app src/features/system --silent && npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<QueryProvider>" app/_layout.tsx` returns 1
    - `grep -c "setFailureReporter" app/_layout.tsx` returns 1
    - `grep -c "area: 'sync'" app/_layout.tsx` returns 1
    - existing 00-17 tests still pass
  </acceptance_criteria>
  <done>The app root restores the encrypted cache, replays queued writes, and reports permanent write failures without amounts.</done>
</task>

<task type="auto">
  <name>Task 2: Sync status line, credits and dev sync probe on the You screen</name>
  <files>src/features/you/YouScreen.tsx, src/features/you/__tests__/YouScreen.test.tsx, src/features/you/components/DevSyncProbe.tsx, src/features/you/__tests__/DevSyncProbe.test.tsx, src/i18n/locales/en.ts, src/i18n/copyStatus.ts</files>
  <read_first>
    - src/features/you/YouScreen.tsx and its test (00-18: section structure, SettingsGroup, how ConnectionStatus is placed)
    - src/ui/SyncStatusLine.tsx (01-14)
    - src/i18n/locales/en.ts (credits.exchangeRateApi)
  </read_first>
  <action>
1. In the Connection section, directly below `ConnectionStatus`, render `<SyncStatusLine />` (the prototype places the sync line in settings, line 5329, D-14).
2. At the foot of the screen (after the Account section), add a credits line: `t('credits.exchangeRateApi')` as a pressable link (accessibilityRole 'link') opening `https://www.exchangerate-api.com` via `Linking.openURL`, styled with the meta size and `colors.inkFaint`, tokens only (D-13 permanent About/credits line; a dedicated About screen arrives later and can move it).
3. `DevSyncProbe.tsx` (rendered by YouScreen only when `__DEV__` is true; release builds never include the row): a pressable row labelled `t('dev.syncProbe.label')` ('Queue a test entry (dev)'). On press: read `useAuth().user`, `useHouseholdId(user.id)`, `useMoneyPrefs(user.id)`, `useAccounts(householdId)`; if no account named 'Sync test' exists, `useAddAccount().add({ household_id, name: 'Sync test', kind: 'cash', currency: prefs.home_currency, opening_balance: 0 })`; then `useAddTransaction().add({ householdId, accountId, amount: minorUnits(-1000), currency: prefs.home_currency === 'EUR' ? 'USD' : 'EUR', homeCurrency: prefs.home_currency, userId: user.id, note: 'sync probe' })` so the foreign-currency stamp path is exercised. Add en.ts keys `dev: { syncProbe: { label: 'Queue a test entry (dev)' } }` and list 'dev.syncProbe.label' in DRAFT_COPY_KEYS. `DevSyncProbe.test.tsx`: with mocked hooks, pressing the row calls add on both mutations when no probe account exists and only the transaction add when it does.
4. Update YouScreen.test.tsx: mock `@/data/sync/useSyncStatus` to return `{ isOnline: false, queued: 3, failed: 0, conflicts: 0, lastSyncedAt: null }` and assert 'offline · 3 changes queued' renders; assert the credit text renders and pressing it calls Linking.openURL with the URL. Wrap renders in a QueryClientProvider if SyncStatusLine's dependencies require it (useSyncStatus is mocked, so it should not). Keep every existing 00-18 assertion.
  </action>
  <verify>
    <automated>npx jest src/features/you --silent && npm run lint && npm run typecheck && npm run depcruise</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SyncStatusLine" src/features/you/YouScreen.tsx` is at least 2 (import and usage)
    - `grep -c "credits.exchangeRateApi" src/features/you/YouScreen.tsx` returns 1
    - `grep -c "offline · 3 changes queued" src/features/you/__tests__/YouScreen.test.tsx` returns 1
    - all YouScreen tests pass, including the pre-existing ones
    - `grep -c "__DEV__" src/features/you/YouScreen.tsx` is at least 1 (probe gated to dev builds)
    - `npx jest src/features/you/__tests__/DevSyncProbe.test.tsx src/i18n` passes
  </acceptance_criteria>
  <done>The You screen shows live sync status and the permanent rate-source credit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → error tracking vendor | failure events leave the device |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15-01 | Information Disclosure | write-failure events | mitigate | Message is `write-failed:<entity>:<kind>:<code>` only; 00-16's scrubber runs on top |
| T-01-15-02 | Denial of Service | provider order mistakes breaking boot | mitigate | Existing 00-17 root-layout tests must stay green; QueryProvider placed inside ThemeProvider without altering splash gating |
</threat_model>

<verification>
- `npx jest app src/features --silent`
- `npm run lint && npm run typecheck && npm run depcruise`
</verification>

<success_criteria>
Phase 1's data layer runs in the real app and its sync state is visible on the You screen.
</success_criteria>

<output>
After completion, create `.planning/phases/01-money-core/01-15-SUMMARY.md`
</output>
