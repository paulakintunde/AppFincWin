---
phase: 01-money-core
plan: 16
type: execute
wave: 7
depends_on: ["01-06", "01-11", "01-13", "01-15"]
files_modified:
  - docs/acceptance/phase-01-money-core.md
autonomous: false
requirements: [MON-05, MON-06, MON-09, MON-10, MON-11, MON-12, SYN-01, SYN-02, SYN-07, DSG-06]
user_setup:
  - service: resend
    why: "Operator FX digest emails (D-09, D-12)"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend dashboard -> API Keys (already listed in .env.local; must be filled)"
      - name: RESEND_FROM_EMAIL
        source: "A sender on the verified fincwin.com domain, e.g. no-reply@fincwin.com"
      - name: FX_ALERT_TO_EMAIL
        source: "The operator inbox that should receive FX alerts; add this key to .env.local"

must_haves:
  truths:
    - "Every Phase 1 migration (20260924000100 to 20260924000700) is applied to the production Supabase project after the full local gate is green (schema push requirement)"
    - "fx-sync, resolve-rate and fx-monitor are deployed, with Resend and alert secrets set and the fx_monitor_url Vault row created, no secret ever echoed or committed"
    - "A live fx-sync run writes today's rates and populates currencies; a live fx-monitor run returns ok (MON-06, MON-10)"
    - "On a real device with airplane mode on, loaded data stays browsable, a new record shows as queued, and it flushes on reconnect (SYN-01, SYN-02, SYN-06)"
    - "Amounts and dates render per device locale on Hermes, not just in Node's ICU (DSG-06, D-22, D-23)"
  artifacts:
    - path: "docs/acceptance/phase-01-money-core.md"
      provides: "production + device acceptance record for Phase 1"
  key_links:
    - from: "npm run supabase:db:push"
      to: "production project cohmcbdfgqmiwykztrdg"
      via: "preflight ref check then supabase db push"
      pattern: "supabase:db:push"
---

<objective>
Roll Phase 1 out to the single (production) Supabase project and prove it on a device: run the full local gate, push the schema [BLOCKING], deploy the three Edge Functions with their secrets and Vault row, smoke-test them live, then have the user confirm offline behaviour and locale formatting on a real device.

Purpose: the phase cannot pass verification without the schema on production; SYN-01/02 need the manual device check listed in 01-VALIDATION.md.
Output: production state changed, one acceptance doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-money-core/01-VALIDATION.md
@.planning/phases/00-foundation/00-09-SUMMARY.md
@scripts/supabase-preflight.mjs
@supabase/config.toml
@docs/ops/fx-operations.md

<interfaces>
- There is one Supabase project, ref in `SUPABASE_PROD_PROJECT_REF` (.env.local); it is production. NEVER use the Supabase MCP connector (wrong account). Use the Supabase CLI / Management API with `SUPABASE_ACCESS_TOKEN` from .env.local.
- `npm run supabase:db:push` = `node scripts/supabase-preflight.mjs && supabase db push`; extra args after `--` reach `supabase db push` (use `-- --yes` to skip the interactive confirm).
- 00-09 precedent: the sandbox may refuse production deploy commands for an executor agent. If a production command is refused, STOP and return a checkpoint with the exact remaining commands; do not change targets or improvise.
- Vault rows are created via the Management API `POST https://api.supabase.com/v1/projects/<ref>/database/query` with body `{ "query": "select vault.create_secret('<url>', 'fx_monitor_url');" }` and `Authorization: Bearer $SUPABASE_ACCESS_TOKEN`, as 00-09 did for fx_sync_url.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Full local gate, then push the schema to production</name>
  <files>docs/acceptance/phase-01-money-core.md</files>
  <read_first>
    - CLAUDE.md (Supabase access section: CLI only, one production project)
    - scripts/supabase-preflight.mjs (what the preflight asserts)
    - .planning/phases/00-foundation/00-09-SUMMARY.md (how the last production push was run and recorded, secret hygiene)
  </read_first>
  <action>
1. Full local gate (all must pass before any production command):
```bash
npm run lint && npm run typecheck && npm run depcruise && npm run check:ignores && npm run test:coverage && npm run verify:gates \
 && npm run lint:migrations && npm run verify:migrations && npm run check:money-mirror \
 && npx supabase db reset --local && npx supabase test db
```
2. Push, non-interactively, with the token loaded from .env.local and never printed:
```bash
set -a; . ./.env.local >/dev/null 2>&1; set +a   # if .env.local is not shell-sourceable, export only SUPABASE_ACCESS_TOKEN and SUPABASE_DB_PASSWORD via a small node reader that prints nothing
npm run supabase:db:push -- --yes
npx supabase migration list --linked
```
If `.env.local` contains comment-only lines with bare keys (it does), sourcing may fail: use `node -e` to read just SUPABASE_ACCESS_TOKEN and SUPABASE_DB_PASSWORD into the environment of a single child process (e.g. a tiny `scripts`-free inline node spawn) without logging them.
3. Confirm `migration list --linked` shows 20260924000100, 000200, 000300, 000400, 000500, 000600 and 000700 in both Local and Remote columns.
4. Start `docs/acceptance/phase-01-money-core.md` with: date, the gate command and its result, the push output summary (no secrets), and the migration list excerpt.
If the sandbox refuses the push, stop and return a `checkpoint:human-action` listing steps 2-3 verbatim for the user or orchestrator to run (00-09 precedent).
  </action>
  <verify>
    <automated>npx supabase migration list --linked | grep -c "20260924000700"</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase migration list --linked` shows all seven 2026092400xxxx migrations applied remotely
    - `docs/acceptance/phase-01-money-core.md` exists and contains "20260924000700"
    - `git diff --cached | grep -cE "sbp_|SUPABASE_ACCESS_TOKEN=|re_[A-Za-z0-9]{10}"` returns 0 (no secret staged)
  </acceptance_criteria>
  <done>The production schema matches git for every Phase 1 migration.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy Edge Functions, set secrets and Vault, smoke-test live</name>
  <files>docs/acceptance/phase-01-money-core.md</files>
  <read_first>
    - docs/ops/fx-operations.md (01-11: secrets and Vault rows each function needs)
    - supabase/config.toml (verify_jwt per function)
    - .planning/phases/00-foundation/00-09-SUMMARY.md (deploy + Vault + invoke proof format)
  </read_first>
  <action>
1. Deploy: `npx supabase functions deploy fx-sync --no-verify-jwt`, `npx supabase functions deploy fx-monitor --no-verify-jwt`, `npx supabase functions deploy resolve-rate` (JWT verified).
2. Secrets: write a temp env file in the scratchpad (not the repo) containing RESEND_API_KEY, RESEND_FROM_EMAIL and FX_ALERT_TO_EMAIL read from .env.local by a node one-liner that prints nothing; `npx supabase secrets set --env-file <tmp>`; delete the temp file. If RESEND_API_KEY or FX_ALERT_TO_EMAIL is empty in .env.local, stop and return a checkpoint naming the missing key (user_setup). FX_SYNC_SECRET is already set from 00-09; fx-monitor reuses it.
3. Vault: create `fx_monitor_url` = `https://<SUPABASE_PROD_PROJECT_REF>.supabase.co/functions/v1/fx-monitor` via the Management API query endpoint (00-09 pattern). Expect HTTP 201/200.
4. Live smoke (print only non-secret results):
   - POST fx-sync with the x-fx-sync-secret header → `{"ok":true,"source":"frankfurter-v2",...}`; a wrong secret → 403.
   - Management API query: `select count(*) from public.currencies` > 100; `select max(rate_date) from public.fx_rates` is within 4 days; `select count(*) from public.fx_rate_holds`; `select jobname, schedule from cron.job` lists fx-sync-daily and fx-monitor-daily.
   - POST fx-monitor with the secret → `{"ok":true,...}`; a wrong secret → 403.
   - POST resolve-rate without Authorization → 401.
5. Append all results to docs/acceptance/phase-01-money-core.md. Commit with `docs(01-16): record production rollout`.
If any production command is refused by the sandbox, stop and return a checkpoint with the remaining commands verbatim.
  </action>
  <verify>
    <automated>grep -c "fx-monitor-daily" docs/acceptance/phase-01-money-core.md</automated>
  </verify>
  <acceptance_criteria>
    - acceptance doc records fx-sync `ok:true`, fx-monitor `ok:true`, both 403 wrong-secret checks, resolve-rate 401 without auth, currencies count > 100, and both cron jobs
    - `git status --porcelain` shows no temp secrets file and `.env.local` untracked
  </acceptance_criteria>
  <done>All three Edge Functions are live and verified against production.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Device check: offline queue, sync line and locale formatting</name>
  <files>docs/acceptance/phase-01-money-core.md</files>
  <what-built>The Phase 1 data layer wired into the app (QueryProvider, encrypted cache, paused-mutation queue, sync status line, rate credit), with NetInfo newly added as a native module.</what-built>
  <how-to-verify>
    1. Rebuild the dev client because NetInfo is new native code: `npx expo run:android --device Pixel_8_API_36` (or the iPhone XR dev build via EAS once 00-20 lands).
    2. Sign in, open the You tab. Expect a line reading "synced just now" (or "synced N minutes ago") under the connection status, and "Rates By Exchange Rate API" at the foot.
    3. Enable airplane mode. Within a few seconds expect "offline". Kill and reopen the app: the You screen still renders from cache.
    4. With airplane mode still on, tap "Queue a test entry (dev)" on the You screen (dev builds only; added by 01-15) once or twice; expect "offline · 2 changes queued" (account + transaction on the first tap). Force-quit and reopen: the queued count is still there.
    5. Disable airplane mode. Within about 30 seconds expect the queued count to clear and "synced just now". In the Supabase dashboard's table view (read-only look), the transaction exists with rate_source and rate_date filled.
    6. Change the device region to Germany (Deutsch) and back: any rendered amount/date uses the regional format (e.g. 1.234,56 €) and minus signs render as "−".
    7. Sign out: the warning mentions unsaved changes only if something is still queued.
  </how-to-verify>
  <read_first>
    - docs/acceptance/phase-01-money-core.md (Tasks 1-2 results)
    - .planning/phases/01-money-core/01-VALIDATION.md (Manual-Only Verifications table)
  </read_first>
  <action>Present the how-to-verify steps to the user, wait for their result, then append the outcome of each numbered step (pass/fail plus any observed difference) to docs/acceptance/phase-01-money-core.md and commit it. Do not mark SYN-01/SYN-02 verified on device until the user approves.</action>
  <verify>
    <automated>grep -c "Device check" docs/acceptance/phase-01-money-core.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/acceptance/phase-01-money-core.md has a "Device check" section with a result for each of steps 1-7
    - the user's resume signal is "approved", or each reported difference is recorded as a gap for /gsd-plan-phase --gaps
  </acceptance_criteria>
  <done>The user has confirmed offline browse, queued writes surviving a force-quit, flush on reconnect, the sync line, the credit line and regional formatting on a real device.</done>
  <resume-signal>Type "approved", or describe what differed at which step</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → production Supabase | schema push, function deploy, secret writes |
| secrets in .env.local → CLI processes | must never reach logs, git or chat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16-01 | Tampering | wrong-project push | mitigate | `supabase:db:push` chains the preflight ref check; never use the MCP connector (wrong account per CLAUDE.md) |
| T-01-16-02 | Information Disclosure | secret leakage during deploy | mitigate | secrets read by node without printing, temp env file in scratchpad deleted after use, staged-diff grep for token patterns |
| T-01-16-03 | Denial of Service | pushing an unproven schema | mitigate | full local gate (pgTAP, mirror, migration compatibility) must pass first |
| T-01-16-04 | Elevation of Privilege | resolve-rate deployed without JWT check | mitigate | deployed without --no-verify-jwt; smoke test asserts 401 without Authorization |
</threat_model>

<verification>
- `npx supabase migration list --linked` shows all Phase 1 migrations remote
- acceptance doc records live function checks
- user approves the device checkpoint
</verification>

<success_criteria>
Phase 1 is live on production and proven on a device: schema pushed, FX functions running, offline queue and locale formatting confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-money-core/01-16-SUMMARY.md`
</output>
