---
phase: quick-260922-sz4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.local
  - scripts/supabase-preflight.mjs
  - CLAUDE.md
  - docs/ops/accounts.md
autonomous: true
requirements: [SUPA-SINGLE-PROJECT]

must_haves:
  truths:
    - "`npm run supabase:preflight` exits 0 against the real .env.local"
    - "The preflight fails, exit 1, naming both refs, when config.toml and SUPABASE_PROD_PROJECT_REF disagree"
    - "Nothing in scripts/, CLAUDE.md or docs/ops/accounts.md still refers to a dev Supabase project"
    - "The preflight source reads no secret env var"
    - ".env.local is never staged or committed"
  artifacts:
    - path: "scripts/supabase-preflight.mjs"
      provides: "Single-project ref-match preflight"
      contains: "SUPABASE_PROD_PROJECT_REF"
    - path: "CLAUDE.md"
      provides: "Supabase access section describing the single production project"
      contains: "## Supabase access"
    - path: "docs/ops/accounts.md"
      provides: "Accounts status and Supabase narrative for a single production project"
  key_links:
    - from: "package.json"
      to: "scripts/supabase-preflight.mjs"
      via: "supabase:preflight and supabase:db:push scripts"
      pattern: "supabase-preflight\\.mjs"
    - from: "scripts/supabase-preflight.mjs"
      to: "supabase/config.toml"
      via: "project_id extraction"
      pattern: "project_id"
---

<objective>
Repoint the Supabase guardrails from a dev/prod split to a single production
project. `Fincwin United` (ref `cohmcbdfgqmiwykztrdg`) is now the one and only
Supabase cloud project and it is production. `SUPABASE_DEV_PROJECT_REF` no
longer exists and must not be reintroduced.

Purpose: the preflight currently *requires* a dev ref and *refuses* when
config.toml points at the prod ref. Both behaviours now block every legitimate
`db push`. The docs assert a half-finished two-project split that no longer
describes reality.

Output: a ref-match-only preflight, a clean `.env.local`, and CLAUDE.md +
docs/ops/accounts.md rewritten for the single-project reality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>

## Locked user decision — do not re-litigate

The user was shown the trade-off and chose **"simple ref match only"**: assert
that `supabase/config.toml`'s `project_id` equals `SUPABASE_PROD_PROJECT_REF`
and let the push proceed. They **explicitly declined** a production-confirmation
gate.

Therefore the implementation MUST NOT add: a confirmation prompt, a
`--yes`/`I_MEAN_IT` opt-in env flag, an ASCII warning banner, a sleep, or any
other speed bump. A plain factual sentence that the target is production is
fine in a comment or in docs. A lecture, a scare-quote, or a "consider
creating a dev project" aside is not. Do not add one.

## Out of scope — do not touch

- `.planning/PROJECT.md` — its line-186 Key Decision ("Separate development
  and production projects, migrations in git") is now contradicted. Amending a
  recorded Key Decision is the user's call; the orchestrator surfaces it. Do
  not edit this file.
- `supabase/config.toml`'s `project_id` value — already correct, leave it.
- Creating any Supabase project, installing the CLI, `ROADMAP.md`.

## Verified current state (checked at planning time)

- `.env.local` line 113 is `SUPABASE_PROD_PROJECT_REF=cohmcbdfgqmiwykztrdg`.
  The duplicate-key bug described in the briefing (a stale comment plus an
  empty second assignment) is **already gone** — grep finds exactly one
  occurrence. Task 1 is therefore mostly a verification, with one residual
  stale comment to fix. Treat it as idempotent: if a duplicate has reappeared,
  remove it; if not, do not manufacture a change.
- `.env.local` line 119 still reads
  `# Dev project → Settings → API. The publishable (anon) key is safe to ship.`
  — stale wording, fix it.
- `.env.local` IS gitignored (`.gitignore:4`). Edit it; never stage it. `git
  status` showing nothing for it afterwards is CORRECT, not a failure.
- `scripts/supabase-preflight.mjs` is the only file in `scripts/`.
- `package.json` already wires both scripts correctly (lines 67-68):
  `"supabase:preflight": "node scripts/supabase-preflight.mjs"` and
  `"supabase:db:push": "node scripts/supabase-preflight.mjs && supabase db push"`.
  **No package.json change is needed.** Do not edit it.
- `CLAUDE.md` is entirely GSD auto-regenerated marker blocks. `## Supabase
  access` sits at line 197, in the UNMARKED GAP between
  `<!-- GSD:workflow-end -->` (line 195) and `<!-- GSD:profile-start -->`
  (line 208). It MUST stay in that gap — content inside a marker block is
  destroyed on regeneration. Do not move it, do not add markers around it.
- `docs/ops/accounts.md` status table row for Supabase is at ~line 37; the
  `### Supabase — plan 00-03` body is at ~line 66. House style: prose
  paragraphs not bullets, British spelling ("organisation"), hard-wrapped at
  ~78 characters, a `→` env-var trailer at the end of each account section.

<interfaces>
Current `scripts/supabase-preflight.mjs` internals worth keeping verbatim:

```js
const REF_PATTERN = /^[a-z0-9]{20}$/;
function parseEnvFile(contents) { /* strips #-comments, splits on first =, unquotes */ }
function extractProjectId(tomlContents) {
  const match = tomlContents.match(/^\s*project_id\s*=\s*["']([^"']+)["']/m);
  return match ? match[1] : null;
}
const envFilePath = resolve(cwd, process.env.SUPABASE_PREFLIGHT_ENV_FILE || ".env.local");
const configTomlPath = resolve(cwd, "supabase/config.toml");
```

`resolve()` passes absolute paths straight through, so
`SUPABASE_PREFLIGHT_ENV_FILE` accepts an absolute fixture path.
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the preflight to a single-project ref match</name>
  <files>scripts/supabase-preflight.mjs, .env.local</files>
  <action>
**Part A — `.env.local` hygiene (idempotent, never commit).**

Confirm exactly one non-empty `SUPABASE_PROD_PROJECT_REF` line remains and
that `SUPABASE_DEV_PROJECT_REF` appears zero times. If a stale duplicate or a
"fincwin-prod does not exist yet" comment has reappeared, delete it, keeping
the line that carries `cohmcbdfgqmiwykztrdg`.

Fix two stale comments:
- The comment above the ref (currently "The 20-character project refs, from
  each project's URL or Settings → General.") → singular, e.g.
  `# The 20-character project ref, from the project's URL or Settings → General.`
- Line ~119 `# Dev project → Settings → API. ...` → drop "Dev project", e.g.
  `# Project → Settings → API. The publishable (anon) key is safe to ship.`

Do not touch any secret value in this file. Do not `git add` it.

**Part B — rewrite `scripts/supabase-preflight.mjs`.**

Keep: Node builtins only (`node:fs`, `node:path`), no dependencies, the
`REF_PATTERN`, `parseEnvFile`, `extractProjectId` helpers verbatim, and the
`SUPABASE_PREFLIGHT_ENV_FILE` override (the negative test depends on it).

New semantics — exactly this, nothing more:
1. Read `project_id` from `supabase/config.toml`.
2. Read `SUPABASE_PROD_PROJECT_REF` from the env file.
3. Problem if either file is missing, if `project_id` is absent, if either
   value is missing/empty, or if either fails `REF_PATTERN`.
4. Problem if the two valid refs are not equal. The mismatch message MUST name
   both, e.g.
   `project ref mismatch: supabase/config.toml has project_id="<a>", <envFilePath> has SUPABASE_PROD_PROJECT_REF="<b>"`.
5. Any problems → print `SUPABASE PREFLIGHT FAILED`, one `- ` line per
   problem, `process.exit(1)`.
6. Otherwise print a one-line OK naming the ref, `process.exit(0)`.

**Delete entirely:** every reference to `SUPABASE_DEV_PROJECT_REF`, and the
old block that refused when `configRef === prodRef` (old lines ~110-120).
That refusal was the whole bug.

Rewrite the header comment for the new reality. Keep the paragraph explaining
why the CLI path is gated (the MCP connector is authorized against a different
account — see `## Supabase access` in CLAUDE.md) and the "Node builtins only"
paragraph. Update the secret-hygiene paragraph to say the script reads exactly
one key, `SUPABASE_PROD_PROJECT_REF`, and never the access token, any
service-role key, or any DB password. A single plain sentence noting that the
pinned project is production is fine; no warning banner, no confirmation gate
(see the locked decision in `<context>`).

Secret hygiene is a hard gate: the finished source must contain no
`SUPABASE_ACCESS_TOKEN`, `SERVICE_ROLE` or `DB_PASSWORD` literal.
  </action>
  <verify>
    <automated>
      cd "$REPO_ROOT" &&
      test "$(grep -c '^SUPABASE_PROD_PROJECT_REF=.\+' .env.local)" = "1" &&
      test "$(grep -c 'SUPABASE_DEV_PROJECT_REF' .env.local)" = "0" &&
      node scripts/supabase-preflight.mjs &&
      grep -vq -e SUPABASE_ACCESS_TOKEN -e SERVICE_ROLE -e DB_PASSWORD scripts/supabase-preflight.mjs;
      printf 'SUPABASE_PROD_PROJECT_REF=aaaaaaaaaaaaaaaaaaaa\n' > "$TMPDIR/wrong.env" &&
      SUPABASE_PREFLIGHT_ENV_FILE="$TMPDIR/wrong.env" node scripts/supabase-preflight.mjs; test $? -eq 1 &&
      SUPABASE_PREFLIGHT_ENV_FILE="$TMPDIR/wrong.env" node scripts/supabase-preflight.mjs 2>&1 | grep -q 'cohmcbdfgqmiwykztrdg' &&
      SUPABASE_PREFLIGHT_ENV_FILE="$TMPDIR/wrong.env" node scripts/supabase-preflight.mjs 2>&1 | grep -q 'aaaaaaaaaaaaaaaaaaaa' &&
      rm -f "$TMPDIR/wrong.env"
    </automated>
  </verify>
  <done>
    `npm run supabase:preflight` exits 0 against the real `.env.local` and
    prints a ref-match OK line. A fixture with a wrong ref exits 1 and the
    message names both `cohmcbdfgqmiwykztrdg` and the fixture ref. The fixture
    is deleted, and it lived in the scratchpad, not the repo. `grep -c
    SUPABASE_DEV_PROJECT_REF scripts/supabase-preflight.mjs` is 0. No secret
    env-var name appears in the source. `.env.local` has exactly one non-empty
    `SUPABASE_PROD_PROJECT_REF` and is unstaged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite CLAUDE.md's Supabase access section</name>
  <files>CLAUDE.md</files>
  <action>
Edit only the `## Supabase access` section (line ~197), which lives in the
unmarked gap between `<!-- GSD:workflow-end -->` and
`<!-- GSD:profile-start -->`. It must remain in that gap. Do not add GSD
markers, do not move the section, do not touch any marker block.

Keep the first two paragraphs **verbatim in substance**: the MCP prohibition
(different account, org `Ealchapp`, project ref `ogbothupjcivwruesgsu`,
disjoint project lists) and the "use the Supabase CLI / Management API
instead, authenticated with `SUPABASE_ACCESS_TOKEN` from `.env.local`"
paragraph. These are still true.

Replace the four stale bullets with three accurate ones:

```
- There is one Supabase project, `Fincwin United`, ref `cohmcbdfgqmiwykztrdg`, and it is production. The repo pins it in `supabase/config.toml`.
- Its ref lives in `SUPABASE_PROD_PROJECT_REF` in `.env.local`, which is gitignored and never committed. There is no dev ref.
- `supabase db push` targets production. Run `npm run supabase:preflight` first, or use `npm run supabase:db:push`, which chains the check. The preflight asserts that `config.toml`'s `project_id` matches `SUPABASE_PROD_PROJECT_REF`.
```

That removes "Dev ref lives in `SUPABASE_DEV_PROJECT_REF`", the "`fincwin-prod`
does not exist yet" bullet, and "Never run migrations against prod by hand" —
the last being replaced by the plain factual statement that `db push` targets
production. Do not editorialise beyond that sentence.
  </action>
  <verify>
    <automated>
      cd "$REPO_ROOT" &&
      grep -c 'SUPABASE_DEV_PROJECT_REF\|fincwin-dev\|fincwin-prod does not exist' CLAUDE.md | grep -qx 0 &&
      grep -q 'ogbothupjcivwruesgsu' CLAUDE.md &&
      test "$(grep -n '## Supabase access' CLAUDE.md | cut -d: -f1)" -gt "$(grep -n 'GSD:workflow-end' CLAUDE.md | cut -d: -f1)" &&
      test "$(grep -n '## Supabase access' CLAUDE.md | cut -d: -f1)" -lt "$(grep -n 'GSD:profile-start' CLAUDE.md | cut -d: -f1)"
    </automated>
  </verify>
  <done>
    `## Supabase access` still sits after the last `GSD:*-end` marker and
    before `GSD:profile-start`. The MCP prohibition survives with the
    `Ealchapp` / `ogbothupjcivwruesgsu` details intact. No
    `SUPABASE_DEV_PROJECT_REF`, no "fincwin-prod does not exist yet", no
    "Never run migrations against prod by hand". No confirmation-gate language.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update docs/ops/accounts.md for the single production project</name>
  <files>docs/ops/accounts.md</files>
  <action>
Two edits, both preserving house style: prose paragraphs not bullets, British
spelling, hard wrap at ~78 characters, no new headings, no restructuring.

**1. Status table row (~line 37).** Change the Status cell from
`Dev project live; prod not created` to `Production project live`. Leave the
Account, Cost and Blocks cells alone.

**2. `### Supabase — plan 00-03` body (~line 66).** Replace only the sentence
that currently reads "It serves as dev, but renaming it to `fincwin-dev` is
still outstanding, and `fincwin-prod` has not been created, so the two-project
split PROJECT.md calls for is only half done." with a statement that it is the
production project and the only one — the rename and the second project are
**removed as outstanding items, not reworded**. Suggested replacement,
matching the surrounding cadence: "It is the production project, and the only
one."

Also change "Save each database password in a password manager" to the
singular ("Save the database password in a password manager, since Supabase
shows it once"), now that there is one project.

Keep untouched: the us-west-2 region fact and its permanence rationale, the
access-token/Management-API sentence, the Scoop / CLI / Docker Desktop
outstanding items, the `sb_publishable_` / `sb_secret_` key-format and
legacy-JWT-deprecation line, the DPA / SCC / UK-addendum sentence, and the
`## Supabase access` in `CLAUDE.md` cross-reference.

**3. The `→` env-var trailer.** Drop `SUPABASE_DEV_PROJECT_REF` so the trailer
lists only what exists, re-wrapping at ~78 chars:

```
→ `SUPABASE_ACCESS_TOKEN`, `SUPABASE_PROD_PROJECT_REF`,
`SUPABASE_PROD_DB_PASSWORD`, `EXPO_PUBLIC_SUPABASE_URL`,
`EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SERVICE_ROLE_KEY` — the last
keeps its legacy name but holds a new-style secret key.
```
  </action>
  <verify>
    <automated>
      cd "$REPO_ROOT" &&
      grep -c 'SUPABASE_DEV_PROJECT_REF\|fincwin-dev\|fincwin-prod\|serves as dev\|half done' docs/ops/accounts.md | grep -qx 0 &&
      grep -q 'Production project live' docs/ops/accounts.md &&
      grep -q 'us-west-2' docs/ops/accounts.md &&
      grep -q 'Scoop' docs/ops/accounts.md &&
      grep -q 'sb_publishable_' docs/ops/accounts.md &&
      grep -q 'Standard Contractual Clauses' docs/ops/accounts.md &&
      awk 'length > 80 {c++} END {exit (c>1)}' docs/ops/accounts.md
    </automated>
  </verify>
  <done>
    The status row reads "Production project live". The Supabase section says
    the live project is production and the only one, with the `fincwin-dev`
    rename and `fincwin-prod` creation gone rather than reworded. Region, DPA,
    key-format, Scoop/CLI/Docker and CLAUDE.md cross-reference all survive.
    The `→` trailer lists exactly the six vars that exist. Wrapping and
    British spelling unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env.local` → script stdout/stderr | Secrets in the env file could leak into terminal output or CI logs |
| working tree → git history | `.env.local` holds a live access token, DB password and service-role key; committing it is unrecoverable |
| repo → production Supabase | `supabase db push` now writes to the only live database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-sz4-01 | Information disclosure | `scripts/supabase-preflight.mjs` | mitigate | Script reads exactly one key, `SUPABASE_PROD_PROJECT_REF`; Task 1's automated gate greps the source for `SUPABASE_ACCESS_TOKEN`, `SERVICE_ROLE` and `DB_PASSWORD` and fails if present. Project refs are not secret — they appear inside `EXPO_PUBLIC_SUPABASE_URL` — so printing both refs on mismatch is correct |
| T-sz4-02 | Information disclosure | `.env.local` | mitigate | File is gitignored (`.gitignore:4`). Final verification asserts it is absent from `git diff --name-only` and `git status --porcelain`; it is edited but never staged |
| T-sz4-03 | Tampering | production Supabase schema | accept | The user explicitly declined a confirmation gate after being shown the trade-off. Mitigation is the ref-match preflight only: it catches a `config.toml` pointing somewhere unexpected, which is the failure mode that remains reachable now that there is one project |
| T-sz4-04 | Tampering | negative-test fixture | mitigate | Fixture written to the session scratchpad, never the repo, and deleted in the same verify command |
</threat_model>

<verification>
Run from the repo root after all three tasks:

1. `npm run supabase:preflight` → exit 0.
2. `grep -rn "SUPABASE_DEV_PROJECT_REF\|fincwin-dev" scripts/ CLAUDE.md docs/ops/accounts.md` → no matches.
3. `git status --porcelain` → shows `scripts/supabase-preflight.mjs`,
   `CLAUDE.md`, `docs/ops/accounts.md` (plus `.planning/`), and **not**
   `.env.local`, **not** `package.json`, **not** `supabase/config.toml`,
   **not** `.planning/PROJECT.md`.
4. `git diff --name-only | grep -v '^\.planning/' | grep -q '\.env\.local'` →
   must find nothing (exit 1).
5. `git diff supabase/config.toml` → empty.
</verification>

<success_criteria>
- Preflight asserts ref equality only, exits 0 on match, exits 1 naming both
  refs on mismatch.
- No dev-ref requirement and no prod-equality refusal remain anywhere.
- No confirmation prompt, opt-in flag, warning banner or other declined speed
  bump was added.
- `.env.local` has exactly one non-empty `SUPABASE_PROD_PROJECT_REF`, zero dev
  refs, and is uncommitted.
- CLAUDE.md's `## Supabase access` is accurate and still outside every GSD
  marker block, MCP prohibition intact.
- `docs/ops/accounts.md` describes one production project in the file's
  existing voice, with the dev/prod split items removed.
- `.planning/PROJECT.md`, `supabase/config.toml` and `package.json` unchanged.
</success_criteria>

<output>
After completion, create
`.planning/quick/260922-sz4-repoint-supabase-guardrails-to-single-pr/260922-sz4-SUMMARY.md`.

Flag for the orchestrator to surface to the user: `.planning/PROJECT.md` line
186 records the Key Decision "Separate development and production projects,
migrations in git", which this change contradicts. It was left untouched
deliberately — amending a recorded Key Decision is the user's call.
</output>
