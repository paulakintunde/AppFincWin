---
phase: quick-260925-8bi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/brand/fincwin-mark.svg
  - assets/brand/fincwin-app-icon.svg
  - scripts/brand/generate-icons.py
  - assets/icon.png
  - assets/android-icon-foreground.png
  - assets/android-icon-background.png
  - assets/android-icon-monochrome.png
  - assets/splash-icon.png
  - assets/favicon.png
  - app.config.ts
autonomous: true
requirements: [QUICK-260925-8bi]

must_haves:
  truths:
    - "The installed app shows the FincWin 'F' mark (navy stem + arm, coral block on cream), not the Expo placeholder"
    - "Every icon PNG can be regenerated byte-for-byte from one script and one set of geometry constants"
    - "Android launchers render a proper adaptive icon (foreground, background, themed monochrome)"
    - "The iOS icon has no alpha channel, so App Store upload does not reject it"
  artifacts:
    - path: "assets/brand/fincwin-mark.svg"
      provides: "Master glyph vector, transparent, viewBox 0 0 480 480"
      contains: "#FF6F61"
    - path: "assets/brand/fincwin-app-icon.svg"
      provides: "Glyph on full-bleed cream square, viewBox 0 0 1024 1024"
      contains: "#F7F4E9"
    - path: "scripts/brand/generate-icons.py"
      provides: "Re-runnable PNG generator with geometry constants and size/mode assertions"
    - path: "assets/icon.png"
      provides: "1024x1024 RGB app icon"
    - path: "app.config.ts"
      provides: "android.adaptiveIcon wiring"
      contains: "adaptiveIcon"
  key_links:
    - from: "app.config.ts"
      to: "assets/android-icon-*.png"
      via: "android.adaptiveIcon { foregroundImage, backgroundImage, monochromeImage }"
      pattern: "foregroundImage: './assets/android-icon-foreground.png'"
    - from: "scripts/brand/generate-icons.py"
      to: "assets/*.png"
      via: "single GLYPH geometry table rendered at every size"
      pattern: "STEM|ARM|BLOCK"
---

<objective>
Replace the Expo placeholder icon set with the FincWin "F" mark, redrawn as exact vector geometry (never an upscale of the user's screenshot), and wire the Android adaptive icon into app.config.ts.

Purpose: The app currently ships Expo's default icons. The brand mark needs to be pixel-clean at every size and regenerable from source.
Output: two master SVGs, one Python generator, six regenerated PNGs, adaptiveIcon wiring, a contact-sheet preview for human review.

Brand-colour note (must be repeated in SUMMARY for the user): navy #172A4F, coral #FF6F61 and cream #F7F4E9 are brand-mark colours that sit OUTSIDE the §2 in-app token palette. They are confined to icon/brand assets only. No in-app UI token, theme file or component changes in this task. The splash background stays #FBFAF7 (the §2 canvas colour) — do not change it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@app.config.ts

<interfaces>
Master geometry — normalised 480-unit glyph box, origin at glyph top-left (measured pixel-exactly from the user's logo; do NOT re-derive, do NOT open the screenshot to trace):
- STEM  (navy #172A4F): x 0–152,   y 0–480, radius 68 on top-left AND bottom-left only; right side square.
- ARM   (navy #172A4F): x 184–480, y 0–152, radius 68 on top-right only; other corners square.
- BLOCK (coral #FF6F61): x 184–398, y 192–332, no radius.
Colours: navy #172A4F, coral #FF6F61, cream #F7F4E9. White #FFFFFF for monochrome.

Equivalent SVG paths (480 box):
- Stem:  M68 0 H152 V480 H68 A68 68 0 0 1 0 412 V68 A68 68 0 0 1 68 0 Z
- Arm:   M184 0 H412 A68 68 0 0 1 480 68 V152 H184 Z
- Block: <rect x="184" y="192" width="214" height="140"/>

From node_modules/@expo/config-types/build/ExpoConfig.d.ts (SDK 57, verified during planning — all three keys exist):
```ts
android?: {
  adaptiveIcon?: {
    foregroundImage?: string;
    monochromeImage?: string;
    backgroundImage?: string;   // overrides backgroundColor; must match foreground dimensions
    backgroundColor?: string;
  };
}
```

Current app.config.ts line 21:
```ts
android: { package: APP_ID }, // edgeToEdgeEnabled removed from @expo/config-types in SDK 57: ...
```

Tooling: Python 3.14 + Pillow 12.2 available. `ImageDraw.rounded_rectangle(xy, radius, fill, corners=(tl, tr, br, bl))`. No new npm dependencies.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Master SVGs and re-runnable icon generator</name>
  <files>assets/brand/fincwin-mark.svg, assets/brand/fincwin-app-icon.svg, scripts/brand/generate-icons.py, assets/icon.png, assets/android-icon-foreground.png, assets/android-icon-background.png, assets/android-icon-monochrome.png, assets/splash-icon.png, assets/favicon.png</files>
  <action>
1. Write `assets/brand/fincwin-mark.svg`: `viewBox="0 0 480 480"`, width/height 480, no background, the three shapes from the interfaces block (stem + arm fill #172A4F, block fill #FF6F61). No strokes, no gradients, no filters, no shadows.

2. Write `assets/brand/fincwin-app-icon.svg`: `viewBox="0 0 1024 1024"`, a full-bleed `<rect width="1024" height="1024" fill="#F7F4E9"/>` (square corners — the OS applies its own mask), then `<g transform="translate(272 272)">` containing the same three shapes (glyph 480 wide centred: (1024-480)/2 = 272).

3. Write `scripts/brand/generate-icons.py` (Pillow only, stdlib otherwise). Structure:
   - Header docstring: what it generates, how to run (`python scripts/brand/generate-icons.py [--preview PATH]`), and that geometry constants mirror assets/brand/*.svg — change both together.
   - Constants: `NAVY = "#172A4F"`, `CORAL = "#FF6F61"`, `CREAM = "#F7F4E9"`, `GLYPH_UNITS = 480`, `RADIUS = 68`, and a shape table: `STEM = (0, 0, 152, 480, (True, False, False, True))`, `ARM = (184, 0, 480, 152, (False, True, False, False))`, `BLOCK = (184, 192, 398, 332, None)`.
   - `render_glyph(size_px, colours) -> RGBA Image`: draw on a canvas of `size_px * SS` (SS = 8 for sizes < 64 px, else 4), scale every coordinate and RADIUS by `size_px * SS / GLYPH_UNITS`, use `rounded_rectangle(..., corners=...)` for stem/arm and `rectangle` for block, then `resize((size_px, size_px), Image.LANCZOS)`. `colours` lets monochrome pass white for all three shapes.
   - `compose(canvas_px, glyph_px, bg=None, tile_radius=None) -> RGBA`: transparent (or solid `bg`) canvas; if `tile_radius` given, draw a cream rounded tile at supersample instead of a flat fill; alpha-composite the glyph centred at `((canvas_px - glyph_px) // 2)`. Do the tile at supersample and downscale too so its edge is antialiased.
   - Outputs (paths relative to repo root, resolve root from `__file__`):
     - `assets/icon.png`: 1024, cream full-bleed, glyph 480 centred, then `.convert("RGB")` — MUST be RGB, no alpha.
     - `assets/android-icon-background.png`: 1024 RGBA, solid cream, fully opaque.
     - `assets/android-icon-foreground.png`: 1024 RGBA transparent, navy/coral glyph at 340 px centred (≈33% of canvas, well inside the 66dp-of-108dp safe circle = 626 px diameter; the glyph's diagonal 340·√2 ≈ 481 px fits).
     - `assets/android-icon-monochrome.png`: 1024 RGBA transparent, glyph at 340 px, all three shapes white #FFFFFF opaque.
     - `assets/splash-icon.png`: 1024 RGBA transparent, glyph at 800 px centred (splash plugin scales it to imageWidth 160; background colour stays in config as #FBFAF7).
     - `assets/favicon.png`: 48 RGBA, cream rounded tile full canvas with corner radius round(0.22 × 48) = 11, glyph at round(0.47 × 48) = 23 px centred.
   - After writing, re-open each file and `assert` size and mode: icon.png `(1024,1024)` mode `"RGB"`; the other five mode `"RGBA"` at their stated sizes. Also assert android-icon-background.png's alpha extrema == (255, 255) and the foreground's corner pixel alpha == 0. Print one line per file on success; exit non-zero on any failure.
   - `--preview PATH` (optional): build a contact sheet (cream-free neutral grey #DDDDDD background) showing: icon.png at 256 with an iOS-style rounded mask (~22.37% radius) applied for preview only; icon.png raw; Android foreground composited on background with a circular mask at 256, plus a squircle-ish rounded-square mask; the 66/108 safe-circle outline drawn over a 256 copy of the composite; monochrome on a dark #333333 tile; splash-icon on a #FBFAF7 tile; favicon at 1× and 4× (nearest-neighbour). Label each cell with `ImageDraw.text` default font. Save to PATH. The preview is never written under assets/.

4. Run: `python scripts/brand/generate-icons.py --preview .planning/quick/260925-8bi-replace-placeholder-app-icons-with-fincw/icon-preview.png`. Open the preview with the Read tool and eyeball: F reads correctly (stem left, arm top-right, coral block below arm), gaps visible, only stem TL/BL and arm TR corners rounded, glyph centred.

Do not trace, sample or resample the source screenshot anywhere in the script — geometry comes solely from the constants.
  </action>
  <verify>
    <automated>python scripts/brand/generate-icons.py --preview .planning/quick/260925-8bi-replace-placeholder-app-icons-with-fincw/icon-preview.png && python -c "from PIL import Image; i=Image.open('assets/icon.png'); assert i.mode=='RGB' and i.size==(1024,1024); print('ok')"</automated>
  </verify>
  <done>Both SVGs exist with the exact interface geometry; the script runs clean twice in a row producing identical files (`git diff --stat` shows no change on the second run); all six PNGs pass the size/mode asserts; icon-preview.png exists in the quick dir and shows a correct F.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Android adaptive icon and verify config</name>
  <files>app.config.ts</files>
  <action>
Edit line 21 of app.config.ts so the android block becomes:
```ts
android: {
  package: APP_ID,
  adaptiveIcon: {
    foregroundImage: './assets/android-icon-foreground.png',
    backgroundImage: './assets/android-icon-background.png',
    monochromeImage: './assets/android-icon-monochrome.png',
  },
}, // edgeToEdgeEnabled removed ... (keep the existing comment text)
```
Keys verified against SDK 57 `@expo/config-types` during planning (all three exist; backgroundImage overrides backgroundColor and matches the 1024 foreground dimensions). Leave `icon: './assets/icon.png'` and the expo-splash-screen plugin entry untouched — backgroundColor stays '#FBFAF7', imageWidth stays 160. Do not touch any theme/token file.

Then run `npx tsc --noEmit` and `npx expo config --type public` and confirm the output contains `adaptiveIcon` with all three image paths. Run the project's lint (`npm run lint` if the script exists) to confirm nothing else regressed.

Commit on the current branch `chore/app-icon` (do not switch branches; main is protected, lands via PR later): stage the two SVGs, the script, the six PNGs and app.config.ts — NOT icon-preview.png. Message: `chore(brand): replace placeholder icons with FincWin F mark`.

SUMMARY must flag for the user: (a) navy/coral/cream are brand-mark colours outside the §2 palette, confined to icon assets; (b) the icon change only reaches devices via a new native build (EAS dev/prod build) — OTA updates cannot change the app icon; (c) where the contact sheet lives for review.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx expo config --type public | grep -E "foregroundImage|backgroundImage|monochromeImage"</automated>
  </verify>
  <done>tsc passes; `expo config --type public` lists all three adaptiveIcon paths; splash config unchanged; commit exists on chore/app-icon containing exactly the 10 files in files_modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Static asset generation and build config only; no user input, network, or data handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8bi-01 | T (Tampering) | assets/icon.png | mitigate | Script asserts mode RGB so an alpha-channel icon cannot silently reach App Store submission (rejection risk per CLAUDE.md submission-hygiene note) |
| T-8bi-02 | D (DoS / availability) | app.config.ts | accept | Misnamed adaptiveIcon key would only break prebuild; covered by tsc + expo config check |
</threat_model>

<verification>
- `python scripts/brand/generate-icons.py` exits 0 and is idempotent.
- `npx tsc --noEmit` passes.
- `npx expo config --type public` shows adaptiveIcon foreground/background/monochrome.
- icon-preview.png in the quick dir reviewed visually.
- `git grep -n "#172A4F\|#FF6F61\|#F7F4E9" -- src app ui theme 2>/dev/null` finds no new in-app usage (brand colours confined to assets/brand and the script).
</verification>

<success_criteria>
- Placeholder icons replaced by the vector-exact F mark at every size.
- icon.png is 1024 RGB with no alpha; Android adaptive set is 1024 RGBA and wired in config.
- Splash background colour unchanged (#FBFAF7).
- Everything regenerable from `scripts/brand/generate-icons.py`.
</success_criteria>

<output>
After completion, create `.planning/quick/260925-8bi-replace-placeholder-app-icons-with-fincw/260925-8bi-SUMMARY.md`
</output>
