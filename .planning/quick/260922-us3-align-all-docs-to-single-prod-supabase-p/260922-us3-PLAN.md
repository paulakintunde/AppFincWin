---
quick_id: 260922-us3
type: execute
mode: quick
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/phases/00-foundation/00-01-PLAN.md
  - .planning/phases/00-foundation/00-03-PLAN.md
  - .planning/phases/00-foundation/00-14-PLAN.md
  - .planning/phases/00-foundation/00-CONTEXT.md
  - .planning/phases/00-foundation/00-RESEARCH.md
  - .planning/phases/00-foundation/00-DISCUSSION-LOG.md
autonomous: true
requirements: [ENV-17, ENV-18, ENV-04, ENV-05]

must_haves:
  truths:
    - "PROJECT.md's Key Decisions table records one Supabase project, not two, and still records migrations in git"
    - "No document under .planning/ outside .planning/quick/ names a Supabase dev project as a thing that exists"
    - "The recorded region for D-22 reads US West / us-west-2 everywhere it is stated as current fact"
    - "00-DISCUSSION-LOG.md still records that US East was the choice made in that conversation, with a revision note beneath it"
    - "00-14-PLAN.md is executable: every EAS environment draws Supabase values from .env.local"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Amended Key Decisions rows 185 and 186"
      contains: "migrations in git"
    - path: ".planning/phases/00-foundation/00-01-PLAN.md"
      provides: "Single Supabase project row in the dependency register"
    - path: ".planning/phases/00-foundation/00-14-PLAN.md"
      provides: "EAS environment variable steps sourced from .env.local only"
    - path: ".planning/phases/00-foundation/00-DISCUSSION-LOG.md"
      provides: "D-22 revision note preserving the original conversation record"
  key_links:
    - from: ".planning/PROJECT.md"
      to: "CLAUDE.md ## Supabase access"
      via: "same single-project, preflight-guarded statement"
      pattern: "supabase:preflight"
    - from: ".planning/phases/00-foundation/00-03-PLAN.md"
      to: "docs/ops/accounts.md"
      via: "publishable/secret keys only, no legacy anon JWT"
      pattern: "sb_publishable_"
---

<objective>
Align every remaining planning document to the verified reality: one Supabase project,
`Fincwin United` (`cohmcbdfgqmiwykztrdg`), region `us-west-2`, and it is production.

Purpose: seven phase plans and ROADMAP.md were already collapsed to single-prod. The
documents listed here were missed, so the project currently contradicts itself on whether
a dev project exists and on which region was chosen. That contradiction costs time every
session that reads them.

Output: seven edited markdown files. No code, no config, no new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/00-foundation/00-01-PLAN.md
@.planning/phases/00-foundation/00-03-PLAN.md
@.planning/phases/00-foundation/00-14-PLAN.md
@.planning/phases/00-foundation/00-CONTEXT.md
@.planning/phases/00-foundation/00-RESEARCH.md
@.planning/phases/00-foundation/00-DISCUSSION-LOG.md

<facts>
Verified live. Treat as settled; do not re-verify, do not hedge.

- One Supabase project exists and will exist: `Fincwin United`, ref `cohmcbdfgqmiwykztrdg`,
  region `us-west-2`. It is production. There is no dev project.
- `.env.local` (gitignored) holds: `SUPABASE_ACCESS_TOKEN`, `SUPABASE_PROD_PROJECT_REF`,
  `SUPABASE_DB_PASSWORD`, `EXPO_PUBLIC_SUPABASE_URL`, `EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY`,
  `SUPABASE_SERVICE_ROLE_KEY`.
- `.env.production.local` does not exist and no plan creates it. Its entries in `.gitignore:26`
  and `00-02-PLAN.md:165` are harmless and stay.
- Both keys in use are new-style (`sb_publishable_`, `sb_secret_`). `docs/ops/accounts.md`
  forbids minting legacy `anon`/`service_role` JWTs, deprecated end of 2026.
- `CLAUDE.md` `## Supabase access` and `docs/ops/accounts.md` `### Supabase` are already
  correct. They are the canonical wording to match. Do not edit either.
</facts>

<do_not_touch>
Editing any of these fails the task:
- Anything under `.planning/quick/` — historical records of completed work. They legitimately
  mention `us-east-1` and `fincwin-dev`.
- `CLAUDE.md`, `docs/ops/accounts.md`, `.env.local`, `.gitignore`,
  `.planning/phases/00-foundation/00-02-PLAN.md`, `scripts/supabase-preflight.mjs`,
  `supabase/config.toml`, `.planning/ROADMAP.md`, and the seven already-collapsed phase
  plans beyond the exact lines named in the tasks below.

Preserve in every edited file: frontmatter where present (the three PLAN files have it; PROJECT.md,
00-CONTEXT.md, 00-RESEARCH.md and 00-DISCUSSION-LOG.md open with an H1 instead), table
structure and column count, decision IDs
(D-22, ENV-18, ENV-04, T-* threat IDs) and heading levels. Renumber nothing.

Add no warnings, no confirmation gates, no editorialising about production risk. The user
has decided. State facts plainly.
</do_not_touch>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend the Key Decisions table in PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <action>
Two rows in the `| Decision | Rationale | Status |` table contradict reality. Edit both in
place. Keep the three-column structure and the trailing `| — Pending |` status cell exactly
as it is on every other row.

1. **Line 186**, currently:
   `| Separate development and production projects, migrations in git | Changing production by hand is the fastest way to lose data nobody else has a copy of | — Pending |`

   Replace the whole row with:
   `| One Supabase project, migrations in git | There is a single project, Fincwin United (us-west-2), and it is production. The safety property is the migration path, not a second project: schema reaches the database only through supabase/migrations/*.sql in git, guarded by the supabase:preflight ref check. No dashboard SQL edits to schema, ever | — Pending |`

   The migrations-in-git half of the old decision survives intact — it is the remaining
   safety property. Only the two-project half changes.

2. **Line 185**, currently ends:
   `... and keeps migrations in git. A free cloud project covers on-device testing, since a phone cannot easily reach the laptop`

   Change that last sentence to:
   `The single production project covers on-device testing, since a phone cannot easily reach the laptop`

   Leave the rest of the row untouched.

3. Lines 183 and 184 (Pro tier, not self-hosted, not free) remain correct as written. Do
   not touch them. No other row in the table asserts a dev/prod split or a region — a grep
   for `dev project`, `development project`, `staging` and `region` returns only line 185.
   Re-run that grep after editing to confirm nothing was missed.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c "Separate development and production projects" .planning/PROJECT.md | grep -qx 0 && grep -q "One Supabase project, migrations in git" .planning/PROJECT.md && grep -q "supabase:preflight" .planning/PROJECT.md && ! grep -qi "free cloud project covers on-device" .planning/PROJECT.md && echo PASS</automated>
  </verify>
  <done>PROJECT.md no longer asserts separate dev and production projects, still asserts migrations in git, and names the preflight ref check as the guard. Table structure and every other row unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Make the three phase plans executable against one project</name>
  <files>.planning/phases/00-foundation/00-01-PLAN.md, .planning/phases/00-foundation/00-03-PLAN.md, .planning/phases/00-foundation/00-14-PLAN.md</files>
  <action>
**00-01-PLAN.md, dependency register, lines 95-96.** Two rows currently exist:

```
- Supabase dev project | on-device testing | pending | company org in Supabase (D-08) | — | Phase 0 | 2026-09-22 | Region US East (D-22); plan 00-03
- Supabase prod project | real user data | pending | company org in Supabase | — | Phase 0 (Free), Pro by Phase 2 (ENV-16) | 2026-09-22 | plan 00-03
```

Collapse them into one row, keeping the register's eight-field pipe structure and the
`2026-09-22` review date:

```
- Supabase project (Fincwin United, production) | real user data and on-device testing | provisioned | company org in Supabase (D-08) | — | Phase 0 (Free), Pro by Phase 2 (ENV-16) | 2026-09-22 | Region US West, us-west-2 (D-22); ref cohmcbdfgqmiwykztrdg; plan 00-03
```

Status is `provisioned`, not `pending` — the project already exists. Leave every other row
in the register alone.

**00-03-PLAN.md, line 99.** Currently:
`6. \`supabase projects api-keys --project-ref "$SUPABASE_PROD_PROJECT_REF" -o json\`. Use the publishable key (\`sb_publishable_...\`) if present, otherwise the \`anon\` key. Take the \`service_role\` / secret key too.`

Drop the legacy fallback. The publishable key exists and `docs/ops/accounts.md` forbids
minting legacy JWTs, which are deprecated at the end of 2026. Replace the second and third
sentences with:
`Use the publishable key (\`sb_publishable_...\`) and the secret key (\`sb_secret_...\`). Do not mint or use the legacy \`anon\` / \`service_role\` JWTs (docs/ops/accounts.md).`

Keep the step number `6.` and the command itself unchanged. Leave steps 5, 7, 8 and 9 alone
— they were already corrected to single-prod and to `us-west-2`.

**00-14-PLAN.md, step 3, lines 109-111.** Three bullets currently split Supabase values
across a dev project and a non-existent `.env.production.local`. Keep the EAS environment
names development / preview / production — those are EAS build profiles and remain
legitimate. Only the Supabase side collapses. Replace the three bullets with:

```
   - All three environments (development, preview, production) point at the same Supabase project; there is only one, and it is production. Every Supabase value comes from `.env.local`.
   - development and preview: EXPO_PUBLIC_APP_ENV (development/preview), EXPO_PUBLIC_SUPABASE_URL and EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY (from .env.local), EXPO_PUBLIC_APPLE_SIGNIN_ENABLED=false, EXPO_PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com, EXPO_PUBLIC_POSTHOG_KEY (if 00-13 has run; otherwise record it as a follow-up for 00-19), EAS_PROJECT_ID, EXPO_OWNER. Use plaintext visibility.
   - production: the same keys with the same values from `.env.local`, with EXPO_PUBLIC_APP_ENV=production.
   - SUPABASE_SERVICE_ROLE_KEY (the single value in `.env.local`) in all three environments with `--visibility secret` and no EXPO_PUBLIC_ prefix (ENV-04). No app code reads it. It exists so the EAS side of the project holds the key for Edge Function deploys from EAS Workflows later. Say this in eas-environments.md.
```

The `- Google client IDs are added by 00-15.` bullet that follows stays as it is. Do not
touch the eas.json block above (lines 100-106), step 4, or the ota-policy content in step 5.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ! grep -q "env.production.local" .planning/phases/00-foundation/00-14-PLAN.md && ! grep -q "dev project" .planning/phases/00-foundation/00-14-PLAN.md && ! grep -qi "Supabase dev project" .planning/phases/00-foundation/00-01-PLAN.md && grep -q "us-west-2 (D-22)" .planning/phases/00-foundation/00-01-PLAN.md && ! grep -q "otherwise the \`anon\` key" .planning/phases/00-foundation/00-03-PLAN.md && grep -q "sb_secret_" .planning/phases/00-foundation/00-03-PLAN.md && echo PASS</automated>
  </verify>
  <done>The register lists one Supabase project marked provisioned in us-west-2; 00-03 takes publishable and secret keys with no legacy fallback; 00-14 sources every Supabase value from .env.local across all three EAS environments and never mentions .env.production.local.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the D-22 region references, annotate the D-22 history</name>
  <files>.planning/phases/00-foundation/00-CONTEXT.md, .planning/phases/00-foundation/00-RESEARCH.md, .planning/phases/00-foundation/00-DISCUSSION-LOG.md</files>
  <action>
D-22 is recorded as "US East" in four places. Reality and the user's ratified decision is
`us-west-2` / US West. Two of these files are operative references that future sessions read
as current fact; one is a log of a conversation that actually happened. Handle them
differently.

**Correct — these are operative references:**

1. `00-CONTEXT.md:48` — `- **D-22:** **Supabase region: US East**, because launch users are mostly North American. ...`
   Change to `**Supabase region: US West (us-west-2)**`. Leave the rest of the bullet
   (North American launch users, ENV-18 reasoning, GDPR/DPA/SCC sentence) exactly as it is.

2. `00-RESEARCH.md:40` — `- D-22: **Supabase region: US East** (launch users mostly North American). ...`
   Change to `**Supabase region: US West (us-west-2)**`. Rest of the line unchanged.

3. `00-RESEARCH.md:87` — table cell `| ENV-18 | Supabase region deliberate, reasoning recorded | D-22 — US East |`
   Change the last cell to `D-22 — US West (us-west-2)`. Keep the three-column structure.

4. `00-RESEARCH.md:226` — ASCII box line
   `│  Supabase (remote, source of truth) — US East region (D-22)      │ │   │`
   Change `US East` to `US West`. Both are seven characters, so the box borders stay aligned.
   Do not reflow or re-pad the line; change only those two words. After editing, confirm the
   trailing `│ │   │` sits in the same column as the lines above and below it.

**Annotate — this is history:**

5. `00-DISCUSSION-LOG.md:64` — the row `| Supabase region | EU London / EU Frankfurt / US East | US East |` records what was actually chosen in that conversation, from those options. Do not falsify it. Leave the row verbatim.

   Add a revision note immediately beneath that table (after the last row of the
   "Quality gates & infra" table, before the `---` that follows it), as a single italic line:

   `*Revised 2026-09-22: D-22 was changed to US West (us-west-2). The project was created in that region and the decision was ratified. The row above records the original choice.*`

   Do not change the table, its headings, or any other row.

**Final sweep.** Run the gate grep below. The only surviving match anywhere under
`.planning/` outside `.planning/quick/` must be the two on the 00-DISCUSSION-LOG.md history
row — the options list and the selected cell. Anything else is a miss; fix it the same way.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -rn "US East\|us-east" .planning/ --include=*.md | grep -v "^.planning/quick/" | grep -v "^.planning/phases/00-foundation/00-DISCUSSION-LOG.md:" | wc -l | grep -qx 0 && grep -q "US West (us-west-2)" .planning/phases/00-foundation/00-CONTEXT.md && grep -c "US West" .planning/phases/00-foundation/00-RESEARCH.md | grep -qx 3 && grep -q "EU London / EU Frankfurt / US East | US East" .planning/phases/00-foundation/00-DISCUSSION-LOG.md && grep -q "Revised 2026-09-22: D-22 was changed to US West" .planning/phases/00-foundation/00-DISCUSSION-LOG.md && echo PASS</automated>
  </verify>
  <done>The three operative references read US West (us-west-2); the discussion log still shows US East as the choice made that day, with a dated revision note beneath the table; no other US East reference survives outside .planning/quick/.</done>
</task>

</tasks>

<verification>
Run all five gates from the repo root after the three tasks complete.

```bash
cd "$(git rev-parse --show-toplevel)"

# (a) zero US East / us-east outside .planning/quick/ and outside the annotated history line
grep -rn "US East\|us-east" .planning/ --include=*.md \
  | grep -v "^.planning/quick/" \
  | grep -v "^.planning/phases/00-foundation/00-DISCUSSION-LOG.md:" \
  | wc -l   # must be 0

# (b) PROJECT.md: no dev/prod split, migrations in git survives
grep -c "Separate development and production projects" .planning/PROJECT.md   # must be 0
grep -c "One Supabase project, migrations in git" .planning/PROJECT.md        # must be 1

# (c) .env.production.local gone from 00-14
grep -c "env.production.local" .planning/phases/00-foundation/00-14-PLAN.md   # must be 0

# (d) nothing under .planning/quick/ was touched
git diff --name-only | grep "^.planning/quick/" | wc -l                       # must be 0

# (e) headers intact: the three PLAN files keep a closed YAML frontmatter block;
#     PROJECT.md, CONTEXT, RESEARCH and DISCUSSION-LOG have no frontmatter and open
#     with an H1 — for those, assert the first line is byte-identical to HEAD.
for f in .planning/phases/00-foundation/00-01-PLAN.md \
  .planning/phases/00-foundation/00-03-PLAN.md \
  .planning/phases/00-foundation/00-14-PLAN.md; do
  head -1 "$f" | grep -q '^---$' \
    && [ "$(head -60 "$f" | grep -c '^---$')" -ge 2 ] \
    && echo "OK frontmatter $f" || echo "FRONTMATTER BROKEN $f"
done

for f in .planning/PROJECT.md \
  .planning/phases/00-foundation/00-CONTEXT.md \
  .planning/phases/00-foundation/00-RESEARCH.md \
  .planning/phases/00-foundation/00-DISCUSSION-LOG.md; do
  [ "$(head -1 "$f")" = "$(git show HEAD:"$f" | head -1)" ] \
    && echo "OK h1 unchanged $f" || echo "H1 CHANGED $f"
done
```

Gate (d) note: the plan file itself lives under `.planning/quick/` and is committed as part
of this work. The gate concerns *edits to pre-existing* files there — if `git diff --name-only`
lists any path under `.planning/quick/` other than this plan's own directory, stop.

Then eyeball two things a grep cannot catch:
- `00-RESEARCH.md` around line 226: the ASCII diagram borders still line up.
- `git diff --stat` shows seven files and no unexpected eighth.
</verification>

<success_criteria>
- PROJECT.md records one Supabase project and still records migrations in git, guarded by the preflight ref check.
- The dependency register in 00-01 lists a single provisioned production project in us-west-2.
- 00-03 takes only new-style publishable and secret keys.
- 00-14 is executable: one Supabase project, all values from `.env.local`, EAS profile names preserved.
- D-22 reads US West (us-west-2) in every operative reference; the discussion log preserves the original choice with a dated revision note.
- All five verification gates pass.
- No file outside the seven in `files_modified` is changed.
</success_criteria>

<output>
Commit with:
`docs(quick-260922-us3): align remaining docs to single production Supabase project`

No SUMMARY file required for quick mode unless the executor hits a conflict between these
instructions and the file contents — in which case stop and report rather than improvise.
</output>
