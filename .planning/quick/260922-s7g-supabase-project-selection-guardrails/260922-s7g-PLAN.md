---
phase: quick
quick_id: 260922-s7g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - supabase/config.toml
  - scripts/supabase-preflight.mjs
  - package.json
  - .env.local
autonomous: true
requirements: []

must_haves:
  truths:
    - "A fresh Claude session reading CLAUDE.md is told the Supabase MCP connector points at a different account and must not be used for this project"
    - "The repo itself declares which Supabase project it belongs to, in a git-tracked file"
    - "Running the preflight with a mismatched dev ref exits non-zero and names both refs it compared"
    - "Running the preflight with the correct dev ref exits zero"
    - ".env.local carries the real dev ref and no placeholder prod ref"
    - "No secret value is printed by the preflight script under any outcome"
  artifacts:
    - path: "CLAUDE.md"
      provides: "## Supabase access section, outside all GSD marker blocks"
      contains: "Supabase access"
    - path: "supabase/config.toml"
      provides: "project_id pinned to the one live FincWin project"
      contains: "cohmcbdfgqmiwykztrdg"
    - path: "scripts/supabase-preflight.mjs"
      provides: "Ref-match assertion, exits non-zero on mismatch"
      min_lines: 50
    - path: "package.json"
      provides: "supabase:preflight and supabase:db:push script entries"
      contains: "supabase:preflight"
  key_links:
    - from: "scripts/supabase-preflight.mjs"
      to: "supabase/config.toml"
      via: "reads project_id"
      pattern: "config\\.toml"
    - from: "scripts/supabase-preflight.mjs"
      to: ".env.local"
      via: "reads SUPABASE_DEV_PROJECT_REF"
      pattern: "SUPABASE_DEV_PROJECT_REF"
    - from: "package.json"
      to: "scripts/supabase-preflight.mjs"
      via: "npm script, chained before supabase db push"
      pattern: "supabase-preflight"
---

<objective>
Stop any agent or session from operating on the wrong Supabase database.

Three guardrails, weakest coupling first:

1. **CLAUDE.md section** — loads into every session automatically, so it steers tool choice before any tool is reached for. It cannot reconfigure the MCP connector; it can only tell the agent not to use it.
2. **`supabase/config.toml`** — the repo declares its own project ref, committed to git, so the answer travels with the checkout.
3. **Preflight script** — a machine check that fails loudly before `supabase db push` if the pinned ref and `.env.local`'s dev ref disagree.

Purpose: the Supabase MCP connector available in Claude sessions is authorized against a **different Supabase account** — org `Ealchapp`, project ref `ogbothupjcivwruesgsu`. It cannot see the FincWin project at all; the two accounts' project lists are disjoint, verified live. An agent that reaches for it operates silently on an unrelated database.

Output: one committed docs section, one committed config file, one committed Node script plus its package.json wiring, and two corrected env vars that are deliberately not committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@docs/ops/accounts.md

## Ground truth — established by live testing, do not re-derive

| Fact | Value |
|---|---|
| The one live FincWin Supabase project | name `Fincwin United`, ref `cohmcbdfgqmiwykztrdg`, region `us-west-2`, `ACTIVE_HEALTHY` |
| Its `public` schema | empty — no tables, no migrations applied |
| `fincwin-prod` | does not exist yet. Creating it is OUT OF SCOPE |
| Supabase MCP connector in Claude sessions | authorized against a different account: org `Ealchapp`, project `ealch-app's Project`, ref `ogbothupjcivwruesgsu`. Cannot see the FincWin project. Disjoint project lists, verified |
| The working path | Supabase CLI / Management API with `SUPABASE_ACCESS_TOKEN` from `.env.local` — verified, Management API returns HTTP 200 and lists `Fincwin United` |
| Key format in `.env.local` | new-style `sb_publishable_` / `sb_secret_`, both verified valid. Legacy `anon` / `service_role` JWTs are deprecated end of 2026 |
| `.env.local` | gitignored at `.gitignore:4`. Never commit it |
| Supabase CLI | not installed, `supabase` not on PATH. No `supabase/` directory exists. Docker Desktop not running |
| Platform | Windows 11, Node/Expo project. The preflight must be a Node script, not bash |

## Two corrections found while reading the repo

1. **`SUPABASE_DEV_PROJECT_REF` is absent from `.env.local` entirely** — not present-but-empty. `grep DEV_PROJECT_REF .env.local` returns nothing. The line must be **added**, not edited. It belongs directly above the existing `SUPABASE_PROD_PROJECT_REF` at line 113, under the comment "The 20-character project refs, from each project's URL or Settings → General."
2. **`CLAUDE.md` is entirely GSD-managed marker blocks** (`GSD:project-start` through `GSD:profile-end`). Anything written inside a block is destroyed the next time that block regenerates from its source file. The new section must go in the **unmarked gap between the `GSD:workflow-end` and `GSD:profile-start` markers** — currently two blank lines, near line 198.

## Already true, do not redo

`.gitignore` already carries `supabase/.temp/` and `supabase/.branches/` and nothing else under `supabase/`. `supabase/config.toml` is therefore committable as-is. No gitignore change needed.

## Out of scope

Creating `fincwin-prod`. Installing the Supabase CLI. Starting Docker. Any schema, migration or RLS work. Renaming the live project.

## Known drift, leave alone

`docs/ops/accounts.md` line ~69 says two Free projects in `us-east-1` named `fincwin-dev` and `fincwin-prod`. Reality is one project named `Fincwin United` in `us-west-2`. Real drift, but it belongs to plan `00-03`. Do not edit `accounts.md` in this plan.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Supabase access section to CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Insert a new `## Supabase access` section into `CLAUDE.md`, in the unmarked gap between the `GSD:workflow-end` and `GSD:profile-start` comment markers. It MUST NOT land inside any `GSD:*-start` / `GSD:*-end` pair — those blocks regenerate from their source files and would silently delete it.

State, plainly and without hedging:

- **Do not use the Supabase MCP connector on this project.** It is authorized against a different Supabase account — org `Ealchapp`, project ref `ogbothupjcivwruesgsu` — and cannot see the FincWin project. Verified live: the two accounts' project lists are disjoint. Any read or write through it lands on an unrelated database.
- **Use the Supabase CLI / Management API instead**, authenticated with `SUPABASE_ACCESS_TOKEN` from `.env.local`. That path is verified working against this project.
- **The one live project** is `Fincwin United`, ref `cohmcbdfgqmiwykztrdg`. The repo pins it in `supabase/config.toml`.
- **Dev ref lives in `SUPABASE_DEV_PROJECT_REF`; prod ref in `SUPABASE_PROD_PROJECT_REF`.** Both in `.env.local`, which is gitignored and never committed.
- **`fincwin-prod` does not exist yet** — `SUPABASE_PROD_PROJECT_REF` is intentionally empty.
- **Never run migrations against prod by hand.** Run `npm run supabase:preflight` before any `supabase db push`, or use `npm run supabase:db:push`, which chains the check.

Write it as tight prose plus a short bullet list, matching the file's existing declarative voice. Name the env variables only — never paste a value from `.env.local` into `CLAUDE.md`.
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs");const c=fs.readFileSync("CLAUDE.md","utf8");const i=c.indexOf("## Supabase access");if(i<0)throw new Error("## Supabase access heading missing");const m=[...c.slice(0,i).matchAll(/GSD:[a-z-]+-(start|end)/g)];if(m.length && m[m.length-1][1]!=="end")throw new Error("section sits INSIDE a GSD marker block - it will be regenerated away");const s=c.slice(i);for(const t of ["MCP","Ealchapp","ogbothupjcivwruesgsu","SUPABASE_ACCESS_TOKEN","SUPABASE_DEV_PROJECT_REF","SUPABASE_PROD_PROJECT_REF","cohmcbdfgqmiwykztrdg"]) if(!s.includes(t)) throw new Error("missing required token: "+t); console.log("OK")'</automated>
  </verify>
  <done>`CLAUDE.md` has a `## Supabase access` section outside every GSD marker block, naming the MCP connector as the wrong account, the CLI / Management API as the right path, both ref variables, and the no-hand-migrations rule.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the project ref in supabase/config.toml and fix the two env vars</name>
  <files>supabase/config.toml, .env.local</files>
  <action>
**Create `supabase/config.toml`.** The `supabase/` directory does not exist yet. Keep this minimal — do not invent a full config surface. `supabase link` would normally generate this file, but the CLI is not installed, so it is written by hand:

```toml
# Pinned by hand -- the Supabase CLI is not installed yet.
# Run `supabase link --project-ref cohmcbdfgqmiwykztrdg` once the CLI is
# available, to reconcile this file with what the CLI expects.
# Project: "Fincwin United" (us-west-2) -- the only live FincWin project.
project_id = "cohmcbdfgqmiwykztrdg"
```

`.gitignore` excludes only `supabase/.temp/` and `supabase/.branches/`, so this file is tracked. Stage and commit it.

**Then fix `.env.local`.** Two edits, in the Supabase block around lines 111-113:

1. **Add** `SUPABASE_DEV_PROJECT_REF=cohmcbdfgqmiwykztrdg` directly above the existing `SUPABASE_PROD_PROJECT_REF` line. The variable is absent from the file entirely — this is an insertion, not an edit.
2. **Clear** `SUPABASE_PROD_PROJECT_REF`. It currently holds 59 characters of placeholder text where a real ref is 20 lowercase alphanumerics. Leave it as `SUPABASE_PROD_PROJECT_REF=` with a comment above it noting that `fincwin-prod` does not exist yet and this gets filled in when it does.

**`.env.local` is gitignored (`.gitignore:4`).** Do not stage it, do not commit it, do not `git add -f` it. `git status` will show nothing for it after these edits — that is correct, not a failure, and not a reason to go looking for the change.

Touch no other line in `.env.local`. Do not echo, log or paste any value from that file into terminal output, the summary, or a commit message.
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs");const t=fs.readFileSync("supabase/config.toml","utf8");const m=t.match(/^\s*project_id\s*=\s*"([^"]+)"/m);if(!m)throw new Error("config.toml: project_id not found");if(m[1]!=="cohmcbdfgqmiwykztrdg")throw new Error("config.toml: wrong project_id");const e={};for(const l of fs.readFileSync(".env.local","utf8").split(/\r?\n/)){if(!/^[A-Za-z_][A-Za-z0-9_]*=/.test(l))continue;const i=l.indexOf("=");e[l.slice(0,i).trim()]=l.slice(i+1).trim().replace(/^["']|["']$/g,"");}if(!/^[a-z0-9]{20}$/.test(e.SUPABASE_DEV_PROJECT_REF||""))throw new Error("SUPABASE_DEV_PROJECT_REF missing or malformed");if(e.SUPABASE_DEV_PROJECT_REF!==m[1])throw new Error("dev ref does not match config.toml");if((e.SUPABASE_PROD_PROJECT_REF||"")!=="")throw new Error("SUPABASE_PROD_PROJECT_REF must stay empty until fincwin-prod exists");console.log("OK")'</automated>
    <automated>git check-ignore -q .env.local && test -z "$(git status --porcelain -- .env.local)" && git ls-files --error-unmatch supabase/config.toml >/dev/null && echo OK</automated>
  </verify>
  <done>`supabase/config.toml` exists, is tracked by git, and pins `project_id = "cohmcbdfgqmiwykztrdg"`. `.env.local` has `SUPABASE_DEV_PROJECT_REF` set to the live ref and `SUPABASE_PROD_PROJECT_REF` empty, and remains untracked and unstaged.</done>
</task>

<task type="auto">
  <name>Task 3: Write the preflight assertion script and wire it into package.json</name>
  <files>scripts/supabase-preflight.mjs, package.json</files>
  <action>
Create `scripts/supabase-preflight.mjs`. The `scripts/` directory does not exist yet. Use ESM `.mjs` because `package.json` has no `"type": "module"`. **No new dependencies** — no `dotenv`, no TOML parser, Node builtins only, so it runs on a fresh clone before any install.

Behaviour:

1. Resolve paths from `process.cwd()`.
2. Env file path is `process.env.SUPABASE_PREFLIGHT_ENV_FILE` when set, else `.env.local`. The override exists so the failure path can be tested against a fixture; it is a developer convenience, not a security boundary. Missing file, fail naming the path tried.
3. Parse the env file by hand: skip blank lines and lines starting with `#`; split each remaining line at the **first** `=`; trim; strip one layer of surrounding single or double quotes.
4. Read `supabase/config.toml` and extract the ref with `/^\s*project_id\s*=\s*["']([^"']+)["']/m`. Missing file or missing key, fail naming which.
5. A valid ref is `/^[a-z0-9]{20}$/`.
6. Run **all** of these checks, collecting every failure rather than bailing on the first, then report them together:
   - `config.toml` `project_id` is a valid ref.
   - `SUPABASE_DEV_PROJECT_REF` is present and non-empty.
   - `SUPABASE_DEV_PROJECT_REF` is a valid ref.
   - **The core assertion:** `config.toml` `project_id` equals `SUPABASE_DEV_PROJECT_REF`. On mismatch, print both values, each labelled with the file it came from.
   - If `SUPABASE_PROD_PROJECT_REF` is non-empty: it must be a valid ref, otherwise report "placeholder text, not a project ref"; and it must not equal `config.toml`'s `project_id`, otherwise report "supabase/config.toml is pinned to the PRODUCTION project - refusing".
7. **Secret hygiene, non-negotiable.** The script looks up exactly two keys from the parsed map — `SUPABASE_DEV_PROJECT_REF` and `SUPABASE_PROD_PROJECT_REF` — and prints nothing else from it. Never dump the map, never iterate it into output, never touch `SUPABASE_ACCESS_TOKEN`, `SUPABASE_SERVICE_ROLE_KEY` or any `*_DB_PASSWORD`. Project refs themselves are not secret — they appear inside `EXPO_PUBLIC_SUPABASE_URL` — so printing the two compared refs is correct and required by the failure message.
8. Success: print one line naming the matched ref and both sources, then `process.exit(0)`.
9. Failure: print a `SUPABASE PREFLIGHT FAILED` banner, one line per problem, then `process.exit(1)`. Fail loudly — that is the whole point of the script.

Then add two entries to `package.json` `scripts`, leaving the existing four untouched:

```json
"supabase:preflight": "node scripts/supabase-preflight.mjs",
"supabase:db:push": "node scripts/supabase-preflight.mjs && supabase db push"
```

`supabase db push` will not run yet — the CLI is not installed. That is expected; the wiring is what matters, and on a mismatch the preflight short-circuits before the CLI is ever reached.
  </action>
  <verify>
    <automated>node scripts/supabase-preflight.mjs && node -e 'const p=require("./package.json");for(const k of ["supabase:preflight","supabase:db:push"]) if(!p.scripts||!p.scripts[k]) throw new Error("package.json missing script: "+k); if(!p.scripts["supabase:db:push"].includes("supabase-preflight")) throw new Error("db:push is not gated by the preflight"); console.log("OK")'</automated>
    <automated>node -e 'require("fs").writeFileSync(".preflight-fixture.tmp","SUPABASE_DEV_PROJECT_REF=aaaaaaaaaaaaaaaaaaaa\nSUPABASE_PROD_PROJECT_REF=\n")'; OUT=$(SUPABASE_PREFLIGHT_ENV_FILE=.preflight-fixture.tmp node scripts/supabase-preflight.mjs 2>&1); C=$?; rm -f .preflight-fixture.tmp; test "$C" -ne 0 && printf '%s' "$OUT" | grep -q aaaaaaaaaaaaaaaaaaaa && printf '%s' "$OUT" | grep -q cohmcbdfgqmiwykztrdg && echo OK</automated>
    <automated>node -e 'const s=require("fs").readFileSync("scripts/supabase-preflight.mjs","utf8");for(const bad of ["SUPABASE_ACCESS_TOKEN","SERVICE_ROLE","DB_PASSWORD"]) if(s.includes(bad)) throw new Error("preflight references a secret key it must never touch: "+bad); console.log("OK")'</automated>
  </verify>
  <done>`npm run supabase:preflight` exits 0 against the real `.env.local`. Against a fixture with a wrong dev ref it exits non-zero and its output names both refs. `supabase:db:push` is gated by the preflight. The script contains no reference to any secret env key, and leaves no fixture file behind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo files → agent context | `CLAUDE.md` and `supabase/config.toml` are read by every session and are what the agent trusts about which database is ours |
| developer shell → Supabase Management API | `supabase db push` and Management API calls mutate a live hosted database; there is no undo on an empty-schema project that turns out to be someone else's |
| `.env.local` → terminal output, git, summaries | the file holds an access token, a service-role key and a DB password alongside the two refs this plan touches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-s7g-01 | Information disclosure | `scripts/supabase-preflight.mjs` output | mitigate | Script looks up exactly two ref keys and prints nothing else from the parsed env map; task 3 verify greps the source for `SUPABASE_ACCESS_TOKEN`, `SERVICE_ROLE` and `DB_PASSWORD` and fails if any appears |
| T-s7g-02 | Tampering | `supabase db push` against the wrong project | mitigate | `supabase:db:push` chains the preflight with `&&`, so a non-zero exit blocks the push before the CLI is invoked |
| T-s7g-03 | Information disclosure | `.env.local` reaching git | mitigate | Already gitignored at `.gitignore:4`; task 2 verify asserts `git check-ignore` succeeds and the porcelain status for the file is empty |
| T-s7g-04 | Elevation of privilege | `config.toml` pinned to prod, then `db push` run | mitigate | Preflight refuses when `config.toml`'s `project_id` equals a non-empty `SUPABASE_PROD_PROJECT_REF` |
| T-s7g-05 | Spoofing | `SUPABASE_PREFLIGHT_ENV_FILE` override redirects the check at a fixture | accept | Developer-local convenience for testing the failure path. Anyone able to set it can already edit `.env.local` directly — this is a wrong-target guard, not a security boundary |
| T-s7g-06 | Repudiation | CLAUDE.md guidance silently deleted by GSD marker-block regeneration | mitigate | Section placed outside every `GSD:*-start` / `GSD:*-end` pair; task 1 verify asserts the last marker before the heading is an `-end` |
</threat_model>

<verification>
Run from the repo root, all three must pass:

1. `node -e '...'` CLAUDE.md check — section present, outside GSD markers, naming the MCP connector, the wrong account, both ref vars and the live ref.
2. `node run supabase:preflight` equivalent: `npm run supabase:preflight` exits 0.
3. Fixture negative test: preflight against a wrong dev ref exits non-zero and prints both compared refs.

Then confirm the commit contains `CLAUDE.md`, `supabase/config.toml`, `scripts/supabase-preflight.mjs` and `package.json` — and **not** `.env.local`:

```
git show --stat --name-only HEAD
```
</verification>

<success_criteria>
- `CLAUDE.md` carries a `## Supabase access` section outside every GSD marker block, telling any session not to use the Supabase MCP connector and to use the CLI / Management API with `SUPABASE_ACCESS_TOKEN` instead.
- `supabase/config.toml` is tracked by git and pins `project_id = "cohmcbdfgqmiwykztrdg"`, with a comment pointing at `supabase link` for reconciliation once the CLI is installed.
- `npm run supabase:preflight` exits 0 today and exits non-zero, naming both refs, when they disagree.
- `npm run supabase:db:push` cannot reach the Supabase CLI without the preflight passing first.
- `.env.local` has `SUPABASE_DEV_PROJECT_REF=cohmcbdfgqmiwykztrdg` and an empty `SUPABASE_PROD_PROJECT_REF`, and is neither staged nor committed.
- No secret value appears in any committed file, terminal transcript or summary.
- `fincwin-prod` was not created. The Supabase CLI was not installed. No schema or migration work happened.
</success_criteria>

<output>
After completion, create `.planning/quick/260922-s7g-supabase-project-selection-guardrails/260922-s7g-SUMMARY.md`.

Commit the four tracked files in one commit, e.g. `chore(supabase): pin project ref and add selection guardrails`. `.env.local` is gitignored and stays out of it.
</output>
